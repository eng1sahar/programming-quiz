# أسئلة مؤتمتة — المحاضرة 7: الصفوف (Classes)
### إعداد وفق أسلوب د. أسامه ناصر — مقرر البرمجة 2

> **ملاحظة:** بفرض أن كل المكتبات المطلوبة قد تم إدراجها في البرنامج (`#include<iostream>`, `#include<string>`, `using namespace std;`).
> عدد الأسئلة: 30 سؤالاً (اختيار من متعدد / صح-خطأ / تحليل كود) — نفس نمط نموذج امتحان الدكتور.

---

## القسم الأول: مفاهيم عامة والصفوف (Class Basics)

### السؤال 1
ما الفرق الأساسي بين تعريف `struct` وتعريف `class` في ++C كما ورد في المحاضرة؟

A. لا يوجد أي فرق بينهما إطلاقاً
B. الصف `class` يمتلك محددات وصول (access modifiers) وتوابع (methods) بالإضافة إلى الحقول، بينما السجل التقليدي لا يحتوي إلا على الحقول
C. السجل `struct` أسرع من الصف `class` في التنفيذ
D. الصف `class` لا يمكن أن يحتوي حقولاً من نوع مصفوفة

**الإجابة الصحيحة: B**

**الشرح:** كما ورد في شريحة "الصفوف"، الصف يشابه السجل من الناحية التقنية لكنه يمتلك بالإضافة للحقول مهام (توابع) ومحددات وصول، وهذا هو جوهر البرمجة الغرضية التوجه.

---

### السؤال 2
بفرض لدينا الصف التالي:
```cpp
class student{
private:
    long id;
    string firstName;
public:
    void changeMarks(int i, int v);
};
```
أين يجب أن يُكتب جسم التابع `changeMarks` وفق الطريقة المعتمدة التي ذكرها الدكتور؟

A. داخل تعريف الصف مباشرة فقط، ولا توجد طريقة أخرى
B. خارج الصف باستخدام عامل تحديد النطاق `::` بالشكل `student::changeMarks(...)`
C. في تابع `main` مباشرة
D. لا يمكن كتابة جسم التابع إطلاقاً إن كان الصف يحوي حقولاً خاصة

**الإجابة الصحيحة: B**

**الشرح:** ذكرت المحاضرة أنه "يمكننا كتابة كود التابع ضمن تعريفه في الصف مباشرة، إلا أن الطريقة المعتمدة هي كتابته خارج الصف"، باستخدام `ClassName::methodName`.

---

### السؤال 3
عند كتابة `void student::changeMark(int i,int v){...}` فإن مجال رؤية هذا التابع:

A. معزول تماماً ولا يرى أي شيء خارج وسائطه المحلية
B. يتضمن حقول الصف الذي هو عضو فيه بالإضافة إلى مجاله المحلي، وهذا استثناء من قاعدة عزل مجال رؤية التوابع
C. يرى كل متحولات البرنامج بما فيها المتحولات العامة (Global) فقط دون حقول الصف
D. يعتمد فقط على ترتيب استدعائه في `main`

**الإجابة الصحيحة: B**

**الشرح:** التابع عادة يُعتبر مجال رؤية معزول عن المستدعي، لكن **الاستثناء** هو أن يكون التابع عضواً ضمن صف، عندها يصبح مجال رؤيته شاملاً لحقول الصف الذي هو عضو فيه، وهذا ما يسمح باستخدام `marks` مباشرة داخل `changeMarks` دون الحاجة لتمريرها كوسيط.

---

## القسم الثاني: محددات الوصول (Access Modifiers)

### السؤال 4
أي عبارة صحيحة بخصوص المحدد `private`؟

A. يمكن لأي تابع خارج الصف الوصول لعناصره طالما هو ضمن نفس الملف
B. لا يستطيع الوصول إلى العناصر الموسومة بـ private سوى التوابع الأعضاء ضمن الصف نفسه (والأصدقاء لاحقاً)
C. المحدد private مخصص فقط للحقول ولا يمكن تطبيقه على التوابع
D. لا فرق عملي بين private و public في لغة ++C

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة صراحة: "المحدد private يعني أن التوابع والحقول الموجودة هنا خاصة، لا أحد قادر على الوصول إليها سوى التوابع ضمن الصف نفسه والأصدقاء (لاحقاً)".

---

### السؤال 5
الهدف الأساسي من استخدام محددات الوصول (كإخفاء الحقول ضمن private) هو تحقيق مبدأ:

A. الوراثة (Inheritance)
B. التغليف وإخفاء التفاصيل (Encapsulation)
C. التعددية الشكلية (Polymorphism)
D. إعادة استخدام البرمجيات فقط (Software Reuse)

**الإجابة الصحيحة: B**

**الشرح:** ورد في المحاضرة: "تشجع البرمجة الغرضية التوجه على مبدأ التغليف وإخفاء التفاصيل... لا يهمنا كيف يعمل الغرض على تخزين معلوماته، المهم أننا نحصل على المعلومات التي نريدها وقت ما نريدها بالشكل الصحيح".

---

## القسم الثالث: البناء والهدم (Constructors & Destructors)

### السؤال 6
بفرض الصف `student` وفيه الباني التالي:
```cpp
student::student(long id, string firstName, string lastName, float points){
    id = id;
    firstName = firstName;
    ...
}
```
لماذا يُعتبر السطر `id = id;` **خاطئاً منطقياً** رغم أنه صحيح من الناحية القواعدية (syntax)؟

A. لأن المترجم يرفض ترجمة هذا السطر أساساً
B. لأن اسم الوسيط `id` يتشارك بالاسم مع حقل الصف `id`، والمترجم يعتبر أن `id` تشير إلى الوسيط الأقرب في مجال الرؤية (وليس الحقل)، وبالتالي الحقل لا يتم إسناد قيمة حقيقية له
C. لأن الحقول من النوع `long` لا يمكن إسنادها ضمن الباني
D. لأن ترتيب الوسائط في الباني غير صحيح

**الإجابة الصحيحة: B**

**الشرح:** كما وضّحت المحاضرة، عند تشابه اسم الوسيط مع اسم الحقل، "الوسيط أقرب للتابع من الحقل"، لذلك يعتبر المترجم أن `id` تشير إلى الوسيط لا الحقل، فتصبح العبارة إسناداً للوسيط لنفسه دون أي تأثير على حالة الغرض.

---

### السؤال 7
ما الحل الذي اقترحته المحاضرة لمعالجة مشكلة السؤال السابق؟

A. تغيير نوع البيانات للحقل
B. استخدام المؤشر `this` للإشارة إلى الحقل، مثل `this->id = id;`، أو إعادة تسمية الوسيط
C. حذف الحقل من الصف نهائياً
D. استخدام الكلمة المفتاحية `static` قبل اسم الحقل

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة: "ما الحل؟ لدينا حلان، الأول إعادة تسمية الوسيط والثاني استخدام المؤشر this".

---

### السؤال 8
المؤشر `this` ضمن باني الصف `student` عند تنفيذ السطر:
```cpp
student x(1,"a","b",232);
```
تكون قيمته:

A. تشير دوماً إلى أول غرض تم إنشاؤه في البرنامج بغض النظر عن اسمه
B. `this = &x` أي يشير إلى عنوان الغرض الذي استدعى هذا الباني (x)
C. تساوي `NULL` حتى انتهاء تنفيذ الباني
D. لا معنى له داخل الباني تحديداً

**الإجابة الصحيحة: B**

**الشرح:** ورد صراحة: "في هذا الاستدعاء قام الغرض x باستدعاء الباني ذو الوسطاء، وعليه فإن قيمة this ضمن هذا الباني تشير إلى الغرض x أي أن `this=&x`".

---

### السؤال 9
هل يتوجب استخدام المؤشر `this` عند الوصول إلى المتحول `marks` (مصفوفة العلامات) داخل الباني، إذا لم يكن هناك وسيط بنفس الاسم `marks`؟

A- صح: لا نحن ملزمون باستخدامه دائماً حتى لو لم يوجد تشابه بالأسماء
B- خطأ: طالما لا يوجد متحول محلي (وسيط) يحمل نفس اسم الحقل، فنحن غير ملزمين باستخدام `this`

**الإجابة الصحيحة: B**

**الشرح:** ورد في المحاضرة: "لعدم تعريف المتحول marks ضمن التابع لسنا ملزمين باستخدام المؤشر this"، أي أن استخدام `this` ضروري فقط لحل تعارض التسمية.

---

### السؤال 10
ما هي أنواع البواني الثلاثة التي ذكرتها المحاضرة في لغة ++C؟

A. الباني الافتراضي، الباني الساكن، الباني الوراثي
B. الباني الافتراضي، الباني ذو الوسطاء، الباني الناسخ
C. الباني العام، الباني الخاص، الباني المحمي
D. باني الحقول، باني التوابع، باني الصف

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة حرفياً: "في لغة ++C لدينا 3 أنواع من البواني: الباني الافتراضي (الذي لا يحتاج وسطاء)، الباني ذو الوسطاء، الباني الناسخ".

---

### السؤال 11
عند كتابة التعريف التالي:
```cpp
student x();
student y;
```
ما الفرق بين استدعاء `x` و `y`؟

A. `x` يستدعي الباني ذو الوسطاء بينما `y` يستدعي الباني الافتراضي
B. كلاهما يستدعيان الباني الافتراضي، لأن عدم وضع أي قوس بعد اسم المتحول (كما في `y`) يستدعي الباني الافتراضي؛ ووضع قوسين فارغين (كما في `x`) يُفسَّر أيضاً كاستدعاء له
C. السطر الخاص بـ `y` يحتاج بالضرورة لقوسين فارغين وإلا فهو خطأ ترجمة
D. لا يمكن تعريف غرضين من نفس الصف بطريقتين مختلفتين

**الإجابة الصحيحة: B**

**الشرح:** طبقاً للمحاضرة: "هنا نلاحظ أننا لم نضع أي قوس بعد اسم المتحول المعبر عن الغرض، هذه الطريقة في الكتابة تستدعي الباني الافتراضي" (بخصوص `y`)، بينما استدعاء `x()` يستدعي الباني بدون وسطاء أيضاً (وهي الطريقة القياسية لاستدعاء الباني بشكل عام في هذا السياق).

---

### السؤال 12
لماذا يُمرَّر الوسيط في الباني الناسخ بالمرجع كما في:
```cpp
student::student(student& c){ ... }
```
A. لأن ++C لا تسمح بتمرير الأغراض إلا بالمرجع مطلقاً
B. لتجنب استنساخ الغرض (نسخة إضافية) في الذاكرة عند التمرير
C. لأن التمرير بالقيمة يسبب خطأ ترجمة دائماً مع الأصناف
D. لتسريع عملية القراءة من الملفات فقط

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة: "مررنا الوسيط c بالمرجع لتجنب استنساخ الغرض في الذاكرة".

---

### السؤال 13
ما وظيفة الهادم (Destructor)؟

A. إسناد قيم ابتدائية لحقول الغرض عند إنشائه
B. تنظيم بعض العمليات أثناء حذف الغرض من الذاكرة، مثل إغلاق الملفات وحذف المؤشرات والمصفوفات الديناميكية
C. نسخ قيم غرض إلى غرض آخر
D. تحويل الصف إلى سجل عند الحاجة

**الإجابة الصحيحة: B**

**الشرح:** ورد بالنص: "يُستخدم الهادم لتنظيم بعض العمليات أثناء حذف الغرض من الذاكرة، يسمح لنا ذلك بإغلاق الملفات وحذف المؤشرات والمصفوفات الديناميكية من الذاكرة في المراحل الأخيرة من عمر الغرض في الذاكرة".

---

### السؤال 14
هل يمتلك الهادم أي وسطاء؟

A- صح، يمكن أن يمتلك عدداً من الوسطاء كأي تابع عادي
B- خطأ، الهادم لا يمتلك أي وسيط، وله نفس اسم الصف يسبقه الرمز `~`

**الإجابة الصحيحة: B**

**الشرح:** نص صريح من المحاضرة: "لا يمتلك الهادم أي وسيط، وله نفس اسم الصف يسبقه الرمز ~".

---

### السؤال 15
إذا لم يُعرِّف المبرمج أي باني أو هادم ضمن الصف، فإن المترجم:

A. يرفض ترجمة الصف نهائياً
B. يضيف باني فارغاً بدون وسطاء وهادماً فارغاً تلقائياً
C. يمنع إنشاء أي غرض من هذا الصف
D. يستخدم باني الصف الأب تلقائياً بغض النظر عن وجود وراثة

**الإجابة الصحيحة: B**

**الشرح:** ورد بالنص: "إذا لم يتم تعريف أي هادم يتم إضافة هادم فارغ" و"إذا لم يتم تعريف أي باني يتم إضافة باني فارغ بدون وسطاء".

---

### السؤال 16
بفرض الكود التالي (بتصرف عن مثال المحاضرة):
```cpp
student gl(10,"first","First",22);
int main(){
    cout << "main starts" << endl;
    student mn(11,"second","Second",33);
    for(int i=0;i<3;i++)
        student mm(12+i,"third","Third",44);
    cout << "main ending" << endl;
    return 0;
}
```
متى يتم استدعاء باني الغرض العام (Global) `gl` بالنسبة لبداية تنفيذ `main`؟

A. بعد طباعة "main starts"
B. قبل تنفيذ أي تابع في البرنامج، أي قبل الدخول إلى `main` نفسه
C. عند نهاية البرنامج فقط
D. لا يتم استدعاؤه إطلاقاً لأنه معرف خارج `main`

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة: "يتم بناء الأغراض المعرفة بصفة متحولات عامة Global قبل استدعاء أي تابع في البرنامج"، وهذا يفسر ظهور "Building student with Id: 10" قبل عبارة "main starts" في مثال الخرج.

---

### السؤال 17
في المثال نفسه، متى يتم استدعاء هادم الغرض `mm` المُعرَّف داخل حلقة `for`؟

A. عند نهاية البرنامج فقط مع بقية الأغراض
B. في نهاية كل تكرار من الحلقة (نهاية مجال الرؤية الذي عُرِّف الغرض ضمنه)
C. لا يُستدعى الهادم إطلاقاً لأغراض المصفوفات
D. قبل بدء الحلقة

**الإجابة الصحيحة: B**

**الشرح:** ورد في المحاضرة: "يُستدعى الهادم من قبل المترجم في آخر سطر من مجال الرؤية الذي تم تعريف الغرض فيه"، ولأن `mm` يُعرَّف من جديد داخل جسم الحلقة، فهو يُبنى ويُهدم في كل تكرار (كما يظهر في مثال الخرج المرفق بالمحاضرة بالتناوب Building/Destroying).

---

### السؤال 18
عند تعريف متحول ساكن (static) من نوع صف داخل تابع، متى يُستدعى الباني الخاص به؟

A. في كل مرة يُستدعى فيها التابع
B. لمرة واحدة فقط، عند الوصول لسطر تعريف المتحول الساكن للمرة الأولى
C. عند بداية تنفيذ البرنامج بغض النظر عن استدعاء التابع
D. لا يُستدعى الباني إطلاقاً للمتحولات الساكنة

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة صريح: "يتم استدعاء الباني لمرة واحدة فقط عند الوصول لسطر تعريف المتحول الساكن للمرة الأولى".

---

### السؤال 19
إذا لم يُنهَ تنفيذ البرنامج بشكل سليم (كحدوث توقف مفاجئ)، فإن هوادم الأغراض العامة (Global) والساكنة (static):

A- صح: تُستدعى دائماً بغض النظر عن طريقة إنهاء البرنامج
B- خطأ: لن تُستدعى إذا لم يتم إنهاء البرنامج بشكل سليم

**الإجابة الصحيحة: B**

**الشرح:** كررت المحاضرة في الحالتين (Global و static): "إذا لم يتم إنهاء البرنامج بشكل سليم لن يُستدعى الهادم".

---

## القسم الرابع: Getters & Setters

### السؤال 20
لماذا نحتاج إلى توابع `getters` ضمن الصف؟

A. لتعديل القيم الخاصة (private) مباشرة من خارج الصف
B. لقراءة قيم الحقول الخاصة (private) من خارج الصف دون كسر مبدأ التغليف
C. لحذف الحقول من الذاكرة
D. لا حاجة فعلية لها إن كانت جميع الحقول public

**الإجابة الصحيحة: B**

**الشرح:** طرحت المحاضرة السؤال: "كيف يمكن لنا أن نقرأ قيم الحقول بعد البناء في الغرض إذا كانت هذه الحقول private؟" وأجابت: "الحل بكل بساطة باستخدام توابع تُعرف باسم getters".

---

### السؤال 21
بفرض الكود التالي مأخوذاً من ملف `student.h`:
```cpp
float getPoint();
int getMark(int);
void setId(long);
```
ما التصنيف الصحيح لهذه التوابع؟

A. `getPoint` و `getMark` هما setters بينما `setId` هو getter
B. `getPoint` و `getMark` هما getters (تعيدان قيمة)، بينما `setId` هو setter (يعدّل قيمة حقل)
C. جميعها من نوع getters فقط
D. جميعها من نوع setters فقط

**الإجابة الصحيحة: B**

**الشرح:** التوابع التي تبدأ بـ `get` وتُعيد قيمة الحقل الخاص هي getters، بينما التوابع التي تبدأ بـ `set` وتُعدّل قيمة الحقل (دون أن تُعيد بالضرورة قيمة مفيدة) هي setters، تماماً كما فُصِّل في شرائح المحاضرة.

---

## القسم الخامس: فوائد الصفوف والوراثة والتركيب

### السؤال 22
ما الفرق بين "التركيب" (Composition) و"الوراثة" (Inheritance) كما وردا ضمن فوائد الصفوف؟

A. التركيب هو استخدام غرض ضمن غرض آخر، بينما الوراثة هي إنشاء صف جديد يرث صفاً قديماً
B. لا فرق بينهما، وهما مصطلحان مترادفان
C. التركيب هو الوراثة المتعددة، والوراثة هي التركيب الفردي
D. التركيب يُستخدم فقط في ملفات .h بينما الوراثة تُستخدم فقط في ملفات .cpp

**الإجابة الصحيحة: A**

**الشرح:** نص المحاضرة تحت "إعادة استخدام البرمجيات (Software Reuse)": "التركيب Composition: استخدام غرض ضمن غرض" و"الوراثة Inheritance: صف جديد يرث صف قديم".

---

### السؤال 23
مفهوم "الوجهات" (Interface) في سياق فوائد الصفوف يعني:

A. الواجهات الرسومية للمستخدم (GUI) حصراً
B. أنه لا يهم كيف يعمل الغرض داخلياً، المهم أننا نحصل على النتيجة المطلوبة عبر التوابع العامة المتاحة
C. مجموعة الملفات التي تحمل الامتداد .h فقط دون أي معنى آخر
D. آلية لتحويل الصف struct إلى class

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة: "الوجهات... لا نقصد هنا الواجهات البرمجية GUI... نقصد Programming Interface، ماذا يعني هذا؟ لا يهم كيف يعمل الغرض في الداخل، المهم أننا نحصل على النتيجة المطلوبة".

---

## القسم السادس: مجال الرؤية والوصول إلى عناصر الصف

### السؤال 24
بفرض لدينا:
```cpp
Class1 c;
Class1 *c1 = new Class1();
```
أي من الاستدعاءات التالية **خاطئة** للوصول إلى تابع `function1` من الصف؟

A. `c.function1();`
B. `c1->function1();`
C. `(*c1).function1();`
D. `c1.function1();`

**الإجابة الصحيحة: D**

**الشرح:** بما أن `c1` مؤشر إلى غرض (Pointer)، فلا يمكن استخدام عامل النقطة `.` مباشرة عليه؛ الطرق الصحيحة هي إما `c1->function1()` أو تفكيك المؤشر أولاً `(*c1).function1()`، كما وضّحت شريحة "مجال الرؤية في الصف".

---

### السؤال 25
إذا احتوى تابع عضو على متحول محلي يحمل نفس اسم حقل ضمن الصف، فإن قاعدة "الأقرب أولاً" في مجال الرؤية تعني أن:

A. المترجم يرفض الترجمة دوماً في هذه الحالة
B. التابع سيرى المتحول الخاص به (المحلي/الوسيط) وليس حقل الصف، إلا إذا استُخدم `this` أو `ClassName::VariableName` لتمييز الحقل
C. حقل الصف يُلغى نهائياً من الذاكرة
D. القيمتان تندمجان تلقائياً بمتحول واحد

**الإجابة الصحيحة: B**

**الشرح:** طبقاً لشريحة "مجال الرؤية في الصف": "في حال وجد ضمن التابع متحول يحمل نفس اسم متحول/حقل ضمن الصف فوفقاً لقواعد مجال الرؤية (الأقرب أولاً) سيرى التابع المتحول الخاص به وليس حقل الصف؛ لحل هذه المشكلة نستخدم إما المتحول this أو ClassName::VariableName".

---

## القسم السابع: فصل الكود عن الواجهة (Header/Source Separation)

### السؤال 26
ما الغرض الأساسي من فصل تعريف الصف (ملف .h) عن تنفيذه (ملف .cpp)؟

A. تسريع عملية الترجمة فقط دون أي فائدة أخرى
B. حماية الكود المصدري من السرقة والتلاعب، حيث يمكن توزيع ملفات .h مع ملفات تنفيذية (dll) دون كشف المنطق البرمجي الفعلي
C. لتقليل حجم البرنامج التنفيذي فقط
D. لأن لغة ++C تفرض هذا الفصل إجبارياً في كل الحالات

**الإجابة الصحيحة: B**

**الشرح:** نص المحاضرة: "عندما ترغب بتوزيع الكود الذي تعبت وأنت تكتبه، كي تتجنب سرقته والتلاعب به تلجأ لتحويله إلى ملفات تنفيذية (صيغة dll)... تقوم بإرسال الملفات الرئيسية headers مع الملفات التنفيذية dll... وبالتالي تضمن ألّا يتم التلاعب بالكود الخاص بك".

---

### السؤال 27
عند تضمين مكتبات جاهزة من لغة ++C مثل `iostream` أو `string`، بينما نُضمِّن ملفنا الخاص `student.h`، ما الفرق في صيغة `#include`؟

A. لا فرق إطلاقاً، ويمكن استخدام `<>` أو `""` بلا تمييز
B. المكتبات الجاهزة تُضمَّن ضمن أقواس `<>` بينما الملف الخاص بنا يُضمَّن ضمن علامتي تنصيص `" "`
C. المكتبات الجاهزة تُضمَّن بعلامتي تنصيص، والملف الخاص بأقواس `<>`
D. لا يمكن تضمين أي ملف خاص بنا بتاتاً

**الإجابة الصحيحة: B**

**الشرح:** ورد بالمحاضرة: "نلاحظ الفرق بين عمليتي include للملفات التي جاءت مع ++C (مثل iostream وstring) بأنها ضمن `<>`، أما ملفنا الخاص فهو ضمن `" "`".

---

### السؤال 28
ما وظيفة الأسطر التالية الموجودة في بداية ونهاية ملف `student.h`؟
```cpp
#ifndef STUDENT_H
#define STUDENT_H
...
#endif
```
A. لا وظيفة فعلية لها، هي أسطر اختيارية بحتة
B. حراسة رأس الملف (Header Guard) لمنع تضمين محتوى الملف أكثر من مرة ضمن نفس الترجمة، وبذلك تجنّب أخطاء إعادة التعريف
C. لتشفير محتوى الملف
D. لتحديد اسم الصف فقط

**الإجابة الصحيحة: B**

**الشرح:** ذكرت المحاضرة أن "السطر الأول والثاني بدء عملية تعريف الملف الرئيسي" و"السطر الأخير إنهاء عملية التعريف"، وهذه الآلية (`ifndef/define/endif`) هي حارس تضمين قياسي في ++C يمنع تكرار تعريف الصف عند تضمين نفس الملف أكثر من مرة.

---

## القسم الثامن: مشكلة المرجع (Reference Problem)

### السؤال 29
بفرض التعديل التالي على تابع `getId`:
```cpp
long& student::getId(){ return id; }
```
ثم في `main`:
```cpp
student c;
long& x = c.getId();
x = 10;
cout << c.getId();
```
ما ناتج الطباعة، ولماذا؟

A. تُطبع القيمة الأصلية لـ id دون تغيير، لأن `getId` تعيد نسخة من القيمة
B. تُطبع القيمة 10، لأن إعادة القيمة بالمرجع تجعل التعديل على `x` يؤثر مباشرة على الحقل الخاص `id` المخزّن ضمن الغرض، متجاوزاً بذلك قواعد محددات الوصول (private) ودون المرور بـ `setId`
C. خطأ ترجمة لأن الحقل `id` خاص (private)
D. خطأ تنفيذ (Runtime Error)

**الإجابة الصحيحة: B**

**الشرح:** هذا بالضبط ما وضحته المحاضرة: "تكون قيمة الخرج 10 مما يعني أن التعديل على القيمة المعادة بالمرجع أثر على القيمة المخزنة ضمن الغرض دون المرور بالتابع setId، ضاربين بعرض الحائط قواعد محددات الوصول". لذلك في هذه الحالة **لا يجب** استخدام الإعادة بالمرجع لحقول خاصة.

---

### السؤال 30
ما الدرس المستفاد الذي يجب تطبيقه عند كتابة توابع `getters` لحقول من نوع مركّب (كالمصفوفات أو الكائنات) بحيث لا يُخترق مبدأ التغليف؟

A. يجب دائماً إعادة القيمة بالمرجع (`&`) لتسريع الأداء بغض النظر عن العواقب
B. يجب تجنّب إعادة القيمة بالمرجع للحقول الخاصة، أو إعادتها كـ `const` مرجع على الأقل، لمنع تعديل الحالة الداخلية للغرض من خارج توابعه
C. يجب حذف جميع توابع getters نهائياً
D. لا فرق بين الإعادة بالقيمة أو بالمرجع في أي حال من الأحوال

**الإجابة الصحيحة: B**

**الشرح:** خلصت المحاضرة إلى أن "في هذه الحالة لا يجب استخدام الإعادة بالمرجع"، لأن ذلك يسمح بتعديل حالة الغرض الداخلية مباشرة متجاوزاً محددات الوصول (private) والتوابع المخصصة (setters) التي قد تحتوي على قيود تحقق من صحة القيم (Validation)، وبالتالي يُفقد الهدف من التغليف بالكامل.

---

## جدول الإجابات السريع (Answer Key)

| # | الإجابة | # | الإجابة | # | الإجابة |
|---|---------|---|---------|---|---------|
| 1 | B | 11 | B | 21 | B |
| 2 | B | 12 | B | 22 | A |
| 3 | B | 13 | B | 23 | B |
| 4 | B | 14 | B | 24 | D |
| 5 | B | 15 | B | 25 | B |
| 6 | B | 16 | B | 26 | B |
| 7 | B | 17 | B | 27 | B |
| 8 | B | 18 | B | 28 | B |
| 9 | B | 19 | B | 29 | B |
| 10 | B | 20 | B | 30 | B |

---

*تم إعداد هذه الأسئلة بالاعتماد الحصري على محتوى محاضرة "الصفوف" (المحاضرة 7) ونمط الأسئلة المعتمد في نموذج امتحان د. أسامه ناصر / د. محسن حسين لمقرر البرمجة 2.*
