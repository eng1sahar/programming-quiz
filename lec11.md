# أسئلة مؤتمتة – مقرر البرمجة 2 (نظري)
## المحاضرة الحادية عشر: الوراثة بالأمثلة (2) – شجرة Polygon

**إعداد آلي بأسلوب امتحانات د. أسامه ناصر / د. محسن حسين**
**عدد الأسئلة: 26 | زمن مقترح: 45 دقيقة**

> ملاحظة: هذه الأسئلة اجتهاد آلي مبني على محتوى شريحة المحاضرة 11 وعلى تحليل أسلوب النموذج الامتحاني المرفق (نمط اختيار من متعدد، صح/خطأ، وتتبّع كود). لا تُعتبر بديلاً رسميًا عن أسئلة الدكتور، بل أداة تدريب ومراجعة.

---

## الكود المرجعي المستخدم في الأسئلة

```cpp
class Polygon {
private:
    const int numAngles;
    const int numSides;
protected:
    std::vector<double> angles;
    std::vector<double> sides;
public:
    Polygon(int anglesCount = 0, int sidesCount = 0)
        : numAngles(anglesCount), numSides(sidesCount),
          angles(anglesCount, 0.0), sides(sidesCount, 0.0) {}

    virtual ~Polygon() {}

    void setSides(const std::vector<double>& sideLengths) {
        if (sideLengths.size() == sides.size()) {
            sides = sideLengths;
        }
    }

    double circumference() const {
        double total = 0;
        for (double side : sides) total += side;
        return total;
    }

    virtual double area() const { return 0.0; }
};

class Triangle : public Polygon {
public:
    Triangle() : Polygon(3, 3) {}
    double area() const override {
        double s = circumference() / 2.0;
        return std::sqrt(s * (s - sides[0]) * (s - sides[1]) * (s - sides[2]));
    }
};

class Quadrilateral : public Polygon {
public:
    Quadrilateral() : Polygon(4, 4) {}
};

class Parallelogram : public Quadrilateral {
protected:
    double height;
public:
    Parallelogram(double h = 0.0) : Quadrilateral(), height(h) {}
    void setHeight(double h) { height = h; }
    double area() const override { return sides[0] * height; }
};

class Trapezoid : public Quadrilateral {
private:
    double height;
public:
    Trapezoid(double h = 0.0) : Quadrilateral(), height(h) {}
    void setHeight(double h) { height = h; }
    double area() const override {
        return ((sides[0] + sides[2]) / 2.0) * height;
    }
};

class Rectangle : public Parallelogram {
public:
    Rectangle() : Parallelogram() {}
    double area() const override { return sides[0] * sides[1]; }
};

class Square : public Rectangle {
public:
    Square() : Rectangle() {}
};
```

---

## القسم الأول: اختر الإجابة الصحيحة (الأسئلة 1 – 18، 3 درجات لكل سؤال)

### السؤال 1
ما هو ترتيب توارث الصفوف الصحيح لصف `Rectangle` بدءًا من الجذر؟

A- `Polygon → Rectangle`
B- `Polygon → Quadrilateral → Rectangle`
C- `Polygon → Quadrilateral → Parallelogram → Rectangle`
D- `Polygon → Triangle → Rectangle`

**الإجابة الصحيحة: C**
**التوضيح:** حسب مخطط الشجرة في المحاضرة، يرث `Rectangle` من `Parallelogram`، ويرث `Parallelogram` من `Quadrilateral`، ويرث `Quadrilateral` من `Polygon`. أي أن `Rectangle` صف حفيد بثلاث مستويات وراثة.

---

### السؤال 2
لماذا كُتب باني `Polygon` بالشكل التالي؟
```cpp
Polygon(int anglesCount = 0, int sidesCount = 0)
    : numAngles(anglesCount), numSides(sidesCount),
      angles(anglesCount, 0.0), sides(sidesCount, 0.0) {}
```

A- لأن `numAngles` و `numSides` من نوع `const` فيجب إسنادهما ضمن لائحة التهيئة فقط، ولبناء الشعاعين مباشرةً بالحجم المطلوب وقيمة ابتدائية 0.0
B- لتسريع تنفيذ البرنامج فقط دون أي داعٍ بنيوي
C- لأن لغة ++C تفرض تهيئة كل الحقول ضمن لائحة التهيئة حتى لو لم تكن `const`
D- لأن الشعاعين `vector` لا يمكن إسنادهما داخل جسم الباني إطلاقًا

**الإجابة الصحيحة: A**
**التوضيح:** الحقول الثابتة (`const`) لا يمكن إسنادها إلا في initializer list وليس داخل جسم الباني، وقد استُخدم مُنشئ `vector(count, value)` لبناء الشعاعين مباشرة بالحجم المطلوب مع تصفير كل العناصر.

---

### السؤال 3
لماذا كُتب الهادم `~Polygon()` على الشكل التالي: `virtual ~Polygon() {}`؟

A- لأن كل هادم في ++C يجب أن يكون virtual إجباريًا
B- لضمان استدعاء الهادم الصحيح للصف الابن عند حذف عنصر عبر مؤشر من نوع الصف الأب (تفادي تسرب الذاكرة)
C- لأن الصف Polygon لا يحتوي حقولاً ديناميكية فلا داعي لهادم أصلاً
D- لتسريع عملية الحذف

**الإجابة الصحيحة: B**
**التوضيح:** بما أن البرنامج يتعامل مع الأشكال عبر مؤشرات من نوع `Polygon*` (تعددية الأشكال)، فإن جعل الهادم افتراضيًا (`virtual`) يضمن عند تنفيذ `delete` عبر مؤشر الأب استدعاء هادم الصف الابن الفعلي أولاً ثم هادم الأب، بدلًا من استدعاء هادم Polygon فقط.

---

### السؤال 4
ما نوع تابع `area()` في الصف `Polygon`، وهل هو تابع مجرد (Pure Virtual)؟

A- تابع مجرد Pure Virtual لأنه لا يصح حساب مساحة كثير أضلاع عام
B- تابع عادي غير افتراضي
C- تابع افتراضي `virtual` لكنه ليس مجردًا لأنه يملك جسمًا (`return 0.0;`) فيمكن استدعاؤه مباشرة من كائن Polygon
D- تابع ثابت `static`

**الإجابة الصحيحة: C**
**التوضيح:** التابع مكتوب `virtual double area() const { return 0.0; }` أي أن له تحقيقًا افتراضيًا (يعيد 0.0)، وهذا يختلف عن الشكل `= 0;` الذي يجعله Pure Virtual/Abstract. لاحظ الملاحظة الواردة في الشريحة: التابع المكتوب بخط مائل (italic) في المخطط UML هو المجرد، أما `area` هنا فهو افتراضي عادي قابل لإعادة التعريف.

---

### السؤال 5
ماذا يحدث عند استدعاء `myPolygon.setSides({1.0, 2.0, 3.0})` إذا كان عدد أضلاع `myPolygon` الفعلي يساوي 4؟

A- يتم إسناد القيم الثلاث وتبقى القيمة الرابعة كما كانت
B- خطأ ترجمة (Compilation Error)
C- لا يحدث أي تغيير على `sides` لأن الشرط `sideLengths.size() == sides.size()` غير محقق
D- يتم توسعة الشعاع تلقائيًا ليصبح حجمه 3

**الإجابة الصحيحة: C**
**التوضيح:** تابع `setSides` يتحقق أولًا من تطابق حجم الشعاع الممرَّر مع حجم `sides` الحالي، فإذا لم يتطابقا (3 ≠ 4) لا يُنفَّذ الإسناد إطلاقًا وتبقى القيم كما هي (أصفار أو القيم السابقة).

---

### السؤال 6
باني الصف `Triangle` مكتوب كالتالي: `Triangle() : Polygon(3, 3) {}`. ما الفائدة من استدعاء `Polygon(3, 3)` صراحةً؟

A- بدون هذا الاستدعاء لن يترجم الكود إطلاقًا لأن `Polygon` ليس له باني افتراضي بدون وسطاء (خطأ منطقي شائع، فعليًا الوسطاء لهم قيمة افتراضية 0)
B- لضمان تهيئة `numAngles` و `numSides` والشعاعين `angles` و `sides` بالحجم 3 مباشرة عند إنشاء أي كائن `Triangle`
C- لتحسين الأداء فقط
D- لا فائدة فعلية ويمكن حذفه دون أي أثر

**الإجابة الصحيحة: B**
**التوضيح:** المثلث شكل من 3 أضلاع و3 زوايا، فاستدعاء باني الأب `Polygon(3,3)` ضمن initializer list للابن يضمن أن ينفَّذ باني الأب أولًا (كأول خطوة إجبارية) ليهيئ الحقول الثابتة والشعاعين بالحجم الصحيح 3.

---

### السؤال 7
لدينا الكائن التالي:
```cpp
Triangle myTriangle;
myTriangle.setSides({3.0, 4.0, 5.0});
cout << myTriangle.area();
```
ما هو الناتج المطبوع؟

A- `12`
B- `7.5`
C- `6`
D- خطأ تنفيذ لأن `sides` فارغة

**الإجابة الصحيحة: C**
**التوضيح:** نصف المحيط `s = (3+4+5)/2 = 6`، وبتطبيق صيغة هيرون: `area = sqrt(6 × (6-3) × (6-4) × (6-5)) = sqrt(6×3×2×1) = sqrt(36) = 6`.

---

### السؤال 8
لماذا يُعاد تعريف `area()` في كل من `Rectangle` و `Parallelogram` رغم أن `Rectangle` يرث من `Parallelogram` أصلاً؟

A- لأن صيغة حساب مساحة المستطيل (`sides[0] * sides[1]`) تختلف عن صيغة متوازي الأضلاع العامة (`sides[0] * height`)، فكل صف يحتاج تحقيقه الخاص المناسب لخصائصه الهندسية
B- خطأ في التصميم ولا داعي لإعادة التعريف في `Rectangle`
C- لأن `Parallelogram` لا يملك تابع `area` أصلاً
D- لتفادي خطأ ترجمة إجباري عند الوراثة من صف فيه تابع افتراضي

**الإجابة الصحيحة: A**
**التوضيح:** في المستطيل يمكن حساب المساحة مباشرة من ضلعين متعامدين (`sides[0]*sides[1]`) دون الحاجة لحقل `height` المستقل، بينما متوازي الأضلاع العام يحتاج الارتفاع الفعلي `height` لأن أضلاعه غير متعامدة بالضرورة، لذلك يُعاد تعريف `area` في كل مستوى بما يناسبه (تعددية الأشكال Polymorphism).

---

### السؤال 9
ما الشرط الذي **يجب** توفره في تحقيق `area()` ضمن الصفوف الوارثة (مثل `Triangle`, `Rectangle`...) بما أن التابع الأصلي معرف كـ `virtual double area() const`؟

A- لا يوجد أي شرط، يمكن حذف `const` في الابن دون مشاكل
B- يجب أن يحمل تحقيق الابن نفس المحدد `const` أيضًا وإلا اعتُبر تابعًا جديدًا غير مرتبط بالـ override (خطأ ترجمة عند استخدام `override`)
C- يجب أن يكون تحقيق الابن `static`
D- يجب حذف الكلمة `virtual` من تحقيق الابن

**الإجابة الصحيحة: B**
**التوضيح:** كما ورد صراحة في شريحة `Parallelogram` "نقطة مهمة: في حال كان تابع virtual يمتلك المحدد const يجب أن يتوفر في التحقيق ضمن الابن"، فتوقيع التابع (بما فيه `const`) يجب أن يتطابق تمامًا مع الأب لتفعيل آلية التجاوز الصحيحة، خصوصًا عند استخدام الكلمة `override`.

---

### السؤال 10
ما نتيجة تنفيذ الكود التالي؟
```cpp
Rectangle myRectangle;
myRectangle.setSides({5.0, 10.0, 5.0, 10.0});
cout << myRectangle.area() << " " << myRectangle.circumference();
```

A- `50 30`
B- `15 30`
C- `50 15`
D- خطأ ترجمة لأن `circumference` غير موروث بشكل صحيح

**الإجابة الصحيحة: A**
**التوضيح:** `area = sides[0]*sides[1] = 5*10 = 50`. أما `circumference()` موروث من `Polygon` دون تعديل ويجمع كل عناصر `sides`: `5+10+5+10 = 30`.

---

### السؤال 11
لدينا:
```cpp
Trapezoid myTrapezoid(6.0);
myTrapezoid.setSides({4.0, 5.0, 8.0, 5.0});
cout << myTrapezoid.area();
```
ما الناتج؟

A- `22`
B- `36`
C- `13`
D- `78`

**الإجابة الصحيحة: B**
**التوضيح:** صيغة شبه المنحرف: `area = ((sides[0] + sides[2]) / 2.0) * height = ((4 + 8) / 2.0) * 6 = 6 * 6 = 36`. (لاحظ استخدام `sides[0]` و `sides[2]` أي الضلعين المتوازيين، وليس `sides[1]`).

---

### السؤال 12
لماذا لم يُعِد الصف `Square` تعريف تابع `area()` رغم أنه يرث من `Rectangle`؟

A- لأن المربع حالة خاصة من المستطيل (كل أضلاعه متساوية)، والصيغة `sides[0]*sides[1]` الموروثة من `Rectangle` تُعطي النتيجة الصحيحة دون تعديل
B- لأن المربع لا يمتلك مساحة أصلاً
C- خطأ سهو من الدكتور ويجب أن يعاد تعريفها
D- لأن `Square` يرث مباشرة من `Polygon` وليس من `Rectangle`

**الإجابة الصحيحة: A**
**التوضيح:** كما ورد في الشريحة "Square: مثل المستطيل تمامًا"، فالمربع مستطيل خاص بأضلاع متساوية، وبالتالي فإن التابع الموروث من `Rectangle` (`sides[0]*sides[1]`) يعطي مساحة صحيحة بما أن `sides[0] = sides[1]` في حالة المربع، فلا حاجة لإعادة التعريف.

---

### السؤال 13
في دالة `main`، عُرِّف الشعاع التالي:
```cpp
Polygon* shapes[] = { &myTriangle, &myRectangle, &mySquare, &myTrapezoid };
for (int i = 0; i < 4; ++i) {
    cout << shapes[i]->area() << "\n";
}
```
ما هو المفهوم البرمجي الذي يسمح باستدعاء `area()` الصحيح الخاص بكل صف فعلي رغم أن نوع المؤشر هو `Polygon*` في جميع الحالات؟

A- التحميل الزائد Overloading
B- التغليف Encapsulation
C- تعددية الأشكال Polymorphism بالاعتماد على التوابع الافتراضية virtual
D- القوالب Templates

**الإجابة الصحيحة: C**
**التوضيح:** بما أن `area()` معرف `virtual` في الأب، فإن استدعاءه عبر مؤشر من نوع الأب (`Polygon*`) يُنفَّذ فعليًا التحقيق الخاص بالنوع الحقيقي للكائن المؤشَّر إليه (Triangle, Rectangle...) وهذا ما يُعرف بتعددية الأشكال في وقت التنفيذ (Runtime Polymorphism)، تمامًا كما وردت الملاحظة في نهاية الشريحة الأخيرة.

---

### السؤال 14
ماذا يعبر الحقلان `numAngles` و `numSides` في الصف `Polygon` ولماذا عُرِّفا `const`؟

A- يمثلان عدد الزوايا والأضلاع، وجُعلا ثابتين لأن عدد أضلاع/زوايا الشكل الهندسي (مثلث، رباعي...) لا يتغير بعد إنشاء الكائن
B- ثوابت عالمية مشتركة بين جميع الكائنات
C- يمثلان الحد الأقصى لعدد الأضلاع الذي يمكن أن يصل إليه أي شكل مستقبلاً
D- لا علاقة لهما بالحجم الفعلي للشعاعين

**الإجابة الصحيحة: A**
**التوضيح:** بمجرد تحديد نوع الشكل (مثلاً مثلث = 3 أضلاع/3 زوايا) فإن هذا العدد صفة ثابتة له طوال عمر الكائن، لذلك من المنطقي جعلها `const` لمنع تعديلها عن طريق الخطأ بعد الإنشاء.

---

### السؤال 15
أي صف من التالية **لا** يمتلك حقل `height` إضافيًا؟

A- `Parallelogram`
B- `Trapezoid`
C- `Rectangle`
D- كل الصفوف المذكورة تمتلك `height`

**الإجابة الصحيحة: C**
**التوضيح:** `Rectangle` يرث من `Parallelogram` (وبالتالي يرث حقل `height` تقنيًا)، لكنه لا يستخدمه في حساب مساحته إطلاقًا؛ فهو يحسب المساحة بالاعتماد على `sides[0]*sides[1]` مباشرةً دون أي حاجة لـ `height`. المقصود من السؤال أن الصفين اللذين يعتمدان فعليًا على `height` في صيغة مساحتهما هما `Parallelogram` و `Trapezoid` فقط.

---

### السؤال 16
ما الفرق الجوهري بين حقل `height` في `Parallelogram` وحقل `height` في `Trapezoid` من ناحية محدد الوصول (Access Specifier)؟

A- كلاهما `private`
B- كلاهما `public`
C- في `Parallelogram` معرف `protected`، وفي `Trapezoid` معرف `private`
D- لا وجود لحقل `height` في `Trapezoid`

**الإجابة الصحيحة: C**
**التوضيح:** بالرجوع إلى كود الشريحتين: `class Parallelogram { protected: double height; ... }` بينما `class Trapezoid { private: double height; ... }`، أي أن `Parallelogram` يسمح لأبنائه (مثل `Rectangle`) بالوصول المباشر إلى `height`، بينما `Trapezoid` يحصرها ضمن الصف نفسه فقط.

---

### السؤال 17
عند تنفيذ:
```cpp
Quadrilateral q;
cout << q.area();
```
ما الناتج؟

A- خطأ ترجمة لأن `Quadrilateral` لا يملك تابع `area`
B- `0` (أو `0.0`) لأن `Quadrilateral` لم يُعِد تعريف `area` فيستخدم تحقيق `Polygon` الافتراضي الذي يعيد 0.0
C- خطأ تنفيذ Runtime Error
D- قيمة عشوائية غير محددة

**الإجابة الصحيحة: B**
**التوضيح:** الصف `Quadrilateral` كما ورد في الشريحة "يعبر عن شكل يمتلك أربع أضلاع" فقط، ولم يُعِد تعريف `area()`، لذلك عند استدعائها يُستخدم تحقيق `Polygon::area()` الموروث والذي يعيد `0.0` دائمًا.

---

### السؤال 18
ما هو محدد الوراثة (Inheritance Specifier) المستخدم في جميع علاقات الوراثة ضمن هذه الشجرة (`Triangle`, `Quadrilateral`, `Parallelogram`...)؟

A- `private`
B- `protected`
C- `public`
D- لا يوجد محدد صريح في أي منها

**الإجابة الصحيحة: C**
**التوضيح:** جميع أسطر التعريف مثل `class Triangle : public Polygon` و `class Rectangle : public Parallelogram` تستخدم `public` صراحةً، مما يحافظ على مستويات الوصول الأصلية (public/protected) للأعضاء الموروثة كما هي في الصف الابن.

---

## القسم الثاني: صح أم خطأ (الأسئلة 19 – 26، درجتان لكل سؤال)

### السؤال 19
الحقل `sides` في الصف `Polygon` معرف بمحدد الوصول `protected` وليس `private`.

A- صح
B- خطأ

**الإجابة الصحيحة: A – صح**
**التوضيح:** `protected: std::vector<double> angles; std::vector<double> sides;` هذا يسمح للصفوف الوارثة (`Triangle`, `Parallelogram`...) بالوصول المباشر إلى `sides` داخل توابعها مثل `area()`، وهو ما يفسر إمكانية استخدام `sides[0]` مباشرة داخل Triangle وRectangle وغيرهما.

---

### السؤال 20
لو حُذفت الكلمة `virtual` من تعريف `~Polygon()`، فإن حذف كائن `Triangle` عبر مؤشر `Polygon*` سينفذ هادم `Triangle` ثم هادم `Polygon` بشكل صحيح تمامًا كما لو بقيت الكلمة.

A- صح
B- خطأ

**الإجابة الصحيحة: B – خطأ**
**التوضيح:** بدون `virtual`، فإن `delete` عبر مؤشر أب من نوع `Polygon*` يستدعي هادم `Polygon` فقط (Static Binding)، متجاهلاً أي هادم خاص بالصف الابن الفعلي، وهذا قد يسبب تسرب ذاكرة إن كان للابن موارد ديناميكية خاصة.

---

### السؤال 21
باني `Parallelogram` يستدعي باني `Quadrilateral` صراحة ضمن لائحة التهيئة، وهذا يضمن تنفيذ باني `Polygon` قبله تسلسليًا (Polygon ثم Quadrilateral ثم Parallelogram).

A- صح
B- خطأ

**الإجابة الصحيحة: A – صح**
**التوضيح:** `Parallelogram(double h = 0.0) : Quadrilateral(), height(h) {}` يستدعي `Quadrilateral()`، والذي بدوره يستدعي `Polygon(4, 4)` ضمن بانيه الخاص، فتُنفَّذ سلسلة البواني من الجذر (Polygon) نزولًا حتى الصف المُنشَأ فعليًا، وهذا هو الترتيب الإجباري لتنفيذ بواني الآباء في ++C.

---

### السؤال 22
يمكن استبدال جملة `angles(anglesCount, 0.0), sides(sidesCount, 0.0)` بجملة إسناد عادية داخل جسم الباني `{ angles = ...; sides = ...; }` دون أي فرق في السلوك أو الأداء.

A- صح
B- خطأ

**الإجابة الصحيحة: B – خطأ**
**التوضيح:** استخدام initializer list يبني الشعاع مباشرة بالحجم والقيمة المطلوبين (بناء واحد Construction)، بينما الإسناد داخل الجسم يستدعي أولًا الباني الافتراضي الفارغ للشعاع ثم عملية إسناد إضافية (Construction ثم Assignment)، أي أداء أقل كفاءة، إضافة لكون بعض الحقول (كـ `const`) لا يمكن إسنادها أصلاً إلا في initializer list.

---

### السؤال 23
تابع `circumference()` معرف بالمحدد `const` بينما تابع `setSides()` غير معرف بـ `const`، وهذا منطقي لأن `setSides` يُعدّل حالة الكائن بينما `circumference` لا يُعدّلها.

A- صح
B- خطأ

**الإجابة الصحيحة: A – صح**
**التوضيح:** `double circumference() const` يقرأ فقط عناصر `sides` دون تعديلها فأصبح من الآمن والمنطقي وسمه `const`، بينما `void setSides(...)` يُعدّل قيم `sides` مباشرة، لذلك لا يجوز وسمه بـ `const`.

---

### السؤال 24
في شجرة الوراثة المعروضة، يمكن اعتبار `Square` أبًا لـ `Rectangle` بما أن المربع حالة خاصة من المستطيل.

A- صح
B- خطأ

**الإجابة الصحيحة: B – خطأ**
**التوضيح:** العلاقة معكوسة: `class Square : public Rectangle`، أي أن `Square` هو **الابن** ويرث من `Rectangle` (الأب)، وليس العكس. في تصميم الوراثة الشيء الأكثر تخصصًا (Square) يرث من الأعم (Rectangle).

---

### السؤال 25
لو استدعينا `mySquare.setSides({4.0, 4.0, 4.0, 5.0})` (أي بأطوال غير متساوية فعليًا)، فإن الكود لن يمنع ذلك برمجيًا، لأن `setSides` يتحقق فقط من تطابق **عدد** العناصر (4) وليس من تساوي قيمها.

A- صح
B- خطأ

**الإجابة الصحيحة: A – صح**
**التوضيح:** شرط `setSides` الوحيد هو `sideLengths.size() == sides.size()`؛ لا يوجد أي تحقق منطقي على تساوي الأضلاع لضمان كون الشكل مربعًا فعليًا هندسيًا، وهذه ثغرة تصميمية يجدر الانتباه لها (البرنامج يثق بالمستخدم في إدخال بيانات متسقة مع نوع الشكل).

---

### السؤال 26
استدعاء `shapes[i]->circumference()` ضمن حلقة `main` على مصفوفة `Polygon*` يعمل بشكل صحيح لكل الأشكال دون الحاجة لأن يكون `circumference` تابعًا افتراضيًا `virtual`، لأنه غير مُعاد تعريفه في أي صف ابن أصلاً.

A- صح
B- خطأ

**الإجابة الصحيحة: A – صح**
**التوضيح:** بما أن جميع الصفوف الوارثة (Triangle, Rectangle, Trapezoid...) لا تُعيد تعريف `circumference()` إطلاقًا، فإن استدعاءه من خلال أي مؤشر (سواء `Polygon*` أو مؤشر الابن نفسه) يستدعي دائمًا نفس التحقيق الوحيد الموجود في `Polygon`، بصرف النظر عن كونه `virtual` أم لا؛ فحاجة `virtual` تظهر فقط عند وجود إعادة تعريف فعلية في الأبناء (كما في `area`).

---

## جدول الإجابات السريع

| # | الإجابة | # | الإجابة |
|---|---------|---|---------|
| 1 | C | 14 | A |
| 2 | A | 15 | C |
| 3 | B | 16 | C |
| 4 | C | 17 | B |
| 5 | C | 18 | C |
| 6 | B | 19 | A (صح) |
| 7 | C | 20 | B (خطأ) |
| 8 | A | 21 | A (صح) |
| 9 | B | 22 | B (خطأ) |
| 10 | A | 23 | A (صح) |
| 11 | B | 24 | B (خطأ) |
| 12 | A | 25 | A (صح) |
| 13 | C | 26 | A (صح) |

---

### المفاهيم التي غطتها هذه الأسئلة
- initializer list وسبب وجوب استخدامه مع الحقول `const` والشعاعات (`vector(count, value)`)
- الهادم الافتراضي `virtual ~Polygon()` وأهميته مع تعددية الأشكال
- الفرق بين التابع الافتراضي العادي (`virtual` مع جسم) والتابع المجرد (Pure Virtual)
- تطابق محدد `const` بين تعريف الأب وتحقيق الابن عند استخدام `override`
- ترتيب استدعاء بواني الآباء (Polygon → Quadrilateral → Parallelogram/Trapezoid → Rectangle → Square)
- إعادة تعريف `area()` في كل مستوى بحسب الصيغة الهندسية المناسبة (هيرون، الارتفاع، الضلعين المتعامدين)
- محددات الوصول `protected` مقابل `private` في الحقول الموروثة
- تعددية الأشكال Polymorphism عبر مصفوفة مؤشرات `Polygon*`
- الفرق بين التوابع التي تحتاج `virtual` فعليًا (المُعاد تعريفها) والتي لا تحتاجه (غير المُعاد تعريفها مثل `circumference`)
- تتبّع تنفيذ كود حسابي (حساب المساحة والمحيط لأشكال مختلفة بقيم عددية فعلية)
