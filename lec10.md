# أسئلة مؤتمتة – المحاضرة العاشرة: الوراثة بالأمثلة (List / ArrayList / LinkedList)

**المقرر:** برمجة 2 | **إعداد:** أسئلة مؤتمتة على غرار أسلوب د. أسامة ناصر
**تعليمات:** اختر الإجابة الصحيحة. افترض أن كل المكتبات اللازمة قد تم إدراجها في البرنامج.

---

## القسم الأول: بنية الصف List (التجريد، virtual، pure virtual)

### السؤال 1
بالنظر إلى تعريف الصف التالي:

```cpp
template <typename T>
class List {
protected:
    int len = 0;
    virtual T& getAsRef(int index) = 0;
public:
    virtual ~List() {}
    virtual T get(int index) = 0;
    virtual void set(int index, T value) = 0;
    virtual void append(T value) = 0;
    virtual T pop() = 0;
    T& operator[](int index) {return getAsRef(index);}
    int length() const { return len; }
};
```

هل الكود التالي صحيح؟

```cpp
List<int> x;
```

A- صح، لأن `List` صف عادي  
B- خطأ، لأن `List` صف مجرد (abstract) بسبب وجود توابع `pure virtual`  
C- صح بشرط تمرير قيمة ابتدائية  
D- خطأ، لأن `int` لا يمكن استخدامه مع `template`

**الإجابة الصحيحة: B**
**الشرح:** إضافة `= 0` في نهاية تعريف تابع `virtual` تجعله `pure virtual`، وأي صف يحوي تابعًا واحدًا على الأقل من هذا النوع يصبح صفًا مجردًا (abstract) لا يمكن بناء كائن منه مباشرة، بل فقط من أبنائه الذين يعيدون تعريف (override) كل التوابع المجردة.

---

### السؤال 2
ماذا يعني استخدام الكلمة المفتاحية `virtual` في سياق البرمجة الغرضية التوجه؟

A- أن التابع لا يمكن استدعاؤه إلا من داخل الصف نفسه  
B- أن التابع يُنفَّذ فقط عند الترجمة (compile time)  
C- في حال أعاد الابن تعريف التابع (override)، وتم استدعاؤه عبر مؤشر/مرجع من نوع الأب، يُنفَّذ كود الابن وليس كود الأب  
D- أن التابع لا يمكن إعادة تعريفه في الأبناء

**الإجابة الصحيحة: C**
**الشرح:** هذا هو التعريف الأساسي لآلية الـ`virtual dispatch`: عند التعامل مع الابن عبر مؤشر أو مرجع من نوع الأب، يتم تنفيذ النسخة الموجودة لدى الابن من التابع (إن وُجدت) وليس النسخة الموجودة لدى الأب.

---

### السؤال 3
لماذا يستخدم المعامل `operator[]` التابع `getAsRef` بدلاً من التابع `get`؟

A- لأن `getAsRef` أسرع من `get`  
B- لكي يعيد قيمة بالمرجع (reference)، مما يسمح باستخدام المعامل `[]` في القراءة والكتابة معًا  
C- لأن `get` غير موجود في الصف الأب  
D- لا يوجد فرق فعلي بينهما

**الإجابة الصحيحة: B**
**الشرح:** لكي يعمل المعامل `[]` بشكل سليم ويسمح بالقراءة والكتابة (مثل `list[i] = value;`) يجب أن يُعيد التابع قيمة بالمرجع `T&` وليس بالقيمة `T`، ولهذا السبب اعتُمد `getAsRef` بدلاً من `get`.

---

### السؤال 4
أي الأكواد التالية صحيحة اعتمادًا على `template <typename T>` في تعريف الصف `List`؟

A- `List<int> x;` فقط  
B- `List<double> y;` فقط  
C- `List<int> x; List<double> y; List<Class1> z;` جميعها صحيحة من ناحية القبول العام لنوع البيانات (بغض النظر عن كون `List` صفًا مجردًا)  
D- لا شيء مما سبق صحيح لأن `template` يقبل الأعداد فقط

**الإجابة الصحيحة: C**
**الشرح:** الهدف من `template <typename T>` هو جعل القائمة تتقبل أي نوع بيانات، لذلك فإن جميع الأسطر مقبولة من حيث توافق الأنواع؛ إلا أن بناء كائن مباشرة (Instantiation) من `List` نفسه غير ممكن لأنه صف مجرد كما ورد في السؤال الأول.

---

### السؤال 5
الحقل `len` معرّف ضمن الصف `List` بمحدد وصول `protected`. ما الذي يترتب على ذلك بالنسبة لأبناء `List` مثل `ArrayList` و`LinkedList`؟

A- لا يمكن لأي منهما الوصول إلى `len`  
B- يمكن للأبناء الوصول إلى `len` مباشرة، لكن لا يمكن لأي كود خارج شجرة الوراثة الوصول إليه  
C- يمكن للجميع (public) الوصول إلى `len`  
D- يجب استخدام `friend` للوصول إلى `len`

**الإجابة الصحيحة: B**
**الشرح:** محدد `protected` يسمح لأبناء الصف (وأبناء الأبناء) بالوصول المباشر إلى العضو، بعكس `private` الذي يمنع الجميع سوى الصف نفسه، وبعكس `public` الذي يفتح الوصول للجميع.

---

## القسم الثاني: الصف ArrayList

### السؤال 6
بفرض الكود التالي لصف `ArrayList`:

```cpp
template <typename T>
class ArrayList : public List<T> {
private:
    T* dataPtr = nullptr;
    int capacity = 0;
    void expandData(int newCapacity) {
        if (newCapacity <= capacity) return;
        int actualNewCapacity = (capacity == 0) ? 2 : capacity * 2;
        if (newCapacity > actualNewCapacity) actualNewCapacity = newCapacity;
        T* newData = new T[actualNewCapacity]();
        for (int i = 0; i < this->len; i++)
            newData[i] = dataPtr[i];
        delete[] dataPtr; dataPtr = newData;
        capacity = actualNewCapacity;
    }
    ...
};
```

ما الهدف من محدد الوراثة `public` في `class ArrayList : public List<T>`؟

A- يعني أن أبناء `ArrayList` فقط يعرفون أنه وارث لـ `List`  
B- يعني أن كل الكود العام (خارج الصف) يعرف أن `ArrayList` وارث لـ `List`، أي يمكن التعامل مع `ArrayList*` كـ `List*`  
C- يعني أن `ArrayList` لا يمكن أن يرث من `List` إلا داخليًا  
D- لا يوجد فرق بين `public` و`private` في الوراثة

**الإجابة الصحيحة: B**
**الشرح:** محدد الوراثة `public` يجعل علاقة "هو نوع من" (is-a) معروفة للجميع، بينما `private` يخفي هذه العلاقة عن الخارج بالكامل، و`protected` يجعلها معروفة فقط لأبناء `ArrayList`.

---

### السؤال 7
عند استدعاء `expandData(newCapacity)` وكانت القيمة الأولية لـ `capacity` تساوي 0، فإن السعة الفعلية الجديدة (`actualNewCapacity`) الافتراضية (قبل مقارنتها مع `newCapacity`) تُحسب كـ:

A- `0`  
B- `1`  
C- `2`  
D- `capacity * 2`

**الإجابة الصحيحة: C**
**الشرح:** وفق السطر:
```cpp
int actualNewCapacity = (capacity == 0) ? 2 : capacity * 2;
```
عند أول استدعاء تكون `capacity == 0`، فتكون القيمة الافتراضية للسعة الجديدة هي `2` (لا يتم مضاعفة الصفر).

---

### السؤال 8
ما هو الشرط الذي يجعل التابع `expandData` يتوقف فورًا (`return`) دون تنفيذ أي عملية إعادة حجز؟

A- عندما تكون `capacity` تساوي `0`  
B- عندما تكون `newCapacity` أكبر من `capacity`  
C- عندما تكون `newCapacity` أصغر من أو تساوي `capacity` الحالية  
D- لا يوجد شرط توقف في التابع

**الإجابة الصحيحة: C**
**الشرح:** السطر `if (newCapacity <= capacity) return;` يمنع إعادة حجز الذاكرة إذا كانت السعة المطلوبة الجديدة لا تتجاوز السعة الحالية أصلًا، أي لا حاجة للتوسعة.

---

### السؤال 9
أي مما يلي يصف بدقة العلاقة بين `capacity` و`len` في الصف `ArrayList` (بالتشبيه مع الفندق)؟

A- `capacity` هي عدد الغرف المحجوزة، و`len` هي السعة الكلية للفندق  
B- `capacity` هي السعة الكلية للمصفوفة (عدد الغرف في الفندق)، و`len` هي عدد الخانات المستخدمة (الغرف المحجوزة)  
C- `capacity` و`len` قيمتان متطابقتان دائمًا  
D- `len` تمثل عدد المرات التي تمت فيها إعادة الحجز

**الإجابة الصحيحة: B**
**الشرح:** كما ورد حرفيًا في المحاضرة: `capacity` تعبر عن السعة الكلية للمصفوفة، بينما `len` (الموروثة من `List`) تعبر عن عدد الخانات المستخدمة فعليًا.

---

### السؤال 10
بفرض الكود التالي لتابع `getAsRef` في `ArrayList`:

```cpp
protected:
    T& getAsRef(int index) override {
        if (index < 0 || index >= this->len) throw out_of_range("Index out of bounds");
        return dataPtr[index];
    }
```

ما الذي يحدث عند تنفيذ `(*alist)[100]` إذا كانت القائمة تحوي 10 عناصر فقط؟

A- تُعاد قيمة عشوائية من الذاكرة  
B- يُرمى استثناء `out_of_range`  
C- يتم توسيع المصفوفة تلقائيًا لتشمل الفهرس 100  
D- خطأ ترجمة (compile error)

**الإجابة الصحيحة: B**
**الشرح:** بما أن `operator[]` يستدعي `getAsRef`، وهذا التابع يتحقق من كون الفهرس ضمن المجال `[0, len)`، فإن أي فهرس خارج هذا المجال (مثل 100 مع طول 10) يؤدي إلى رمي استثناء `out_of_range`.

---

### السؤال 11
بفرض الكود التالي:

```cpp
void push(T value) override {
    if (this->len >= capacity) expandData(this->len + 1);
    dataPtr[this->len++] = value;
}
```

ما الترتيب الصحيح لتنفيذ هذا التابع عند الإضافة إلى قائمة ممتلئة (`len == capacity`)؟

A- تتم إضافة العنصر أولاً ثم يتم توسيع المصفوفة  
B- يتم استدعاء `expandData` لتوسيع السعة أولاً، ثم يُخزَّن العنصر الجديد في الفهرس الحالي `len`، ثم تُزاد قيمة `len`  
C- يُرمى استثناء لأن القائمة ممتلئة  
D- لا يحدث شيء لأن `len` لا يمكن أن تساوي `capacity`

**الإجابة الصحيحة: B**
**الشرح:** الشرط `if (this->len >= capacity)` يتحقق أولاً هل السعة الحالية كافية، فإن لم تكن كذلك يوسّع السعة عبر `expandData`، بعدها يُخزَّن العنصر الجديد ضمن `dataPtr[this->len]` ثم تُزاد `len` بمقدار واحد (بسبب `len++`).

---

### السؤال 12
ماذا يحدث عند استدعاء `pop()` التالي على `ArrayList` فارغة (`len == 0`)؟

```cpp
T pop() override {
    if (this->len == 0) throw runtime_error("Empty list");
    return dataPtr[--this->len];
}
```

A- تعاد قيمة افتراضية بصمت  
B- يُرمى استثناء `runtime_error`  
C- تُنقص `len` وتصبح قيمة سالبة  
D- يتوقف البرنامج دون رسالة خطأ

**الإجابة الصحيحة: B**
**الشرح:** التابع يتحقق أولًا من أن القائمة ليست فارغة، وفي حال كانت `len == 0` يرمي استثناء `runtime_error("Empty list")` بدلاً من محاولة الوصول إلى عنصر غير موجود.

---

## القسم الثالث: الصف LinkedList

### السؤال 13
تتكون عقدة `LinkedListItem` بشكل أساسي من:

```cpp
template <typename T>
struct LinkedListItem {
    T data;
    LinkedListItem* next;
    LinkedListItem* prev;
};
```

ما هما الحقلان اللذان **يجب** توفرهما على الأقل في أي عقدة ضمن أي قائمة مترابطة (وفق تعريف المحاضرة)؟

A- `data` و`prev` فقط  
B- `next` و`prev` فقط  
C- `data` و`next` على الأقل (المؤشر `next` إلزامي)  
D- لا يوجد حقول إلزامية

**الإجابة الصحيحة: C**
**الشرح:** وفق المحاضرة، تتكون كل عقدة أساسًا من: البيانات (Data) والمؤشر إلى العقدة التالية (Next)، ويجب أن يتواجد هذا المؤشر على الأقل؛ أما `prev` فيُستخدم فقط في القوائم المزدوجة الاتجاه.

---

### السؤال 14
ما الفرق الجوهري بين القائمة المترابطة الأحادية (Singly) والقائمة المترابطة المزدوجة (Doubly)؟

A- الأحادية أسرع في كل العمليات دون استثناء  
B- المزدوجة تحتوي كل عقدة فيها على مؤشرين (للسابق وللتالي) مما يسمح بالتنقل في الاتجاهين، بينما الأحادية تسمح بالتنقل باتجاه واحد فقط  
C- لا يوجد فرق عملي بينهما  
D- الأحادية تستخدم فقط مع الأعداد الصحيحة

**الإجابة الصحيحة: B**
**الشرح:** في القائمة الأحادية، تشير كل عقدة إلى التي تليها فقط والتنقل باتجاه واحد. أما في القائمة المزدوجة، تحتوي كل عقدة على مؤشرين (Prev وNext) مما يسمح بالتنقل للأمام والخلف.

---

### السؤال 15
ما الذي يميز القائمة المترابطة الدائرية (Circular Linked List) عن باقي الأنواع؟

A- لا تحتوي على مؤشر `next` إطلاقًا  
B- العقدة الأخيرة فيها تشير إلى `NULL` كما في باقي الأنواع  
C- العقدة الأخيرة فيها تشير إلى العقدة الأولى بدلاً من `NULL`، مما يشكل حلقة مغلقة  
D- لا يمكن أن تحتوي على أكثر من عقدة واحدة

**الإجابة الصحيحة: C**
**الشرح:** في القائمة الدائرية تشير العقدة الأخيرة إلى العقدة الأولى بدلاً من الإشارة إلى `NULL`، وهذا ما يجعلها "دائرية" أي تشكل حلقة مغلقة.

---

### السؤال 16
بفرض الكود التالي لتابع `moveIndex` في `LinkedList`:

```cpp
void moveIndex(int index) const {
    if (index < 0 || index >= this->len) throw out_of_range("Index out of bounds");
    current = start;
    for (int i = 0; i < index; i++) {
        current = current->next;
    }
}
```

ما هي الآلية المستخدمة هنا للوصول إلى العنصر عند فهرس معين؟

A- الوصول المباشر عبر عنوان الذاكرة (كما في المصفوفات)  
B- المرور بحلقة تبدأ من `start` وتتنقل عبر `next` حتى الوصول إلى الفهرس المطلوب  
C- استخدام `prev` فقط بدءًا من `end`  
D- البحث الثنائي (Binary Search)

**الإجابة الصحيحة: B**
**الشرح:** بعكس المصفوفات التي تسمح بالوصول المباشر O(1)، فإن القائمة المترابطة (بهذا التحقيق الأبسط) تتطلب المرور بحلقة تبدأ من العقدة الأولى `start` وتتقدم عبر `next` خطوة بخطوة حتى بلوغ الفهرس المطلوب.

---

### السؤال 17
لماذا يجب في هادم (destructor) الصف `LinkedList` حذف (`delete`) كل عقدة على حدة ضمن حلقة، على خلاف هادم `ArrayList` الذي يكتفي بحذف المصفوفة بأكملها دفعة واحدة؟

```cpp
~LinkedList() {
    while (start) {
        LinkedListItem<T>* temp = start;
        start = start->next;
        delete temp;
    }
}
```

A- لأن كل عقدة في `LinkedList` محجوزة بشكل منفصل في الذاكرة (`new` مستقل لكل عقدة)، بعكس المصفوفة التي هي كتلة ذاكرة واحدة متجاورة  
B- لأن `LinkedList` لا يحتاج فعليًا إلى هادم  
C- لأن العقد لا تُحجز ديناميكيًا أصلاً  
D- لتجنب استخدام `delete[]`

**الإجابة الصحيحة: A**
**الشرح:** كل عقدة في القائمة المترابطة تُنشأ باستدعاء `new` منفصل، لذلك يجب تحرير كل عقدة بشكل مستقل عبر `delete`؛ بينما في `ArrayList` تُخزَّن العناصر ضمن كتلة ذاكرة واحدة متجاورة تم حجزها بـ `new T[...]`، فيكفي استدعاء `delete[]` واحد لتحريرها بالكامل.

---

### السؤال 18
بفرض الكود التالي لتابع `push` في `LinkedList`:

```cpp
void push(T value) override {
    LinkedListItem<T>* newNode = new LinkedListItem<T>(value);
    if (!start) {
        start = end = newNode;
    } else {
        end->next = newNode;
        newNode->prev = end;
        end = newNode;
    }
    this->len++;
}
```

عند إضافة أول عنصر إلى قائمة فارغة، ماذا يحدث لكل من `start` و`end`؟

A- يبقيان `nullptr`  
B- يشيران كلاهما إلى العقدة الجديدة `newNode` (بما أنها أول وآخر عنصر في آن واحد)  
C- يشير `start` فقط إلى العقدة الجديدة  
D- يُرمى استثناء لأن القائمة فارغة

**الإجابة الصحيحة: B**
**الشرح:** عندما لا يوجد عناصر سابقة (`!start` صحيحة)، فإن العنصر الجديد يمثل في الوقت نفسه أول عنصر وآخر عنصر في القائمة، لذلك يُسند إلى كل من `start` و`end`.

---

### السؤال 19
بفرض الكود التالي لتابع `pop` في `LinkedList`:

```cpp
T pop() override {
    if (!end) throw runtime_error("Cannot pop from empty list");
    T value = end->data;
    LinkedListItem<T>* toDelete = end;
    if (start == end) {
        start = end = nullptr;
    } else {
        end = end->prev;
        end->next = nullptr;
    }
    delete toDelete;
    this->len--;
    return value;
}
```

إذا كانت القائمة تحوي عنصرًا واحدًا فقط (`start == end`)، ماذا يحدث لكل من `start` و`end` بعد `pop()`؟

A- يبقى كل منهما مؤشرًا على العقدة المحذوفة (dangling pointer)  
B- يصبح كلاهما `nullptr`  
C- يبقى `start` كما هو ويصبح `end` فقط `nullptr`  
D- يُرمى استثناء لأن القائمة تحتوي عنصرًا واحدًا فقط

**الإجابة الصحيحة: B**
**الشرح:** عند وجود عنصر وحيد في القائمة، فإن حذفه يجعل القائمة فارغة تمامًا، لذلك يُسند لكل من `start` و`end` القيمة `nullptr` لتفادي أي مؤشر معلّق (dangling pointer) بعد `delete toDelete`.

---

## القسم الرابع: override و overload والوراثة عمومًا

### السؤال 20
ما الفرق بين `override` (إعادة التحقيق) و`overload` (التحميل الزائد)؟

A- لا يوجد فرق، وهما مترادفان تمامًا  
B- التحميل الزائد (overload) يعطي القدرة على جعل التابع يتعرف على نوع أو عدد مختلف من الوسطاء وقد يتضمن تعديلاً في المنطق، بينما إعادة التحقيق (override) تعني تغييرًا كاملاً لمنطق التابع الموروث من الأب  
C- إعادة التحقيق (override) تحدث فقط بين توابع الصف نفسه  
D- التحميل الزائد يتطلب دائمًا الكلمة المفتاحية `virtual`

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة حرفيًا: التحميل الزائد يعطينا القدرة على جعل التابع يتعرف على نوع جديد من الوسطاء أو عدد مختلف منها، بينما إعادة التحقيق (override) تعني تغيرًا كاملاً لمنطق التابع في الصف الابن.

---

### السؤال 21
هل استخدام الكلمة المفتاحية `override` عند إعادة تعريف تابع من الأب إلزامي في ++C؟

A- نعم، إلزامي دائمًا وإلا يحدث خطأ ترجمة  
B- لا، غير إلزامي، لكنه يُفضَّل استخدامه لتوضيح أن هذا التابع تمت إعادة تحقيقه  
C- إلزامي فقط مع التوابع الـ `pure virtual`  
D- إلزامي فقط في الصفوف المجردة

**الإجابة الصحيحة: B**
**الشرح:** كما ذُكر في المحاضرة: هل هي إلزامية؟ لا، لكن يُفضَّل تواجدها لأنها توضح للقارئ (وللمترجم) أن هذا التابع يعيد تعريف تابع موجود في الأب، وتساعد على اكتشاف الأخطاء (مثل اختلاف التوقيع) في مرحلة الترجمة.

---

### السؤال 22
بفرض الكود التالي في `main`:

```cpp
List<int> *alist = new ArrayList<int>();
List<int> *llist = new LinkedList<int>();
for (int i = 0; i < 500; i++) {
    alist->push(i);
    llist->push(i);
}
for (int i = 10; i < 20; i++)
    cout << (*llist)[i] << "\t";
cout << endl;
for (int i = 10; i < 20; i++)
    cout << (*alist)[i] << "\t";
```

بما أن كلا الكائنين `alist` و`llist` أُضيفت لهما نفس القيم (0 حتى 499) بنفس الترتيب، ماذا نتوقع بخصوص الخرج الناتج عن الحلقتين؟

A- الخرج مختلف تمامًا لأن `ArrayList` و`LinkedList` يخزنان القيم بترتيب مختلف  
B- الخرجان متطابقان قيمًا (من 10 إلى 19) رغم اختلاف التحقيق الداخلي للصفين، لأن كلاهما يطبّق نفس واجهة `List` بشكل صحيح  
C- خطأ ترجمة لأن `List<int>*` لا يمكن أن يشير إلى `ArrayList<int>` أو `LinkedList<int>`  
D- خطأ تنفيذ (Runtime Error) لأن `operator[]` غير معرف في `List`

**الإجابة الصحيحة: B**
**الشرح:** بما أن كلا الصفين يرثان `List<int>` بشكل `public` ويطبّقان `getAsRef` بشكل صحيح، وبما أن `operator[]` معرف في الأب ويستدعي `getAsRef` (التي تُنفَّذ بشكل مضلعي/polymorphic حسب النوع الفعلي للكائن)، فإن القيم المخزنة والمسترجعة متطابقة رغم اختلاف التحقيق الداخلي (مصفوفة مقابل عقد مترابطة).

---

### السؤال 23
أيّ الأسطر التالية يمثّل الطريقة الصحيحة لتعريف مؤشر من نوع الصف الأب يشير إلى كائن من نوع الصف الابن، اعتمادًا على بنية المحاضرة؟

A- `ArrayList<int> *x = new List<int>();`  
B- `List<int> *x = new ArrayList<int>();`  
C- `List x = new ArrayList();`  
D- `List<int> x = ArrayList<int>();`

**الإجابة الصحيحة: B**
**الشرح:** بما أن `List` صف مجرد (abstract) لا يمكن بناء كائن منه مباشرة (الخيار A يحاول ذلك عبر `new List<int>()` وهو خاطئ من ناحية أخرى أيضًا لأن `List` لا `ArrayList` هو من يجب أن يُبنى)، فإن الأسلوب الصحيح هو تعريف مؤشر من نوع الأب `List<int>*` وإسناد كائن مبني من الابن `new ArrayList<int>()` إليه — وهذا يفعّل مبدأ تعدد الأشكال (Polymorphism).

---

### السؤال 24
في الصف `ArrayList`، لماذا نسند قيمًا ابتدائية للحقول عند تعريفها ضمن الصف مباشرة كما يلي؟

```cpp
T* dataPtr = nullptr;
int capacity = 0;
```

A- لضمان أن يبدأ كل كائن جديد بمؤشر لا يشير إلى أي شيء (`nullptr`) وسعة تساوي صفرًا، تحسبًا لعدم تهيئتها لاحقًا في الباني  
B- لأن ++C يفرض إسناد قيمة ابتدائية لكل عضو  
C- لتسريع عملية الترجمة فقط  
D- لا فائدة عملية لذلك، وهي مجرد أسلوب كتابة

**الإجابة الصحيحة: A**
**الشرح:** إسناد قيم ابتدائية مباشرة عند التعريف (In-class initialization) يضمن أن يبدأ `dataPtr` بقيمة `nullptr` (أي لا يؤشر على أي شيء) وتبدأ `capacity` بالقيمة `0`، مما يجعل السلوك متوقعًا وآمنًا حتى قبل استدعاء منطق الباني الصريح.

---

### السؤال 25
بشكل عام، وبالمقارنة مع الوصول العشوائي في المصفوفة (`ArrayList`)، ما هي الكلفة التقريبية للوصول إلى عنصر عند فهرس عشوائي `i` في `LinkedList` وفق التحقيق المعروض في المحاضرة (باستخدام `moveIndex`)؟

A- كلفة ثابتة O(1) تمامًا كما في المصفوفة  
B- كلفة تناسبية مع الفهرس `i` (يجب المرور عبر `i` عقدة بدءًا من `start`)  
C- كلفة أسية (Exponential)  
D- لا يمكن الوصول إلى عنصر بالفهرس إطلاقًا في `LinkedList`

**الإجابة الصحيحة: B**
**الشرح:** بما أن `moveIndex` تبدأ من `start` وتتنقل عبر `next` ضمن حلقة `for` تكرر بعدد `index` مرة، فإن كلفة الوصول إلى عنصر عند فهرس `i` تتناسب خطيًا مع قيمة `i`، بخلاف المصفوفة التي تسمح بالوصول المباشر بكلفة ثابتة.

---

## جدول الإجابات الصحيحة (مرجع سريع)

| السؤال | الإجابة | السؤال | الإجابة | السؤال | الإجابة |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | B | 10 | B | 19 | B |
| 2 | C | 11 | B | 20 | B |
| 3 | B | 12 | B | 21 | B |
| 4 | C | 13 | C | 22 | B |
| 5 | B | 14 | B | 23 | B |
| 6 | B | 15 | C | 24 | A |
| 7 | C | 16 | B | 25 | B |
| 8 | C | 17 | A | | |
| 9 | B | 18 | B | | |

---

*انتهت الأسئلة*
