# جامعة حمص - كلية الهندسة المعلوماتية
## مقرر البرمجة -2- | السنة الأولى
## أسئلة مؤتمتة على المحاضرة السادسة: الملفات العشوائية (الجزء الثاني)

> إعداد: نموذج أسئلة تدريبية مصاغ بنفس أسلوب د. أسامة ناصر (اختيار من متعدد / صح - خطأ / إكمال كود)
> ملاحظة: افترض أن جميع المكتبات اللازمة (`fstream`, `iostream` ...) قد تم تضمينها في كل الأكواد.

---

### تعليمات
- اختر الإجابة الصحيحة لكل سؤال.
- كل سؤال يليه توضيح مختصر لسبب اختيار الإجابة الصحيحة (وسبب استبعاد بقية الخيارات عند الحاجة).
- الأسئلة موزعة حسب بنية المحاضرة: تعريف الملفات العشوائية → آلية التخزين → أعلام الفتح → القراءة والكتابة → المؤشرات → معرفة عدد السجلات → المصفوفات من السجلات.

---

## القسم الأول: مفهوم الملفات العشوائية وآلية التخزين

### السؤال 1
ما الذي يميّز الملفات العشوائية (Random Files) عن الملفات التسلسلية (Sequential Files)؟

A- الملفات العشوائية لا يمكن أن تحوي أعداداً صحيحة
B- الملفات العشوائية تسمح بالانتقال إلى أي موقع ضمن الملف بشكل كيفي، بعكس التسلسلية التي تُقرأ بايتاً بعد بايت
C- الملفات العشوائية أسرع دائماً من حيث حجم التخزين على القرص
D- لا يوجد فرق حقيقي، والتسمية فقط هي المختلفة

**الإجابة الصحيحة: B**
**التوضيح:** كما ورد في المحاضرة، الملفات التسلسلية تُقرأ بشكل متتالٍ (بايت مع تاليه)، بينما تسمح الملفات العشوائية بالانتقال الكيفي ضمن الملف، أي التعامل معه كمصفوفة بايتات.

---

### السؤال 2
عند تخزين العدد الصحيح `23986743` ضمن **ملف نصي (تسلسلي)**، فإن كل خانة (رقم) من هذا العدد تُخزَّن:

A- على 4 بايتات ثابتة كما في الذاكرة
B- على بايت واحد مستقل لكل خانة (محرف)
C- لا يمكن تخزين الأعداد في الملفات النصية
D- يعتمد على نظام التشغيل فقط

**الإجابة الصحيحة: B**
**التوضيح:** في الملفات التسلسلية (النصية) يُخزَّن العدد بصفة نص، بمعنى أن كل خانة (محرف) تأخذ بايتاً مستقلاً، فكلما زاد عدد الخانات زاد عدد البايتات اللازمة.

---

### السؤال 3
الملفات العشوائية تُخزِّن البيانات:

A- كما هي ممثَّلة ضمن الذاكرة تقريباً (صورة عن الذاكرة)، أي أن العدد الصحيح يُخزَّن دوماً على 4 بايتات بغض النظر عن عدد خاناته
B- بصفة نص دائماً كالملفات التسلسلية
C- بصفة نص إذا كان العدد أكبر من 9999
D- لا علاقة لحجم البيانات في الذاكرة بحجمها ضمن الملف العشوائي

**الإجابة الصحيحة: A**
**التوضيح:** الملفات العشوائية تعمل على تخزين البيانات كما هي في الذاكرة (أي أن العدد الصحيح يبقى 4 بايتات ثابتة)، وهذا ما يجعل حجم السجل ثابتاً ويسهّل حساب المواقع.

---

## القسم الثاني: التعامل مع الملفات العشوائية في ++C

### السؤال 4
للتعامل مع الملفات العشوائية في ++C، غالباً ما نلجأ إلى الصف:

A- `string`
B- `vector`
C- `fstream`
D- `array`

**الإجابة الصحيحة: C**
**التوضيح:** رغم إمكانية استخدام `ofstream` أو `ifstream`، إلا أننا نعمل عادةً مع الصف `fstream` عند التعامل مع الملفات العشوائية لأنه يسمح بالقراءة والكتابة معاً.

---

### السؤال 5
أي مما يلي هو شكل صحيح لفتح ملف عشوائي بحيث نستطيع القراءة والكتابة منه معاً بصيغة ثنائية؟

A
```cpp
fstream file("path", ios::out);
```

B
```cpp
fstream file;
file.open("path", ios::out | ios::in | ios::binary);
```

C
```cpp
fstream file;
file.write("path", ios::in);
```

D
```cpp
ifstream file("path", ios::binary | ios::out);
```

**الإجابة الصحيحة: B**
**التوضيح:** يمكن فتح الملف إما مباشرة عند البناء `fstream file('path', flags)` أو ببناء الصف ثم استدعاء `open()`. الخيار B يجمع بين `ios::out` و`ios::in` و`ios::binary` باستخدام معامل الدمج `|` وهذا هو الشكل الصحيح لفتح ملف للقراءة والكتابة الثنائية معاً.

---

### السؤال 6
بفرض أردنا فتح ملف بحيث تبدأ الكتابة دائماً من **نهاية** الملف دون حذف محتواه الحالي، أي علم من التالية نستخدمه؟

A- `ios::in`
B- `ios::out`
C- `ios::app`
D- `ios::binary`

**الإجابة الصحيحة: C**
**التوضيح:** العلم `ios::app` يفتح الملف في وضع الكتابة لكن من نهاية الملف، بعكس `ios::out` الذي يبدأ الكتابة من بداية الملف.

---

### السؤال 7
المعامل `|` المستخدم في دمج أعلام الفتح (مثل `ios::in | ios::out`) يشبه من الناحية المنطقية عملية:

A- الطرح
B- الضرب
C- الجمع (Or منطقية)
D- القسمة

**الإجابة الصحيحة: C**
**التوضيح:** نص المحاضرة يذكر صراحة أن عملية Or المنطقية (والتي يُعبَّر عنها بالمعامل `|`) تشابه عملية الجمع من حيث دمج الخصائص/الأعلام معاً.

---

## القسم الثالث: القراءة والكتابة (write / read)

بفرض لدينا البنية التالية والتي سيُعتمد عليها في عدة أسئلة لاحقة:

```cpp
struct Student{
    long id;
    char firstName[20];
    char lastName[20];
    float points;
};
```

### السؤال 8
لماذا اعتُمد في البنية `Student` على `char firstName[20]` بدلاً من الصف `string`؟

A- لأن `string` غير مدعوم في ++C أساساً
B- لأن حجم `string` ثابت دوماً مثل المصفوفة
C- لأن الموضوع له علاقة بتثبيت حجم السجل المقروء والمكتوب من/إلى الملف، بينما `string` ذو حجم متغيّر يحتاج ترتيبات إضافية
D- لا فرق فعلي، والاستخدام هنا اعتباطي فقط

**الإجابة الصحيحة: C**
**التوضيح:** ذكرت المحاضرة أن استخدام مصفوفة المحارف بدلاً من `string` مرتبط بضرورة تثبيت حجم السجل؛ فقراءة/كتابة سجل متغيّر الحجم (كما لو استُخدم `string`) ممكنة نظرياً لكنها أصعب وتتطلب ترتيبات إضافية.

---

### السؤال 9
لدينا الكائن `Student v;` وملف مفتوح باسم `file`. أي الأسطر التالية صحيحة لكتابة الكائن `v` بالكامل إلى الملف؟

A- `file.write(v, sizeof(student));`
B- `file.write((char*)(&v), sizeof(Student));`
C- `file << v;`
D- `file.write((Student*)(&v), sizeof(Student));`

**الإجابة الصحيحة: B**
**التوضيح:** التابع `write` يتطلب مؤشراً من النوع `char*` (أو ما يعادله عبر `reinterpret_cast<char*>`)، لذلك يجب قصر (تحويل) مؤشر `Student*` إلى `char*`، مع تمرير `sizeof(Student)` كوسيط ثانٍ يمثل عدد البايتات المطلوب كتابتها.

---

### السؤال 10
ما الفائدة من قصر (تحويل) مؤشر السجل من `Student*` إلى `char*` عند استدعاء `write` أو `read`؟

A- لتحسين سرعة التنفيذ فقط
B- لأن التابعين `write` و`read` يتعاملان مع الملف كسلسلة بايتات (`char`)، فيجب أن يكون نوع المؤشر الممرَّر متوافقاً مع ذلك
C- لأن `Student*` غير مسموح استخدامه إطلاقاً في اللغة
D- لتحويل السجل تلقائياً إلى نص قابل للقراءة

**الإجابة الصحيحة: B**
**التوضيح:** بما أن الملفات العشوائية تُعامَل كمصفوفة بايتات، فإن توابع `fstream` مثل `write`/`read` تتوقع مؤشراً من نوع `char*`؛ لذلك نلجأ للقصر (Cast) أو لعملية `reinterpret_cast<char*>`.

---

### السؤال 11
بفرض أردنا قراءة سجل من الملف إلى الكائن `Student v2;`، أي الأسطر التالية هي الصحيحة؟

A- `file.read((char*)(&v2), sizeof(Student));`
B- `file.read((char*)(v2), sizeof(Student*));`
C- `file >> v2;`
D- `file.get((char*)(&v2));`

**الإجابة الصحيحة: A**
**التوضيح:** يجب تمرير عنوان الكائن `&v2` مقصوراً إلى `char*`، مع تمرير `sizeof(Student)` (حجم السجل وليس حجم المؤشر) كوسيط ثانٍ لتحديد عدد البايتات المقروءة.

---

### السؤال 12
عند تنفيذ عملية `read` أو `write` بنجاح على ملف عشوائي، فإن مؤشر القراءة/الكتابة الموافق:

A- يبقى ثابتاً في مكانه دون أي تغيير
B- يعود دوماً إلى بداية الملف
C- يتحرك تلقائياً بمقدار حجم العملية المنفذة (عدد البايتات التي تمت قراءتها/كتابتها)
D- ينتقل دوماً إلى نهاية الملف بغض النظر عن حجم العملية

**الإجابة الصحيحة: C**
**التوضيح:** نص المحاضرة صريح: "عندما ننفذ عملية قراءة أو كتابة يتحرك المؤشر الموافق بحجم العملية المنفذة".

---

### السؤال 13 (صح / خطأ)
يمتلك كل ملف عشوائي مفتوح بالوضع `ios::in | ios::out` مؤشرين منفصلين: أحدهما لموقع القراءة (`get pointer`) والآخر لموقع الكتابة (`put pointer`).

A- صح
B- خطأ

**الإجابة الصحيحة: A - صح**
**التوضيح:** تمتلك الملفات العشوائية مؤشرَين مستقلَّين: مؤشر القراءة `get` الذي يُتحكم به عبر `seekg`/`tellg`، ومؤشر الكتابة `put` الذي يُتحكم به عبر `seekp`/`tellp`.

---

### السؤال 14
لتغيير موقع مؤشر **القراءة** نستخدم التابع (X)، ولمعرفة موقعه الحالي نستخدم التابع (Y). ما هما X وY بالترتيب؟

A- `seekp` و`tellp`
B- `seekg` و`tellg`
C- `tellg` و`seekg`
D- `getpos` و`setpos`

**الإجابة الصحيحة: B**
**التوضيح:** مؤشر القراءة `get` يُغيَّر موقعه بالتابع `seekg`، ويُعرَف موقعه الحالي بالتابع `tellg`. أما `seekp`/`tellp` فهما لمؤشر الكتابة `put`.

---

## القسم الرابع: تحريك المؤشرات (seek) ومعرفة عدد السجلات

### السؤال 15
تعليمة `seek` (مثل `seekg` أو `seekp`) تحتاج بشكل عام إلى وسيطين، هما:

A- اسم الملف، وحجم السجل
B- مقدار البايتات المراد الانتقال بها، ونقطة البداية المرجعية
C- اسم البنية، وعدد عناصر المصفوفة
D- نوع الملف (نصي/ثنائي)، وحجم الملف

**الإجابة الصحيحة: B**
**التوضيح:** تعليمات `seek` تأخذ وسيطاً أول يمثل مقدار (عدد) البايتات التي نريد الانتقال بها، ووسيطاً ثانياً يحدد نقطة البداية المرجعية (`ios::beg` أو `ios::cur` أو `ios::end`).

---

### السؤال 16
أي الأعلام التالية تمثل نقطة البداية المرجعية "بداية الملف" (أي ما قبل أول بايت فيه)؟

A- `ios::cur`
B- `ios::end`
C- `ios::beg`
D- `ios::app`

**الإجابة الصحيحة: C**
**التوضيح:** `ios::beg` يعتبر نقطة البداية هي ما قبل أول بايت في الملف، وننتقل منه. أما `ios::cur` فنقطة البداية هي الموقع الحالي للمؤشر، و`ios::end` هي نهاية الملف.

---

### السؤال 17
عند استخدام `ios::cur` كنقطة مرجعية في `seek`، فإن الانتقال يتم بالنسبة إلى:

A- بداية الملف دائماً
B- نهاية الملف دائماً
C- الموقع الذي يؤشر عليه المؤشر (get أو put) حالياً
D- منتصف الملف

**الإجابة الصحيحة: C**
**التوضيح:** `ios::cur` تعتبر نقطة البداية هي البايت الذي يؤشر عليه المؤشر حالياً، فيكون الانتقال نسبياً من هذا الموقع.

---

### السؤال 18 (صح / خطأ)
عند استخدام `ios::end` كنقطة مرجعية، يُفضَّل استخدام قيم **سالبة** للانتقال، لأن القيم السالبة في عملية النقل تعني التراجع للخلف (نحو بداية الملف).

A- صح
B- خطأ

**الإجابة الصحيحة: A - صح**
**التوضيح:** هذا مذكور حرفياً في المحاضرة: يمكن استخدام قيم سالبة في عملية النقل لأنها تعني التراجع للخلف، ويُفضَّل استخدام القيم السالبة مع `ios::end` تحديداً (لأن نقطة الانطلاق أصلاً هي آخر الملف).

---

### السؤال 19
لحساب عدد السجلات المخزَّنة ضمن ملف عشوائي يحوي نوعاً واحداً فقط من السجلات ذات حجم ثابت، أي الأكواد التالية صحيح؟

A
```cpp
file.seekg(0, ios::beg);
long size = file.tellg();
int count = size * sizeof(Student);
```

B
```cpp
file.seekg(0, ios::end);
long size = file.tellg();
int count = size / sizeof(Student);
```

C
```cpp
file.seekp(0, ios::beg);
long size = file.tellp();
int count = size + sizeof(Student);
```

D
```cpp
int count = file.tellg() / file.tellp();
```

**الإجابة الصحيحة: B**
**التوضيح:** يجب أولاً نقل مؤشر القراءة إلى نهاية الملف عبر `seekg(0, ios::end)` لأن الحجم غير معروف مسبقاً، ثم تُقرأ القيمة المطلقة للموقع الحالي عبر `tellg()`، وأخيراً يُقسَم حجم الملف على حجم السجل الواحد للحصول على عدد السجلات.

---

### السؤال 20
لماذا لا يمكننا ببساطة استدعاء `file.seekg(FILE_SIZE, ios::beg)` مباشرةً للانتقال إلى نهاية الملف دون معرفة حجمه؟

A- لأن `ios::beg` لا يقبل قيماً موجبة
B- لأننا أساساً لا نعرف قيمة `FILE_SIZE` (حجم الملف) مسبقاً؛ ولهذا نلجأ إلى علم `ios::end` مباشرة للانتقال إلى النهاية دون الحاجة لمعرفة الحجم
C- لأن هذه العملية تحذف محتوى الملف
D- لأن `seekg` لا يعمل إلا مع الملفات النصية

**الإجابة الصحيحة: B**
**التوضيح:** الهدف الأساسي من معرفة حجم الملف هو استخدام `ios::end` بالذات، لأننا لا نملك مسبقاً قيمة الحجم لنستخدمها مع `ios::beg`؛ لذلك نعتمد على تحريك المؤشر إلى النهاية مباشرة ثم قراءة موقعه بـ`tellg`.

---

### السؤال 21
منذ إصدار ++C 2017 (C++17)، أصبح بالإمكان معرفة حجم الملف مباشرة عبر:

A- `sizeof(file)`
B- `filesystem::file_size` مع تمرير مسار الملف
C- `file.getSize()`
D- لا توجد طريقة بديلة عن `seekg`/`tellg`

**الإجابة الصحيحة: B**
**التوضيح:** ذكرت المحاضرة أن الأمر `filesystem::file_size` (أمر جديد أُضيف في نسخة C++17) يتطلب تمرير مسار الملف، ويمكن استخدامه كبديل عن أسلوب تحريك المؤشر لمعرفة حجم الملف.

---

## القسم الخامس: قراءة وكتابة مصفوفة من السجلات

### السؤال 22
لدينا `Student students[5];` وملف مفتوح باسم `f`. أي الأسطر التالية **غير صحيحة** لقراءة المصفوفة كاملة من الملف؟

A- `f.read((char*)(students), sizeof(Student) * 5);`
B- `f.read((char*)(students), sizeof(students));`
C- `f.read((char*)(&students), sizeof(Student));`
D- كلا الخيارين A وB صحيحان

**الإجابة الصحيحة: C**
**التوضيح:** يجب أن يكون الحجم الممرَّر مساوياً لعدد عناصر المصفوفة مضروباً بحجم العنصر الواحد (أو حجم المصفوفة كلها كما في B)؛ أما تمرير `sizeof(Student)` فقط (حجم عنصر واحد) فيقرأ سجلاً واحداً لا خمسة، وهو خطأ إذا كان الهدف قراءة المصفوفة كاملة.

---

### السؤال 23
عند قراءة أو كتابة مصفوفة من السجلات دفعة واحدة عبر `read`/`write`، يجب مراعاة نقطتين أساسيتين. ما هما؟

A- تحديد اسم البنية، وترتيب الحقول ضمنها
B- تمرير مؤشر على المصفوفة، وأن يكون الحجم الممرَّر مساوياً لعدد العناصر × حجم العنصر الواحد (أو حجم المصفوفة)
C- استخدام `ios::app` فقط، وإغلاق الملف بعد كل عنصر
D- تحويل كل عنصر إلى نص قبل الكتابة

**الإجابة الصحيحة: B**
**التوضيح:** هذا منصوص عليه حرفياً في المحاضرة كنقطتين واجب مراعاتهما عند التعامل مع مصفوفة من السجلات.

---

### السؤال 24
بفرض لدينا الكود التالي المستخدَم لقراءة السجلات من ملف بترتيب عكسي (من الأخير إلى الأول):

```cpp
f.seekp(0, ios::end);
long size = f.tellp();
int structs = size / sizeof(Student);
for(int i = structs - 1; i >= 0; i--){
    Student y;
    f.seekg(i * sizeof(Student));
    f.read((char*)(&y), sizeof(Student));
    cout << y.id << "\t" << y.firstName << endl;
}
```

ما هو ترتيب طباعة السجلات على الشاشة؟

A- من أول سجل مخزَّن في الملف إلى آخر سجل
B- من آخر سجل مخزَّن في الملف إلى أول سجل
C- بترتيب عشوائي غير محدد
D- يُطبع السجل الأخير فقط

**الإجابة الصحيحة: B**
**التوضيح:** الحلقة تبدأ من `i = structs - 1` (فهرس آخر سجل) وتتناقص حتى `i = 0` (أول سجل)، وفي كل مرة يُحرَّك مؤشر القراءة إلى الموقع `i * sizeof(Student)` عبر `seekg` ثم يُقرأ السجل الموافق؛ لذلك تُطبع السجلات بترتيب عكسي من النهاية للبداية، تماماً كما ذُكر صراحة في المحاضرة.

---

### السؤال 25
في السطر `f.seekg(i * sizeof(Student));` الوارد في السؤال السابق، لماذا لم يُستخدم علم مرجعي (`ios::beg` مثلاً) صراحة كوسيط ثانٍ؟

A- لأن هذا خطأ برمجي يجب تصحيحه دائماً
B- لأن `seekg` بوسيط واحد فقط تفترض ضمنياً `ios::beg` كنقطة مرجعية افتراضية
C- لأن `seekg` بوسيط واحد تتجاهل الملف بأكمله ولا تنفذ شيئاً
D- لأن هذا يعني الانتقال من نهاية الملف تلقائياً

**الإجابة الصحيحة: B**
**التوضيح:** عند استدعاء `seek` بوسيط واحد فقط (الإزاحة/Offset)، فإن نقطة البداية الافتراضية هي `ios::beg`؛ ولذلك فإن `i * sizeof(Student)` يمثل الموقع المطلق للسجل رقم `i` بدءاً من أول الملف، وهو تماماً ما يُراد للوصول المباشر إلى أي سجل ضمن الملف.

---

## جدول الإجابات السريع

| رقم السؤال | الإجابة | رقم السؤال | الإجابة |
|:---:|:---:|:---:|:---:|
| 1 | B | 14 | B |
| 2 | B | 15 | B |
| 3 | A | 16 | C |
| 4 | C | 17 | C |
| 5 | B | 18 | A (صح) |
| 6 | C | 19 | B |
| 7 | C | 20 | B |
| 8 | C | 21 | B |
| 9 | B | 22 | C |
| 10 | B | 23 | B |
| 11 | A | 24 | B |
| 12 | C | 25 | B |
| 13 | A (صح) | | |

---

*انتهت الأسئلة — بالتوفيق للجميع* 🎓
