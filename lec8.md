# أسئلة اختبار مؤتمتة – المحاضرة الثامنة
## مقرر البرمجة 2 – القسم النظري – جامعة حمص
### إعداد بأسلوب مشابه لأسئلة د. أسامه ناصر / د. محسن حسين

---

> **ملاحظة:** تغطي هذه الأسئلة المفاهيم التالية من المحاضرة الثامنة:
> 1. أثر الكلمة `const` على الصفوف والأغراض
> 2. الحقل الثابت (const) ضمن الغرض
> 3. التوابع والصفوف الصديقة (Friend)
> 4. التعامل مع المؤشرات ضمن الصفوف
> 5. العناصر الساكنة (Static)
> 6. التحميل الزائد للعمليات (Operator Overloading)
>
> **عدد الأسئلة: 25 سؤال — اختر الإجابة الصحيحة**

---

## القسم الأول: أثر الكلمة const على الصفوف والأغراض

### السؤال 1
بفرض لدينا الصف التالي:
```cpp
class Time{
public:
    Time(int=0,int=0,int=0);
    void setHour(int);
    int getHour() const;
};
```
بفرض التصريح: `const Time c(10,12,13);`
ما هي التوابع التي يمكن استدعاؤها اعتماداً على الغرض `c`؟

A- جميع توابع الصف Time بلا استثناء
B- فقط التوابع الموسومة بالكلمة `const`
C- فقط الباني والهادم
D- لا يمكن استدعاء أي تابع على الإطلاق

**الإجابة الصحيحة: B**
**الشرح:** بما أن الغرض `c` معرّف كـ `const`، فإن قواعد اللغة تمنع استدعاء أي تابع قد يُعدّل محتويات الغرض، لذلك يُسمح فقط باستدعاء التوابع التي وُسمت صراحةً بالكلمة `const` بعد قوسي المعاملات.

---

### السؤال 2
صح أم خطأ: إضافة الكلمة `const` في نهاية تعريف التابع (بعد الأقواس) تؤثر على الكود الداخلي (body) للتابع وتغيّر طريقة تنفيذه.

A- صح 
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** كما ورد في المحاضرة، لا يؤثر إضافة `const` على كود التابع نفسه، بل هي فقط "وسم" (tag) يسمح باستدعاء هذا التابع اعتماداً على غرض ثابت.

---

### السؤال 3
بفرض الكود التالي:
```cpp
class A{
public:
    void f() const;
    void g();
};
int main(){
    const A a1;
    A a2;
    a1.f();   //1
    a1.g();   //2
    a2.f();   //3
    a2.g();   //4
}
```
أي من الأسطر التالية يسبب **خطأ في الترجمة**؟

A- السطر 1 
B- السطر 2 
C- السطر 3 
D- السطر 4

**الإجابة الصحيحة: B**
**الشرح:** الغرض `a1` ثابت (const)، لذلك لا يمكن استدعاء التابع `g()` عليه لأنه غير موسوم بـ `const`. أما `a2` فهو غرض متحول عادي، فيمكنه استدعاء التابعين معاً.

---

### السؤال 4
عند تعريف غرض ثابت من صف ما، يجب أن تُكتب الكلمة `const`:

A- بعد اسم الصف مباشرة فقط
B- قبل تعريف الغرض (تسبق تعريف الصف/الغرض)
C- بعد اسم الغرض مباشرة
D- داخل الباني فقط

**الإجابة الصحيحة: B**
**الشرح:** حسب المحاضرة، في حال أردنا تعريف الغرض على أنه `const` يجب أن تسبق الكلمة المفتاحية `const` تعريف الغرض، كما في: `const bdate(1,1,1991);`

---

## القسم الثاني: الحقل الثابت (const) ضمن الغرض

### السؤال 5
بفرض الصف التالي:
```cpp
class Increment{
public:
    Increment(int c=0, int i=1);
private:
    int count;
    const int increment;
};
```
أي من التحقيقات التالية للباني هو الصحيح؟

A
```cpp
Increment::Increment(int c,int i){
    count=c; increment=i;
}
```
B
```cpp
Increment::Increment(int c,int i):increment(i){
    count=c;
}
```
C
```cpp
Increment::Increment(int c,int i):count(c){
    increment=i;
}
```
D
```cpp
Increment::Increment(int c,int i){
    increment=i; count=c;
}
```

**الإجابة الصحيحة: B**
**الشرح:** بما أن `increment` حقل ثابت (const)، لا يمكن إسناد قيمة له داخل جسم الباني عبر عامل الإسناد `=`، بل يجب تهيئته حصراً عبر قائمة التهيئة (Initialization List) الموجودة بعد النقطتين `:` وقبل قوسي جسم الباني.

---

### السؤال 6
صح أم خطأ: يمكن إسناد قيمة لحقل من نوع `const` ضمن جسم الباني (بين قوسي `{}`) بدلاً من استخدام قائمة التهيئة.

A- صح 
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** الحقل الثابت يجب أن يُعطى قيمته قبل تنفيذ أي كود ضمن جسم الباني، وقائمة التهيئة هي الطريقة الوحيدة الممكنة لتهيئة الحقول من نوع `const`.

---

### السؤال 7
ما هي الصياغة الصحيحة لتهيئة أكثر من حقل باستخدام قائمة التهيئة ضمن الباني؟

A- `Class::Class(int a,int b):x(a);y(b){}`
B- `Class::Class(int a,int b):x(a),y(b){}`
C- `Class::Class(int a,int b){x(a),y(b);}`
D- `Class::Class(int a,int b):x=a,y=b{}`

**الإجابة الصحيحة: B**
**الشرح:** الحقول المتعددة ضمن قائمة التهيئة تُفصل بفاصلة `,` وليس بفاصلة منقوطة، ولا تُستخدم إشارة `=` بل الأقواس `()`.

---

### السؤال 8
لماذا يجبر المترجم على تنفيذ قائمة التهيئة قبل أي سطر ضمن جسم الباني؟

A- لأن المترجم يضيف أسطراً إضافية في بداية الباني لضمان إسناد قيم للثوابت قبل تنفيذ أي تعليمة أخرى
B- لأن هذا اختياري وليس إلزامياً
C- لأن قائمة التهيئة أسرع تنفيذاً من الأسطر العادية فقط
D- لا علاقة لهذا بالثوابت إطلاقاً

**الإجابة الصحيحة: A**
**الشرح:** كما ورد في المحاضرة، طريقة قائمة التهيئة تجبر المترجم على إسناد القيم قبل تنفيذ كود الباني، بمعنى آخر يضيف المترجم أسطراً إضافية في بداية الباني لتنفيذ هذه التعليمات، مما يضمن إسناد قيم للثوابت.

---

## القسم الثالث: التوابع والصفوف الصديقة (Friend)

### السؤال 9
التابع الصديق (friend function):

A- ينتمي للصف ويملك حق الوصول لكل الحقول
B- لا ينتمي للصف، لكنه يملك حق الوصول للحقول من نوع `private` و `protected`
C- ينتمي للصف لكن لا يملك حق الوصول للحقول الخاصة
D- لا ينتمي للصف ولا يملك حق الوصول لأي حقل

**الإجابة الصحيحة: B**
**الشرح:** حسب تعريف المحاضرة، التابع الصديق هو تابع لا ينتمي للصف، ولكن يُمنح صلاحية الوصول للحقول من نوع خاص (private) ومحمي (protected).

---

### السؤال 10
صح أم خطأ: علاقة الصداقة بين الصفوف علاقة متعدية (transitive)، أي إذا كان A صديقاً لـ B، وB صديقاً لـ C، فإن A يكون بالضرورة صديقاً لـ C.

A- صح  
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** من ميزات علاقة الصداقة المذكورة في المحاضرة أن العلاقة غير متعدية (non-transitive)، فصداقة A مع B وB مع C لا تعني بالضرورة أن A صديق C.

---

### السؤال 11
صح أم خطأ: إذا كان الصف A صديقاً للصف B، فهذا يعني بالضرورة أن الصف B صديق للصف A أيضاً.

A- صح 
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** الصداقة علاقة من طرف واحد (one-directional)؛ فمنح A صداقته لـ B لا يعني أن B يصبح صديقاً لـ A تلقائياً ما لم تُحدَّد العلاقة صراحة ضمن B.

---

### السؤال 12
بفرض الكود التالي:
```cpp
class Count{
    friend void setX(Count&,int);
public:
    Count(){x=0;}
private:
    int x;
};
```
أين يجب أن يوضع تصريح التابع الصديق ضمن الصف؟

A- ضمن القسم `public` حصراً
B- ضمن القسم `private` حصراً
C- يمكن وضعه في أي مكان ضمن الصف بغض النظر عن `public` أو `private`
D- يجب تصريحه خارج الصف حصراً

**الإجابة الصحيحة: C**
**الشرح:** بما أن التابع الصديق ليس جزءاً من الصف أصلاً، فإن محددات الوصول `public`/`private`/`protected` لا تؤثر عليه، وبالتالي يمكن كتابة تصريحه في أي موقع ضمن جسم الصف.

---

### السؤال 13
صح أم خطأ: كود التابع الصديق (جسم التابع) يُكتب كجزء من كتلة الصف نفسه، أي داخل الأقواس `{}` الخاصة بالصف.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** كود التابع الصديق يكون كتلة منفصلة تماماً عن الصف، ويُكتب خارج تعريف الصف تماماً كأي تابع عادي.

---

### السؤال 14
الصف الصديق (friend class) هو:

A- صف يرث من الصف الحالي
B- صف يحتوي على غرض من الصف الحالي (تراكب composition)
C- صف خارجي كلياً عن الصف الحالي ولا يرتبط به بأي علاقة تراكب أو وراثة
D- صف داخلي معرّف ضمن الصف الحالي

**الإجابة الصحيحة: C**
**الشرح:** كما عرّفت المحاضرة، الصف الصديق هو صف خارجي كلياً عن الصف الحالي، أي لا يرتبط به بأي علاقة من نوع تراكب composition (صف داخل صف) ولا بعلاقة وراثة.

---

## القسم الرابع: التعامل مع المؤشرات

### السؤال 15
بفرض التعريف: `Class1 *p = new Class1(1,2);` ما هو التابع الذي يُستدعى تلقائياً عند تنفيذ التعليمة `delete p;`؟

A- الباني الافتراضي (Default Constructor)
B- الباني المعاملي (Parameterized Constructor)
C- الهادم (Destructor)
D- لا يُستدعى أي تابع

**الإجابة الصحيحة: C**
**الشرح:** كل استدعاء لعملية `delete` يستدعي الهادم الخاص بالصف، وذلك لتحرير الذاكرة المخصصة للغرض بشكل صحيح.

---

### السؤال 16
صح أم خطأ: مقابل كل تعليمة `new` يجب أن نستدعي تعليمة `delete` مقابلة لها لتفادي مشكلة تسرب الذاكرة (Memory Leak).

A- صح 
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** كما نبّهت المحاضرة، يجب ألا ننسى أنه مقابل كل تعليمة `new` يجب أن نستدعي تعليمة `delete` لتحرير الذاكرة المحجوزة.

---

## القسم الخامس: العناصر الساكنة (Static)

### السؤال 17
الحقل الساكن (static field) ضمن صف:

A- تكون له نسخة منفصلة خاصة بكل غرض من أغراض الصف
B- تكون قيمته مشتركة بين جميع أغراض الصف
C- لا يمكن الوصول إليه إلا من داخل الباني فقط
D- يجب تهيئته من جديد داخل كل باني يُستدعى

**الإجابة الصحيحة: B**
**الشرح:** من أهم ميزات الحقل الساكن أن قيمته تكون مشتركة بين كل الأغراض المُنشأة من الصف، وليست نسخة خاصة بكل غرض.

---

### السؤال 18
بفرض الصف التالي:
```cpp
class Employee{
public:
    static int getCount();
private:
    static int count;
};
```
أين يجب أن تتم تهيئة الحقل الساكن `count` بقيمته الابتدائية؟

A- ضمن تعريف الصف مباشرة (داخل الأقواس)
B- ضمن الباني فقط
C- خارج الصف باستخدام عامل تحديد النطاق `::`
D- لا حاجة لتهيئته لأنه يبدأ بالقيمة صفر تلقائياً

**الإجابة الصحيحة: C**
**الشرح:** كما ورد في المحاضرة عبر السطر `int Employee::count=0;`، تتم تهيئة الحقول الساكنة خارج الصف باستخدام عامل تحديد النطاق `::` مرة واحدة عند بدء تنفيذ الملف.

---

### السؤال 19
صح أم خطأ: يمكن للتابع الساكن (static function) أن يتعامل مع مؤشر `this`.

A- صح 
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** بما أن التابع الساكن لا يرتبط بغرض محدد بل بالصف ككل، فهو لا يملك مؤشر `this` ولا يتعامل معه إطلاقاً.

---

### السؤال 20
صح أم خطأ: يمكن للتابع الساكن أن يصل مباشرة إلى الحقول غير الساكنة (non-static) ضمن الغرض.

A- صح 
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** بما أن التابع الساكن لا يملك مؤشر `this` ولا يرتبط بغرض معين، فلا يمكنه الوصول إلى العناصر غير الساكنة ضمن الغرض؛ فهو يستطيع الوصول فقط إلى العناصر الساكنة.

---

### السؤال 21
بفرض أن الباني يزيد الحقل الساكن `count` بمقدار 1 عند كل استدعاء، وأن الهادم ينقصه بمقدار 1، ما هو خرج البرنامج التالي؟
```cpp
int main(){
    Employee *e1=new Employee("James","Kirck");
    Employee *e2=new Employee("Han","Solo");
    cout<<Employee::getCount();
    delete e1;
    cout<<Employee::getCount();
}
```

A- 22 
B- 21 
C- 12 
D- 11

**الإجابة الصحيحة: B**
**الشرح:** بعد إنشاء الغرضين `e1` و `e2` تصبح قيمة `count` تساوي 2، فيُطبع "2". بعد حذف `e1` عبر `delete` يُستدعى الهادم فتنقص القيمة إلى 1، فيُطبع "1". الخرج الكلي هو "21".

---

## القسم السادس: التحميل الزائد للعمليات (Operator Overloading)

### السؤال 22
عند تنفيذ تحميل زائد لعملية `+` بحيث تُعيد العملية غرضاً من **نفس** الصف الذي طُبّقت عليه العملية، يجب أن يُعرّف التابع كـ:

A- تابع صديق (friend)
B- تابع عضو (member function)
C- تابع ساكن (static)
D- تابع خارجي غير صديق

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، في حال كانت العملية المُحمّلة زائدياً تُعيد نمطاً أساسياً أو غرضاً من نفس الصف الذي نُحمّل العملية عليه، يكون التابع عضواً في الصف.

---

### السؤال 23
عند تنفيذ تحميل زائد لعملية `<<` لطباعة غرض من صف `Complex` باستخدام `cout`، يجب تعريف التابع كـ:

A- تابع عضو عادي ضمن الصف Complex
B- تابع ساكن
C- تابع صديق، لأن العملية لا تعيد غرضاً من نفس الصف Complex
D- باني إضافي

**الإجابة الصحيحة: C**
**الشرح:** التابع `operator<<` يُعيد مرجعاً من نوع `ostream&` وليس غرضاً من نفس الصف `Complex`، وبما أن العملية لا تُعيد غرضاً من نفس الصف الذي نطبق عليه العملية، يجب تعريف التابع كتابع صديق (friend).

---

### السؤال 24
بفرض الكود التالي:
```cpp
class Complex{
private:
    double real, imaginary;
public:
    Complex(double r=0,double i=0):real(r),imaginary(i){}
    Complex& operator+(Complex& other){
        this->real+=other.real;
        this->imaginary+=other.imaginary;
        return *this;
    }
};
```
ما هو نوع القيمة المُعادة من التابع `operator+` كما هو مكتوب أعلاه؟

A- `Complex` (بالقيمة)
B- `Complex&` (بالمرجع)
C- `void`
D- `Complex*` (مؤشر)

**الإجابة الصحيحة: B**
**الشرح:** التوقيع المُصرّح به للتابع هو `Complex& operator+(...)`، وتنفيذه يعيد `*this` أي مرجعاً إلى الغرض الحالي بعد تعديله، وهذا متوافق مع كون العملية تُعدّل الغرض نفسه وتُعيد مرجعاً إليه.

---

### السؤال 25
صح أم خطأ: في حال كان التحميل الزائد لعملية ما يُعيد نمطاً أساسياً (مثل `int` أو `bool`) وليس غرضاً من نفس الصف، فمن الممكن أيضاً أن يكون التابع عضواً في الصف حسب طبيعة العملية.

A- صح 
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** القاعدة العامة من المحاضرة هي: إذا كان التحميل الزائد يُعيد نمطاً أساسياً **أو** غرضاً من نفس الصف الذي نُحمّل العملية عليه، يكون التابع عضواً؛ ويكون صديقاً فقط عندما تُعيد العملية غرضاً من صف آخر مختلف عن الصف الحالي.

---

## جدول تلخيصي سريع للإجابات السابقة

| # | الإجابة | # | الإجابة | # | الإجابة |
|---|---------|---|---------|---|---------|
| 1 | B | 10 | B | 19 | B |
| 2 | B | 11 | B | 20 | B |
| 3 | B | 12 | C | 21 | B |
| 4 | B | 13 | B | 22 | B |
| 5 | B | 14 | C | 23 | C |
| 6 | B | 15 | C | 24 | B |
| 7 | B | 16 | A | 25 | A |
| 8 | A | 17 | B | | |
| 9 | B | 18 | C | | |

---

*تم إعداد هذه الأسئلة اعتماداً على محتوى المحاضرة الثامنة (const، الحقول الثابتة، التوابع/الصفوف الصديقة، المؤشرات، العناصر الساكنة، التحميل الزائد للعمليات) وبأسلوب مشابه لنموذج امتحان مادة البرمجة 2 الرسمي.*
