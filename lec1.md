# بنك أسئلة مؤتمتة — المحاضرة 1: المؤشرات (Pointers)
### مقرر البرمجة -2- | إعداد آلي بناءً على تحليل أسلوب د. أسامه ناصر في نموذج الامتحان المرفق

---

## ملاحظات حول منهجية إعداد الأسئلة

بعد دراسة نموذج امتحان الدكتور، لوحظت الخصائص التالية في أسلوبه، وتم اعتمادها في صياغة الأسئلة أدناه:

- استخدام مقاطع كود قصيرة (Code Tracing) يُطلب تتبع تنفيذها وتحديد الخرج بدقة.
- خيارات متعددة (A/B/C/D) غالبًا ما تتضمن **مشتتات منطقية** (خطأ ترجمة / خطأ تنفيذ / قيمة عشوائية) وليس فقط أرقامًا.
- أسئلة **صح/خطأ** مباشرة لاختبار فهم قاعدة نظرية واحدة.
- التركيز على "الفخاخ" الشائعة: الفرق بين تعديل المؤشر نفسه وتعديل ما يؤشر إليه، الفرق بين `&` و`*`، حجم المؤشر، وإدارة الذاكرة الديناميكية.
- كل سؤال درجة واحدة تقريبًا (وحدة معرفية واحدة لكل سؤال)، بلا حشو.

تم تغطية **كامل عناوين المحاضرة الأولى**: ماهو المؤشر، فائدة المؤشرات، العلاقة بين المؤشرات وإدارة الذاكرة (Heap/Data)، نقاط عامة، أمثلة إضافية (تمرير بالقيمة/بالمرجع/بالمؤشر)، وحجم المؤشر.

---

## القسم الأول: مفهوم المؤشر وطبيعته (4 أسئلة)

### السؤال 1
المؤشر (Pointer) هو نوع خاص من المتحولات:

A- يخزن قيمة عددية فقط
B- يخزن عنوان مكان تخزين متحول آخر في الذاكرة
C- يخزن نصًا يمثل اسم المتحول
D- لا يحتاج إلى تخزين في الذاكرة إطلاقًا

**الإجابة الصحيحة: B**
**الشرح:** كما ورد حرفيًا في المحاضرة، المؤشر "لا يخزن قيمة وإنما يخزن عنوان"، أي مكان تواجد قيمة متحول آخر (تشبيه اللافتة الطرقية).

---

### السؤال 2
أي من التالي هو الوصف الأدق لدور المؤشر بحسب التشبيه المستخدم في المحاضرة؟

A- خزان يحتفظ بالقيمة النهائية
B- لافتة طرقية تدلّنا على مكان تواجد القيمة
C- نسخة طبق الأصل عن المتحول الأصلي
D- دالة تعيد قيمة المتحول

**الإجابة الصحيحة: B**
**الشرح:** استُخدم تشبيه اللافتة الطرقية (Homs/Damascus) في المحاضرة لتوضيح أن المؤشر لا يحمل "الوجهة" (القيمة) بل "الاتجاه" إليها (العنوان).

---

### السؤال 3
بفرض التصريح التالي: `int x = 10;` ثم `int *y = &x;` — العملية `&` هنا تعني:

A- خذ محتوى المتحول x
B- خذ عنوان الذاكرة الخاص بالمتحول x
C- احجز ذاكرة جديدة لمتحول اسمه x
D- قارن بين قيمتين

**الإجابة الصحيحة: B**
**الشرح:** حسب المحاضرة، عملية `&` (التمرير بالمرجع/أخذ العنوان) "تعني أعطني عنوان الذاكرة الخاص بالمتحول".

---

### السؤال 4
ضع رمز `*` قبل اسم المتحول أثناء التعريف (مثل `int *y`) يؤدي إلى:

A- تحويله إلى ثابت (constant)
B- تحويله إلى مؤشر
C- حذفه من الذاكرة
D- مضاعفة قيمته

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة: "وضع رمز `*` قبل اسم المتحول أثناء التعريف يحوله لمؤشر".

---

## القسم الثاني: إدارة الذاكرة الديناميكية (Heap) وفائدة المؤشرات (5 أسئلة)

### السؤال 5
بفرض الكود: `int x = 10;` — عند تحليل الملف التنفيذي (Executable)، فإن القيمة 10 تكون مخزنة مسبقًا ضمن:

A- قسم Code
B- قسم Data
C- قسم Header فقط
D- لا تُخزَّن إطلاقًا في الملف التنفيذي

**الإجابة الصحيحة: B**
**الشرح:** المتحولات العادية (غير الديناميكية) تُخزَّن قيمها ضمن قسم `data` في الملف التنفيذي وتُحمَّل إلى الذاكرة عند فتح البرنامج.

---

### السؤال 6
بفرض الكود: `int *y = new int; *y = 10;` — هل ستجد القيمة 10 مخزنة ضمن قسم `data` في الملف التنفيذي؟

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** كما ورد حرفيًا في المحاضرة: "لن نجد القيمة 10 في قسم data في الملف التنفيذي"، لأن هذه القيمة تُخزَّن ديناميكيًا في الكومة (Heap) أثناء التنفيذ فقط، وليست جزءًا من الملف التنفيذي على القرص.

---

### السؤال 7
الكومة (Heap) هي:

A- جزء من الملف التنفيذي يحوي الكود المصدري
B- مجال من الذاكرة يمنحه نظام التشغيل للبرنامج ليحجز ذاكرة جديدة أثناء التنفيذ
C- نسخة احتياطية من قسم Data
D- ترويسة الملف التنفيذي (Header)

**الإجابة الصحيحة: B**
**الشرح:** عرّفت المحاضرة الكومة بأنها "مجال من الذاكرة يمنحه نظام التشغيل للبرنامج كي يحجز ذاكرة جديدة أثناء التنفيذ".

---

### السؤال 8
الكلمة المفتاحية المستخدمة لحجز حجرة ذاكرية جديدة ضمن الكومة (Heap) هي:

A- `delete`
B- `alloc`
C- `new`
D- `malloc` فقط في ++C

**الإجابة الصحيحة: C**
**الشرح:** المحاضرة توضح أن `new` "تعمل على حجز حجرة ذاكرية ضمن الكومة heap".

---

### السؤال 9
تسمح لنا المؤشرات بإدارة الذاكرة بشكل مباشر، ويكون ذلك من خلال عمليتي:

A- `read` و `write`
B- `new` و `delete`
C- `push` و `pop`
D- `open` و `close`

**الإجابة الصحيحة: B**
**الشرح:** نصّت المحاضرة صراحة على أن فائدة المؤشرات تكمن في إمكانية "إنشاء وتدمير مؤشرات أثناء تنفيذ البرنامج" عبر عمليتي الحجز `new` والتحرير `delete`.

---

## القسم الثالث: العمليتان `&` و `*` (Dereferencing) (4 أسئلة)

### السؤال 10
بفرض: `int x = 10; int *y = &x;` — لتغيير القيمة التي يؤشر عليها y (وليس عنوانه)، يجب استخدام:

A- `y = 20;`
B- `*y = 20;`
C- `&y = 20;`
D- `x.y = 20;`

**الإجابة الصحيحة: B**
**الشرح:** المحاضرة توضح أن تغيير القيمة التي يؤشر عليها المؤشر يتطلب عملية "Dereferencing" باستخدام الرمز `*`. أما `y = 20;` فتُغيّر عنوان المؤشر نفسه (خطير) لا قيمة x.

---

### السؤال 11
ما ناتج تنفيذ البرنامج التالي؟
```cpp
int main(){
    int x = 10;
    int *y = &x;
    (*y)++;
    cout << x << " " << *y;
}
```

A- `10 10`
B- `11 11`
C- `10 11`
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** `(*y)++` يزيد القيمة التي يؤشر عليها y (أي x نفسها) بمقدار 1، فتصبح x تساوي 11، وبالتالي فإن `*y` تساوي أيضًا 11 لأنهما يشيران لنفس الخانة الذاكرية.

---

### السؤال 12
بفرض: `int a = 7; int *aPtr = &a;` — أي التعابير التالية **يساوي** قيمة `aPtr` (أي عنوان a)؟

A- `*aPtr`
B- `&*aPtr`
C- `a`
D- `*&a`

**الإجابة الصحيحة: B**
**الشرح:** `&*aPtr` تعني "خذ عنوان المحتوى الذي يؤشر عليه aPtr"، وبما أن aPtr يؤشر على a، فإن الناتج هو عنوان a، وهو نفسه القيمة المخزنة في aPtr. العمليتان `&` و `*` متعاكستان (Inverses of each other) كما ورد نصًا في المحاضرة.

---

### السؤال 13
ما ناتج تنفيذ البرنامج التالي؟
```cpp
int main(){
    int a = 7;
    int *aPtr = &a;
    cout << *&a;
}
```

A- عنوان a
B- 7
C- عنوان aPtr
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** `&a` تعطي عنوان a، ثم `*` تُرجع محتوى ذلك العنوان أي قيمة a نفسها = 7. هذا مطابق للمبدأ الذي شرحته المحاضرة في مثال `*&aPtr`.

---

## القسم الرابع: التمرير بالقيمة، بالمرجع، وبالمؤشر (5 أسئلة)

### السؤال 14
بفرض التوابع الثلاثة التالية:
```cpp
int f1(int n){ n = n*n*n; return n; }
int f2(int &n){ n = n*n*n; return n; }
int f3(int *n){ *n = *n * *n * *n; return n; }
```
أي التوابع **لا** يُعدّل على قيمة المتحول الأصلي خارج التابع؟

A- `f1` فقط
B- `f2` فقط
C- `f3` فقط
D- `f1` و `f3` معًا

**الإجابة الصحيحة: A**
**الشرح:** `f1` يستقبل نسخة من القيمة (تمرير بالقيمة)، فأي تعديل عليها محلي فقط. أما `f2` (تمرير بالمرجع) و `f3` (تمرير بمؤشر مع Dereferencing عبر `*n`) فكلاهما يُعدّلان على المتحول الأصلي فعليًا.

---

### السؤال 15
في التابع `f3(int *n)` أعلاه، لو استبدلنا `*n = *n * *n * *n;` بالسطر `n = n*n*n;` (دون استخدام `*`)، فإن هذا الكود:

A- سيُترجم بنجاح وسيُعدّل قيمة المتحول الأصلي كالسابق تمامًا
B- سيُترجم بنجاح لكنه سيُغيّر قيمة المؤشر (العنوان) وليس المتحول الأصلي
C- خطأ ترجمة حتمًا
D- سيحذف المتحول الأصلي من الذاكرة

**الإجابة الصحيحة: B**
**الشرح:** بحسب "نقاط عامة" في المحاضرة، في حال غيّرنا قيمة المؤشر نفسه (بدل ما يؤشر إليه عبر `*`)، فإن المتحول الأصلي لن يتأثر بأي شيء؛ لأننا غيّرنا "أين يشير المؤشر" لا "القيمة في تلك الخانة".

---

### السؤال 16
ما ناتج تنفيذ البرنامج التالي؟
```cpp
void update(int *n){ *n = *n + 10; }
int main(){
    int x = 5;
    update(&x);
    cout << x;
}
```

A- 5
B- 15
C- عنوان x
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** تم تمرير عنوان x إلى التابع، وبداخله جرى تعديل محتوى ذلك العنوان عبر `*n`، لذا فإن x الأصلية تتغير فعليًا لتصبح 15.

---

### السؤال 17
ما ناتج تنفيذ البرنامج التالي؟
```cpp
void update(int n){ n = n + 10; }
int main(){
    int x = 5;
    update(x);
    cout << x;
}
```

A- 5
B- 15
C- عنوان x
D- قيمة عشوائية

**الإجابة الصحيحة: A**
**الشرح:** التمرير هنا بالقيمة (Pass by Value)؛ التابع يستقبل نسخة من x، وأي تعديل عليها لا ينعكس على x الأصلية خارج التابع.

---

### السؤال 18
بفرض: `int x, y; int *xptr = &x, *yptr = &y;` — هل تكون `xptr == yptr`؟

A- نعم دائمًا؛ لأن x و y من نفس النوع
B- لا؛ لأن x و y متحولان مختلفان لهما عنوانان مختلفان في الذاكرة
C- خطأ ترجمة، لا يمكن مقارنة مؤشرين
D- يعتمد على قيمة x و y فقط

**الإجابة الصحيحة: B**
**الشرح:** كل متحول محلي منفصل يُحجز له مكان مستقل في الذاكرة (على المكدس Stack)، وبالتالي فإن عنوانيهما (وبالتالي قيمتا المؤشرين) مختلفان حتمًا، بغض النظر عن قيمتيهما.

---

## القسم الخامس: حجم المؤشر (Pointer Size) (3 أسئلة)

### السؤال 19
حجم المؤشر (بالبايت) في لغة ++C يعتمد بشكل أساسي على:

A- نوع البيانات الذي يؤشر عليه المؤشر (int، float، ...)
B- المعالج (Processor) ونظام التشغيل / إعدادات الترجمة (32 بت أو 64 بت)
C- قيمة المتحول المخزنة في العنوان
D- عدد المؤشرات المُعرّفة في البرنامج

**الإجابة الصحيحة: B**
**الشرح:** أوضحت المحاضرة أن حجم المؤشر "مرتبط بعدة عوامل" أهمها المعالج ونظام التشغيل: معالجات 32 بت تُمثّل المؤشر بـ 4 بايت، بينما معالجات 64 بت تحتاج 8 بايت — بغض النظر عن نوع البيانات المؤشَّر إليها.

---

### السؤال 20
بفرض الكود التالي، وتمت ترجمته باستخدام الأمر `g++ p.cpp -m32` (استهداف بيئة 32 بت):
```cpp
int main(){
    int a = 7;
    int *aPtr = &a;
    cout << sizeof(aPtr) << " Bytes" << endl;
    return 0;
}
```
ما الناتج الأرجح؟

A- 8 Bytes
B- 4 Bytes
C- 16 Bytes
D- 2 Bytes

**الإجابة الصحيحة: B**
**الشرح:** عند تحديد استهداف الترجمة لبيئة 32 بت (`-m32`)، يُصبح حجم أي مؤشر 4 بايت (32 بت)، بينما بدون هذا الخيار (بيئة 64 بت الافتراضية) يكون الناتج 8 Bytes.

---

### السؤال 21
هل حجم المؤشر لمتحول من نوع `int*` يساوي حجم المؤشر لمتحول من نوع `char*` على نفس الجهاز ونفس إعدادات الترجمة؟

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** حجم المؤشر كمتحول هو ثابت لكل المؤشرات على نفس المعالج ونفس وضع الترجمة (32/64 بت)، بغض النظر عن نوع البيانات المؤشَّر عليها؛ فالمؤشر دائمًا يخزّن "عنوانًا" وليس "قيمة من نوع معين".

---

## القسم السادس: أسئلة عامة ومخاطر المؤشرات (صح/خطأ) (3 أسئلة)

### السؤال 22
القيمة `NULL` أو `0` عند إسنادها لمؤشر تعني أن المؤشر يشير إلى مكان محدد وصالح في الذاكرة.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** ورد في "نقاط عامة" أن القيمة `NULL/0` "تشير إلى اللاشيء"، أي أن المؤشر لا يشير حاليًا لأي متحول صالح في الذاكرة.

---

### السؤال 23
التعامل مع المؤشرات يمنح المبرمج قوة كبيرة في إدارة الذاكرة، لكنه في المقابل قد يُسبب أخطاء ليس لها بداية أو نهاية واضحة إن لم يُستخدم بحذر.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** هذا مطابق حرفيًا لما ورد في شريحة "نقاط عامة": "لكنه أيضًا يعطي إمكانية أخطاء لها أول ما لها آخر" — إشارة إلى مخاطر المؤشرات (Dangling pointers، تسريب الذاكرة، إلخ).

---

### السؤال 24
بفرض: `int *y;` تم تعريف المؤشر دون تهيئته بأي عنوان، ثم نُفّذ الأمر `cout << *y;` مباشرة. ما الأقرب لوصف سلوك هذا الكود؟

A- سيطبع 0 دائمًا وبأمان تام
B- سلوك غير مُعرَّف (قد يطبع قيمة عشوائية أو يسبب خطأ تنفيذ Crash)
C- خطأ ترجمة حتمًا
D- يُهيَّأ المؤشر تلقائيًا للإشارة إلى x

**الإجابة الصحيحة: B**
**الشرح:** مؤشر غير مُهيَّأ يحتوي على قيمة عشوائية (Garbage) كعنوان، ومحاولة الوصول لمحتواه (Dereferencing) عبر `*y` يُعد من أخطر مخاطر المؤشرات التي حذّرت منها المحاضرة، وينتج عنها سلوك غير معرَّف.

---

## جدول تلخيصي سريع للإجابات

| رقم السؤال | الإجابة | رقم السؤال | الإجابة |
|:---:|:---:|:---:|:---:|
| 1 | B | 13 | B |
| 2 | B | 14 | A |
| 3 | B | 15 | B |
| 4 | B | 16 | B |
| 5 | B | 17 | A |
| 6 | B (خطأ) | 18 | B |
| 7 | B | 19 | B |
| 8 | C | 20 | B |
| 9 | B | 21 | A (صح) |
| 10 | B | 22 | B (خطأ) |
| 11 | B | 23 | A (صح) |
| 12 | B | 24 | B |

---

*تم إعداد هذه الأسئلة آليًا اعتمادًا على محتوى محاضرة "المؤشرات" وتحليل نمط أسئلة نموذج امتحان د. أسامه ناصر (استخدام تتبع الكود، خيارات مشتتة تقنية، وأسئلة صح/خطأ نظرية). يُنصح بمراجعتها من قبل المدرّس قبل الاستخدام الرسمي في التقييم.*
