# الجمهورية العربية السورية
## جامعة حمص – كلية الهندسة المعلوماتية
### مقرر البرمجة 2 – القسم النظري – السنة الأولى
**أسئلة مؤتمتة على المحاضرة الثانية عشرة: الكتابة والقراءة من الملفات الثنائية (Binary Files)**

> تم توليد هذه الأسئلة آلياً اعتماداً على محتوى المحاضرة 12 (مثال ترويسة الملف FileHeader، بنية الطالب Student، الصف StudentClass، التابعان writeToFile وreadFromFile)، وبأسلوب وصياغة ومستوى صعوبة مشابه لنموذج امتحان د. أسامه ناصر / د. محسن حسين.
>
> ✅ تم وضع الإجابة الصحيحة مع شرح مختصر بعد كل سؤال مباشرة.

---

## القسم الأول: أسئلة الاختيار من متعدد المرتبطة بشيفرة برمجية (3 درجات لكل سؤال)

> بفرض لدينا الشيفرات التالية من المحاضرة:

```cpp
// --- 1. FULLY ENCAPSULATED FILE HEADER ---
struct FileHeader {
    char magic[4] = {'S', 'T', 'U', 'D'};
    int majorVersion = 1;
    int minorVersion = 0;
    char teacherName[100] = {0};
    int studentCount = 0;
};

// --- 2. DATA BLOCK STRUCTURE ---
struct Student {
    char firstName[50];
    char lastName[50];
    int yearOfBirth;
    double gpa;
};

class StudentClass {
private:
    std::string internalTeacherName;
    std::vector<Student> students;
public:
    StudentClass() : internalTeacherName("Unassigned") {}
    void setTeacher(const std::string& name) { internalTeacherName = name; }
    void addStudent(const Student& student) { students.push_back(student); }

    bool writeToFile(const std::string& filename) const {
        std::ofstream outFile(filename, std::ios::binary | std::ios::trunc);
        if (!outFile) return false;
        FileHeader header;
        std::strncpy(header.teacherName, internalTeacherName.c_str(), sizeof(header.teacherName) - 1);
        header.studentCount = static_cast<int>(students.size());
        outFile.write(reinterpret_cast<const char*>(&header), sizeof(FileHeader));
        if (header.studentCount > 0) {
            outFile.write(reinterpret_cast<const char*>(students.data()), header.studentCount * sizeof(Student));
        }
        outFile.close();
        return true;
    }

    bool readFromFile(const std::string& filename) {
        std::ifstream inFile(filename, std::ios::binary);
        if (!inFile) return false;
        FileHeader header;
        inFile.read(reinterpret_cast<char*>(&header), sizeof(FileHeader));
        if (std::strncmp(header.magic, "STUD", 4) != 0) return false;
        internalTeacherName = header.teacherName;
        students.clear();
        if (header.studentCount > 0) {
            students.resize(header.studentCount);
            inFile.read(reinterpret_cast<char*>(students.data()), header.studentCount * sizeof(Student));
        }
        inFile.close();
        return true;
    }
};
```

---

### السؤال 1
ما هو الهدف من الحقل `magic` ضمن البنية `FileHeader`؟

A- تخزين اسم الملف
B- تمييز الملف الخاص بنا عن بقية أنواع الملفات الأخرى عند فتحه لاحقاً
C- تحديد حجم الملف على القرص
D- تخزين تاريخ إنشاء الملف

**الإجابة الصحيحة: B**
> الشرح: `magic` هو مجموعة من الرموز الخاصة (Signature) توضع في بداية الملف لتمييزه عن أي ملف آخر، بحيث يستطيع البرنامج التأكد أن الملف المفتوح هو فعلاً من النوع الذي يتوقعه قبل متابعة القراءة.

---

### السؤال 2
ما وظيفة الحقلين `majorVersion` و`minorVersion` ضمن `FileHeader`؟

A- تحديد عدد الطلاب المخزنين
B- التعبير عن إصدار تنسيق الملف، تحسباً لتطوير نسخة جديدة بحقول إضافية مستقبلاً
C- تخزين رقم الصف الدراسي
D- لا فائدة عملية لهما وحذفهما لن يغيّر شيئاً

**الإجابة الصحيحة: B**
> الشرح: كما ذُكر صراحة في المحاضرة، هذان الحقلان يسمحان بالتمييز بين إصدارات مختلفة لتنسيق الملف نفسه، بما يضمن توافقية القراءة مستقبلاً عند إضافة حقول جديدة.

---

### السؤال 3 (صح أم خطأ)
بما أن `teacherName` ضمن `FileHeader` هو مصفوفة `char[100]`، فإنه يمكن إسناد قيمة من نوع `std::string` إليه مباشرة باستخدام عامل الإسناد `=` دون أي تحويل.

A- صح
B- خطأ

**الإجابة الصحيحة: B**
> الشرح: لا يمكن إسناد `std::string` مباشرة إلى `char[]`؛ لهذا استُخدم التابع `c_str()` لأخذ محتوى الـ `string` كمصفوفة `char`، ثم `strncpy` لنسخها إلى الحقل `teacherName`.

---

### السؤال 4
لماذا استخدمنا `std::strncpy` بدلاً من نسخ مباشر عند كتابة اسم المدرس في `header.teacherName`؟

A- لأن `strncpy` أسرع من عامل الإسناد بشكل عام
B- لأن `internalTeacherName` من نوع `string` بينما `teacherName` مصفوفة `char`، فيلزم تحويل الأول عبر `c_str()` ثم نسخه بأمان إلى الثاني
C- لأن `strncpy` يقوم بحذف الملف القديم قبل الكتابة
D- لا داعي لاستخدامه إطلاقاً في هذا السياق

**الإجابة الصحيحة: B**
> الشرح: هذا بالضبط ما وضّحته المحاضرة: نلجأ إلى `c_str()` لأخذ محتوى `string` كمصفوفة `char` ثم `strncpy` لنسخها إلى الحقل ضمن `FileHeader`.

---

### السؤال 5
ما سبب استخدام `sizeof(header.teacherName) - 1` كوسيط ثالث للتابع `strncpy`؟

A- لتوفير مكان لبايت النهاية `'\0'` وتجنّب تجاوز حدود المصفوفة (Buffer Overflow)
B- لأن التابع يتطلب دائماً طرح 1 من أي حجم بشكل قسري
C- لتسريع عملية النسخ
D- لتصغير حجم الملف الناتج

**الإجابة الصحيحة: A**
> الشرح: طرح 1 يضمن ترك خانة أخيرة فارغة داخل المصفوفة لوضع محرف النهاية `'\0'` فيها، بما يمنع تجاوز حجم المصفوفة المخصص (100 بايت).

---

### السؤال 6
في السطر `header.studentCount = static_cast<int>(students.size());` ما سبب استخدام `static_cast<int>` هنا؟

A- لأن `size()` تعيد قيمة من نوع غير علامي (`size_t`) ويجب قصرها بأمان إلى `int` لتخزينها في الحقل المطلوب
B- لأن `studentCount` من نوع `double` في الأصل
C- لتحويل قيمة فاصلة عائمة إلى نص
D- لا حاجة فعلية له إطلاقاً

**الإجابة الصحيحة: A**
> الشرح: `vector::size()` تعيد `size_t`، بينما `studentCount` معرّف كـ `int`، لذلك يلزم قصر (Cast) صريح لتفادي تحذيرات الترجمة وضمان توافق الأنواع.

---

### السؤال 7
ما الفرق بين `static_cast` و`reinterpret_cast` في سياق الشيفرة السابقة؟

A- لا يوجد أي فرق عملي بينهما
B- `static_cast` يُستخدم لتحويل الأنواع المرتبطة منطقياً (كالأعداد)، بينما `reinterpret_cast` يُستخدم لإعادة تفسير الذاكرة كسلسلة بايتات (`char*`) دون تغيير القيم الفعلية
C- `reinterpret_cast` أبطأ من `static_cast` ولهذا لا يُستخدم في كتابة الملفات
D- `static_cast` مخصص فقط للأصناف الموروثة (Inheritance)

**الإجابة الصحيحة: B**
> الشرح: كما ذُكر في المحاضرة، عمليتا القصر هاتان قابلتان للاستبدال بقصر مباشر مثل `(int)` أو `(char*)`، لكن الفرق المفاهيمي أن `static_cast` تحويل نوع منطقي، بينما `reinterpret_cast` إعادة تفسير للذاكرة الخام فقط.

---

### السؤال 8
`outFile.write(reinterpret_cast<const char*>(&header), sizeof(FileHeader));`
ماذا يمثل الوسيط الثاني `sizeof(FileHeader)` لتابع `write`؟

A- عدد السجلات المكتوبة
B- عدد البايتات التي سيتم كتابتها من الذاكرة ابتداءً من العنوان الممرَّر
C- رقم إصدار الملف
D- طول اسم المدرس فقط

**الإجابة الصحيحة: B**
> الشرح: التابع `write` يحتاج مؤشراً على بداية الذاكرة وعدد البايتات المراد كتابتها، وهنا `sizeof(FileHeader)` يحدد بالضبط حجم الترويسة كاملة بالبايت.

---

### السؤال 9
```cpp
if (header.studentCount > 0) {
    outFile.write(reinterpret_cast<const char*>(students.data()),
                   header.studentCount * sizeof(Student));
}
```
ما وظيفة التابع `data()` ضمن الصف `vector` في هذا السياق؟

A- يعيد عدد العناصر ضمن الشعاع
B- يعيد مؤشراً على أول عنصر في المصفوفة الداخلية للشعاع، بحيث يمكن التعامل معها ككتلة متجاورة من الذاكرة
C- يحذف عناصر الشعاع بعد الكتابة
D- ينسخ الشعاع بالكامل إلى شعاع جديد

**الإجابة الصحيحة: B**
> الشرح: كما وضّحت المحاضرة، `data()` يعيد محتوى الشعاع كمصفوفة (مؤشر على أول عنصر)، وهذا ما يسمح بكتابة كل السجلات دفعة واحدة، مع تمرير `حجم السجل * عدد السجلات` كوسيط ثانٍ.

---

### السؤال 10 (صح أم خطأ)
من الممكن استبدال الكتابة الدفعية الواحدة لكل الطلاب (باستخدام `data()`) بحلقة تكتب كل سجل طالب على حدة ضمن الملف، وكلا الأسلوبين صحيحان وينتجان نفس محتوى الملف.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
> الشرح: نصّت المحاضرة صراحة على وجود خيارين: إما الكتابة ضمن حلقة سجلاً سجلاً، أو الكتابة دفعة واحدة عبر `data()`؛ وكلاهما ينتج نفس المحتوى على الملف.

---

## القسم الثاني: أسئلة عامة (2 درجة لكل سؤال)

### السؤال 11
ضمن التابع `readFromFile`، ما الهدف من السطر:
`if (std::strncmp(header.magic, "STUD", 4) != 0) return false;`

A- التأكد من أن الملف المفتوح هو من النوع الصحيح المتوقع من قبل البرنامج
B- التأكد من أن الملف ممتلئ بالكامل
C- التحقق من عدد الطلاب المخزنين
D- التحقق من صحة اسم المدرس

**الإجابة الصحيحة: A**
> الشرح: هذا هو الاستخدام الأساسي لحقل `magic`؛ إن لم يطابق القيمة المتوقعة "STUD"، فهذا يعني أن الملف ليس من النوع الذي يتعامل معه برنامجنا، فيتوقف التابع فوراً.

---

### السؤال 12
بفرض أن قيمة `magic` ضمن الملف المقروء كانت `{'S','T','U','X'}` بدلاً من `{'S','T','U','D'}`، ماذا سيحدث عند استدعاء `readFromFile`؟

A- سيتم تحميل البيانات بشكل طبيعي دون أي مشكلة
B- سيُعيد التابع `false` فوراً ولن تُقرأ بيانات المدرس أو الطلاب من الملف
C- سيحدث خطأ في وقت الترجمة (Compile Error)
D- سيتجاهل البرنامج التحقق تلقائياً ويتابع القراءة

**الإجابة الصحيحة: B**
> الشرح: بما أن `strncmp` ستعيد قيمة غير صفرية (عدم تطابق)، سيتحقق الشرط ويُنفَّذ `return false;` مباشرة قبل قراءة أي بيانات إضافية.

---

### السؤال 13
ما الفائدة من استدعاء `students.clear()` قبل قراءة الطلاب الجدد ضمن `readFromFile`؟

A- لا فائدة له ويمكن حذفه دون أي أثر
B- للتخلص من أي بيانات سابقة موجودة داخل الشعاع قبل تعبئته بالبيانات المقروءة حديثاً من الملف
C- لتصغير حجم الملف على القرص
D- لإغلاق الملف المفتوح

**الإجابة الصحيحة: B**
> الشرح: إذا كان الكائن `StudentClass` يحمل بالفعل بيانات طلاب سابقة (من عملية سابقة)، فإن `clear()` يفرغ الشعاع لضمان أن البيانات الجديدة المقروءة من الملف لا تُضاف فوق بيانات قديمة.

---

### السؤال 14
ما وظيفة التابع `resize(header.studentCount)` على الشعاع `students` قبل عملية `inFile.read(...)`؟

A- تحرير الذاكرة المستخدمة من قبل الشعاع
B- تهيئة عدد كافٍ من الخانات داخل الشعاع مسبقاً، بحيث يمكن قراءة البيانات مباشرة إلى تلك الذاكرة المحجوزة
C- طباعة عدد الطلاب على الشاشة
D- تغيير نوع بيانات عناصر الشعاع

**الإجابة الصحيحة: B**
> الشرح: كما ذُكر في المحاضرة، `resize` ضمن `vector` يهيئ المصفوفة الداخلية للتخزين بعدد الخانات المطلوب، وهذا شرط ضروري قبل استخدام `data()` كوجهة للقراءة.

---

### السؤال 15 (صح أم خطأ)
لو لم نستخدم `resize` قبل استدعاء `inFile.read(reinterpret_cast<char*>(students.data()), ...)` فإن البرنامج سيحاول الكتابة في ذاكرة غير مخصصة أصلاً، وهذا سلوك غير معرّف (Undefined Behavior).

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
> الشرح: بدون `resize` يكون الشعاع فارغاً (`data()` قد تعيد مؤشراً غير صالح لخانات لا وجود لها)، فتؤدي محاولة الكتابة المباشرة عبر `read` إلى سلوك غير معرّف وربما تعطّل البرنامج.

---

### السؤال 16
السطر: `internalTeacherName = header.teacherName;` يمثّل:

A- إسناد `char[]` إلى `char[]` مباشرة
B- إسناد ضمني من `char*` (الناتج عن اضمحلال المصفوفة) إلى `std::string` عبر الباني المناسب للصف `string`
C- خطأ ترجمة بسبب عدم توافق الأنواع
D- عملية نسخ ثنائية (bitwise) للذاكرة فقط

**الإجابة الصحيحة: B**
> الشرح: `std::string` يمتلك باني (Constructor) يقبل `const char*`، لذلك عند إسناد مصفوفة `char` (والتي تضمحل تلقائياً إلى مؤشر) إلى متحول `string`، يتم بناء الكائن الجديد ضمنياً من تلك السلسلة.

---

### السؤال 17 (ناتج تنفيذ)
بفرض الكود التالي ضمن `main`:
```cpp
StudentClass sc;
sc.setTeacher("Ahmad");
sc.writeToFile("data.bin");

StudentClass sc2;
sc2.readFromFile("data.bin");
```
ما القيمة التي سيحملها الحقل الداخلي لاسم المدرس في `sc2` بعد تنفيذ `readFromFile` بنجاح؟

A- "Unassigned"
B- "Ahmad"
C- قيمة فارغة تماماً
D- خطأ تنفيذ (Runtime Error)

**الإجابة الصحيحة: B**
> الشرح: `writeToFile` كتبت اسم المدرس "Ahmad" ضمن ترويسة الملف، و`readFromFile` تقرأ هذه الترويسة وتُسند القيمة المقروءة إلى `internalTeacherName`، لذلك ستحمل "Ahmad" وليس القيمة الافتراضية.

---

### السؤال 18
لماذا عُرّف الباني الافتراضي للصف `StudentClass` بالشكل: `StudentClass() : internalTeacherName("Unassigned") {}`؟

A- لضمان أن يمتلك الحقل `internalTeacherName` قيمة ابتدائية منطقية قبل استدعاء `setTeacher` أو `readFromFile`
B- لأن `std::string` يجب أن يُهيّأ إجبارياً بهذا الشكل فقط ولا بديل آخر
C- لتسريع تنفيذ البرنامج
D- لا فائدة عملية حقيقية لذلك

**الإجابة الصحيحة: A**
> الشرح: تهيئة الحقل بقيمة افتراضية واضحة ("Unassigned") تمنع الحصول على حقل فارغ أو غير معرّف في حال استُخدم الكائن قبل استدعاء `setTeacher` أو قراءة ملف فعلي.

---

### السؤال 19 (صح أم خطأ)
يمكن الاستعاضة عن نمط الفتح `std::ios::binary | std::ios::trunc` عند الكتابة بالنمط `std::ios::binary | std::ios::app` للحصول على نفس النتيجة تماماً (استبدال محتوى الملف بالكامل في كل مرة).

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
> الشرح: `trunc` يمسح محتوى الملف القديم بالكامل قبل الكتابة، بينما `app` يضيف البيانات الجديدة إلى نهاية الملف دون حذف المحتوى القديم؛ لذلك النمطان يعطيان نتيجتين مختلفتين تماماً.

---

### السؤال 20
عند الرغبة بالوصول العشوائي مباشرة إلى سجل طالب رقمه `i` (يبدأ العد من 0) ضمن الملف الثنائي دون قراءة الملف بالكامل، فإن الإزاحة الصحيحة عن بداية الملف هي:

A- `i * sizeof(Student)`
B- `sizeof(FileHeader) + i * sizeof(Student)`
C- `sizeof(FileHeader) * i`
D- `(i - 1) * sizeof(Student)`

**الإجابة الصحيحة: B**
> الشرح: كما ذُكر في ملاحظة المحاضرة الأخيرة، يجب أن نتنقل بمقدار حجم الترويسة (لتخطيها) بالإضافة إلى حجم السجل مضروباً برقمه، أي `sizeof(FileHeader) + i * sizeof(Student)`.

---

### السؤال 21 (صح أم خطأ)
من الممكن استبدال `reinterpret_cast<const char*>` بقصر مباشر بالشكل `(const char*)` في هذا السياق، ويعطي نفس الأثر عملياً.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
> الشرح: نصّت المحاضرة صراحة على أن عمليتي `static_cast` و`reinterpret_cast` يمكن استبدالهما بقصر مباشر مثل `(int)` أو `(char*)`.

---

### السؤال 22
لماذا لا يمكن كتابة كائن كامل من نوع `StudentClass` مباشرة عبر `outFile.write(reinterpret_cast<const char*>(&sc), sizeof(StudentClass));` بدلاً من الأسلوب المتّبع في المحاضرة (كتابة `FileHeader` ثم `Student` كل على حدة)؟

A- لأن `StudentClass` يحتوي على `std::string` و`vector`، وهما نوعان يديران الذاكرة ديناميكياً عبر مؤشرات داخلية؛ كتابتهما كبايتات خام سينسخ قيم المؤشرات فقط وليس المحتوى الفعلي، مما يجعل الملف غير قابل للاستخدام لاحقاً
B- لأن حجم `StudentClass` أكبر من أن يُكتب دفعة واحدة تقنياً
C- لا مشكلة إطلاقاً، ويمكن القيام بذلك بأمان بدل الطريقة المتبعة
D- لأن `StudentClass` لا يحتوي أي بيانات فعلية

**الإجابة الصحيحة: A**
> الشرح: هذا هو السبب الجوهري لتصميم بنيتين منفصلتين (`FileHeader` و`Student`) بحقول من الأنواع الأساسية (char, int, double) بدلاً من كتابة الصف نفسه؛ فالأنواع مثل `string` و`vector` تحتوي مؤشرات إلى ذاكرة خارجية (heap)، ونسخها كبايتات خام لا ينسخ محتوى تلك الذاكرة.

---

### السؤال 23
العنوان الوارد في تعليق الشيفرة `// --- 1. FULLY ENCAPSULATED FILE HEADER ---` يشير إلى:

A- أن الترويسة تحتوي كل المعلومات الوصفية اللازمة لفهم وتفسير محتوى الملف لاحقاً دون الحاجة لمصدر خارجي
B- أن الصف `FileHeader` يستخدم محدد الوصول `private` فقط لجميع حقوله
C- أنه لا يمكن تعديل الترويسة بعد كتابتها أبداً
D- أن الترويسة مشفّرة بالكامل

**الإجابة الصحيحة: A**
> الشرح: تجميع `magic` والإصدار واسم المدرس وعدد الطلاب ضمن ترويسة واحدة في بداية الملف يجعل الملف "مكتفياً ذاتياً" من ناحية المعلومات الوصفية اللازمة لقراءته وفهمه لاحقاً.

---

### السؤال 24
بفرض أردنا لاحقاً إضافة حقل جديد مثل `schoolName` إلى `FileHeader`، ما الفائدة العملية الحقيقية لوجود الحقلين `majorVersion` و`minorVersion` في هذه الحالة؟

A- تسمحان للبرنامج بالتمييز بين إصدارات الملف المختلفة، والتعامل مع كل إصدار بالطريقة المناسبة له (توافقية عكسية)
B- لا فائدة عملية إطلاقاً حتى في هذه الحالة
C- تُستخدمان فقط للزخرفة داخل الترويسة
D- تحلّان محل الحقل `magic` بالكامل

**الإجابة الصحيحة: A**
> الشرح: عند تطوير نسخة جديدة من تنسيق الملف بحقول إضافية، يستطيع البرنامج قراءة رقم الإصدار أولاً واتخاذ القرار المناسب (قراءة الحقول الإضافية أو تجاهلها) بناءً عليه، بدل أن يفشل بشكل كامل عند مصادفة ملف بإصدار مختلف.

---

### السؤال 25
عند إضافة طالب جديد عبر التابع `void addStudent(const Student& student)`، لماذا مُرِّر الوسيط بالمرجع الثابت `const Student&` بدلاً من تمريره بالقيمة `Student student`؟

A- لتجنّب نسخ البنية بالكامل عند كل استدعاء (توفير في الأداء)، مع منع تعديل الكائن الممرَّر داخل التابع بفضل `const`
B- لأنه غير ممكن تمرير بنية (struct) بالقيمة في ++C إطلاقاً
C- للسماح بتعديل قيمة الكائن الأصلي في مكان الاستدعاء
D- لا يوجد سبب حقيقي، هو مجرد أسلوب اعتباطي في الكتابة

**الإجابة الصحيحة: A**
> الشرح: تمرير بالمرجع الثابت أسلوب شائع لتفادي كلفة نسخ الكائن كاملاً في كل استدعاء (خصوصاً إذا كانت البنية كبيرة)، مع الحفاظ على أمان الكود عبر `const` الذي يمنع أي تعديل غير مقصود على الكائن الأصلي.

---

## ملخص المفاهيم التي غطتها هذه الأسئلة

- الغاية من حقل `magic` وآلية التحقق منه عبر `strncmp`
- دور حقلي `majorVersion` و`minorVersion` في التوافقية المستقبلية
- الفرق بين `std::string` و`char[]` وكيفية التحويل بينهما (`c_str`, `strncpy`)
- استخدام `static_cast` و`reinterpret_cast` وإمكانية استبدالهما بقصر مباشر
- آلية عمل `ofstream::write` و`ifstream::read` مع الوسيطين (المؤشر، عدد البايتات)
- دور `vector::data()`, `vector::size()`, `vector::resize()`, `vector::clear()`, `vector::push_back()`
- أنماط فتح الملفات: `ios::binary`, `ios::trunc`, `ios::app`
- خطورة كتابة كائنات تحتوي `string`/`vector` مباشرة كبايتات خام إلى ملف ثنائي
- حساب الإزاحة (Offset) للوصول العشوائي إلى سجل معيّن ضمن ملف يحتوي ترويسة + سجلات ثابتة الحجم
- أهمية تهيئة الحقول (Constructor initialization list) بقيم افتراضية منطقية

---

**انتهت الأسئلة**
