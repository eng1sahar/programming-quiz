# أسئلة مؤتمتة — المحاضرة 3: المؤشرات (الجزء الثالث والأخير)
**بنفس أسلوب ومستوى صعوبة أسئلة د. أسامه ناصر (نموذج امتحان برمجة 2)**

> ملاحظة عامة: افترض أن كل المكتبات المطلوبة قد تم إدراجها في البرنامج (`#include <iostream>`, `#include <string>`, `using namespace std;`) في كل الأسئلة التي تتضمن كودًا.

---

## القسم الأول: العلاقة مع الثوابت (Pointers & const)

### السؤال 1
ما هو الخطأ الذي يعطيه المترجم عند تنفيذ الكود التالي؟

```cpp
const int x = 10;
int *xPtr = &x;
```

A- لا يوجد أي خطأ، الكود صحيح
B- `invalid conversion from 'const int*' to 'int*'`
C- `undefined reference to x`
D- خطأ أثناء التنفيذ فقط (runtime error)

**الإجابة الصحيحة: B**
**الشرح:** لا يمكن تعريف مؤشر عادي (غير ثابت) يؤشر على بيانات ثابتة، لأن ذلك يسمح لاحقًا بتعديل قيمة ثابتة عبر المؤشر، وهذا يتعارض مع مفهوم `const`. المترجم يرفض تحويل `const int*` إلى `int*` تلقائيًا.

---

### السؤال 2
ما ناتج تنفيذ الكود التالي؟

```cpp
int x = 10;
int * const xPtr = &x;
xPtr++;
```

A- ينفذ بنجاح ويصبح `xPtr` يؤشر على العنصر التالي
B- خطأ أثناء الترجمة (compiler error)
C- خطأ أثناء التنفيذ (runtime error)
D- ينفذ بنجاح لكن يعطي قيمة عشوائية

**الإجابة الصحيحة: B**
**الشرح:** `xPtr` هو مؤشر ثابت (`* const`)، أي أن عنوانه لا يمكن تغييره بعد الإسناد الأولي. تنفيذ `xPtr++` يحاول تغيير العنوان الذي يخزنه المؤشر، وهذا مخالف لكونه ثابتًا، فيعطي خطأ ترجمة.

---

### السؤال 3
بفرض الكود التالي:

```cpp
int x = 10;
int * const xPtr = &x;
(*xPtr)++;
```

A- صح — الكود ينفذ بدون مشاكل
B- خطأ — لا يمكن تعديل القيمة المؤشر عليها

**الإجابة الصحيحة: A**
**الشرح:** المؤشر ثابت (لا يمكن تغيير العنوان) لكنه يؤشر على بيانات متغيرة، لذلك يمكن تعديل المحتوى الذي يؤشر عليه عبر `(*xPtr)++` بشكل سليم تمامًا.

---

### السؤال 4
ما ناتج تنفيذ الكود التالي؟

```cpp
int main(){
   const int * const xPtr = new int(5);
   cout << *xPtr;
   *xPtr = 10;
   cout << *xPtr;
   return 0;
}
```

A- `510`
B- `55`
C- خطأ أثناء الترجمة 
D- خطأ أثناء التنفيذ

**الإجابة الصحيحة: C**
**الشرح:** `xPtr` من نوع "مؤشر ثابت على بيانات ثابتة"، أي لا يمكن تغيير العنوان ولا يمكن تغيير المحتوى المؤشر عليه. السطر `*xPtr = 10;` يحاول تعديل قيمة ثابتة، فيعطي خطأ ترجمة قبل تنفيذ أي شيء.

---

### السؤال 5
أي تعريف من التعريفات التالية يمثل "مؤشرًا متغيرًا على بيانات ثابتة"؟

A- `int * const ptr;`
B- `const int * ptr;`
C- `const int * const ptr;`
D- `int * ptr;`

**الإجابة الصحيحة: B**
**الشرح:** عندما تسبق `const` نوع البيانات (`const int *`) فهذا يعني أن البيانات المؤشر عليها ثابتة (لا يمكن تعديل المحتوى)، بينما المؤشر نفسه (العنوان) يبقى قابلًا للتغيير لأن `const` لم تُطبّق مباشرة على المتحول `ptr`.

---

### السؤال 6
صل بين كل تعريف والوصف الصحيح له (اختر الوصف الصحيح لكل حالة):

```cpp
int x = 10;
int * xPtr1 = &x;              // (1)
int * const xPtr2 = &x;        // (2)
const int * xPtr3 = &x;        // (3)
const int * const xPtr4 = &x;  // (4)
```

أيّ من الأسطر التالية يعبّر بشكل **صحيح** عن الحالة (2)؟

A- يمكن تغيير العنوان والمحتوى معًا
B- لا يمكن تغيير العنوان لكن يمكن تغيير المحتوى
C- يمكن تغيير العنوان لكن لا يمكن تغيير المحتوى
D- لا يمكن تغيير العنوان أو المحتوى

**الإجابة الصحيحة: B**
**الشرح:** الحالة (2) هي "مؤشر ثابت على بيانات متغيرة" (`* const` مباشرة بعد اسم النوع وقبل اسم المؤشر)، لذلك العنوان مثبت عند التصريح، لكن يمكن تعديل القيمة المخزنة في المتحول `x` من خلال المؤشر.

---

### السؤال 7
لماذا لا يمكن تعريف مؤشر يؤشر على ثابت باستخدام قيمة رقمية مباشرة (بدون متحول)، مثل محاولة جعل مؤشر يؤشر على القيمة `10` مباشرة؟

A- لأن المؤشرات لا تدعم الأعداد الصحيحة أساسًا
B- لأن المؤشر يتطلب عنوان متحول موجود في الذاكرة، والقيمة الرقمية المباشرة لا تملك عنوانًا يمكن الإشارة إليه
C- لأن الكلمة `const` تمنع استخدام المؤشرات نهائيًا
D- لا يوجد سبب، هذا ممكن فعليًا

**الإجابة الصحيحة: B**
**الشرح:** المؤشر يخزّن عنوان متحول في الذاكرة، والقيم الثابتة الرقمية (Literals) لا تُخصَّص لها بالضرورة عناوين ذاكرة يمكن التعامل معها بنفس طريقة المتحولات، لذلك لا يمكن ربط مؤشر مباشرة بقيمة رقمية.

---

### السؤال 8
أراد مبرمج تمرير مصفوفة إلى تابع `print` بحيث يستطيع التابع قراءة عناصرها والتنقل ضمنها **دون** أن يستطيع تعديل أي عنصر فيها. أي تعريف للتابع هو الأنسب؟

A- `void print(int * arr, int len);`
B- `void print(const int * arr, int len);`
C- `void print(int * const arr, int len);`
D- `void print(const int * const arr, int len);`

**الإجابة الصحيحة: B**
**الشرح:** المطلوب هو مؤشر يستطيع التنقل بحرية (تغيير العنوان أثناء السير على المصفوفة إن أراد المبرمج ذلك ضمن التابع) لكن لا يستطيع تعديل محتوى العناصر، وهذا بالضبط ما يوفره "مؤشر متغير على بيانات ثابتة" `const int *`.

---

## القسم الثاني: العلاقة مع التوابع (Function Pointers)

### السؤال 9
أي تعريف صحيح لمؤشر اسمه `f` يؤشر على تابع لا يُعيد قيمة (`void`) ويقبل وسيطًا واحدًا من نوع `int`؟

A- `void f(*int);`
B- `void (*f)(int);`
C- `void *f(int);`
D- `(void*) f(int);`

**الإجابة الصحيحة: B**
**الشرح:** الصيغة الصحيحة لتعريف مؤشر على تابع تكون بوضع اسم المؤشر بين قوسين مسبوقًا بنجمة `(*f)`، ثم نوع القيمة المعادة قبله، ولائحة أنواع الوسطاء بعده: `void (*f)(int)`.

---

### السؤال 10
بفرض التابع التالي:

```cpp
void bubbleSort(int * data, int len, bool (*compare)(int, int)){
   for(int i = 0; i < len-1; i++)
      for(int j = 0; j < len-i-1; j++)
         if(compare(data[j], data[j+1]))
            swap(&data[j], &data[j+1]);
}

bool ascending(int a, int b){ return a > b; }
bool descending(int a, int b){ return a < b; }
```

عند تنفيذ الاستدعاء `bubbleSort(a, 9, descending);` فإن النتيجة تكون:

A- ترتيب المصفوفة تصاعديًا
B- ترتيب المصفوفة تنازليًا
C- خطأ ترجمة لأن `descending` ليس مؤشرًا
D- لا يحدث أي ترتيب

**الإجابة الصحيحة: B**
**الشرح:** اسم التابع `descending` عند تمريره كوسيط يتحول تلقائيًا إلى مؤشر يؤشر عليه (لا حاجة لكتابة `&descending`). التابع `descending` يعيد `true` عندما يكون العنصر الأول أصغر، مما يؤدي لتبديل العناصر في هذه الحالة فينتج ترتيب تنازلي.

---

### السؤال 11
عند استدعاء التابع الذي يؤشر عليه مؤشر التابع `compare` ضمن `bubbleSort`، أي من الصيغ التالية **صحيحة**؟

A- `compare(a, b)` فقط
B- `(*compare)(a, b)` فقط
C- كلا الصيغتين `compare(a, b)` و `(*compare)(a, b)` صحيحتان
D- لا يمكن استدعاء التابع من خلال المؤشر مباشرة، يجب استخدام تابع وسيط

**الإجابة الصحيحة: C**
**الشرح:** في ++C، يمكن استدعاء التابع الذي يؤشر عليه مؤشر تابع بكتابة اسم المؤشر متبوعًا بالوسطاء مباشرة، أو بكتابة `(*اسم_المؤشر)` متبوعة بالوسطاء؛ كلتا الصيغتين متكافئتان دلاليًا.

---

### السؤال 12
هل يمكن تمرير مؤشر تابع كوسيط (Parameter) لتابع آخر في ++C؟

A- نعم 
B- لا

**الإجابة الصحيحة: A**
**الشرح:** مؤشرات التوابع تُعامل كمتحولات عادية من ناحية التمرير، لذلك يمكن تمريرها كوسطاء لتوابع أخرى، وهذا بالضبط ما يحدث في مثال `bubbleSort` حيث يُمرَّر `compare` كوسيط ثالث.

---

## القسم الثالث: سلاسل النصوص (Strings)

### السؤال 13
ما ناتج تنفيذ الكود التالي؟

```cpp
char a[] = {'h','i','\0'};
cout << sizeof(a) / sizeof(char);
```

A- 2 
B- 3
C- 4
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** مصفوفة المحارف تحتوي فعليًا على 3 عناصر: `'h'`، `'i'`، والمحرف الصفري `'\0'` الذي يُستخدم لتحديد نهاية السلسلة النصية، لذلك حجم المصفوفة (عدد عناصرها) هو 3.

---

### السؤال 14
أي مما يلي هو السبب الرئيسي لاستخدام المحرف الصفري (`'\0'`) في نهاية مصفوفات المحارف؟

A- لتسريع عملية الطباعة فقط
B- لتحديد نهاية السلسلة النصية وحساب طولها دون الحاجة لتخزين الطول بشكل منفصل
C- لأن المترجم يفرضه إجباريًا على كل مصفوفة
D- لا يوجد له أي وظيفة عملية

**الإجابة الصحيحة: B**
**الشرح:** بما أن مصفوفات المحارف التقليدية لا تخزّن طولها بشكل مباشر، يُستخدم المحرف الخاص `'\0'` كعلامة نهاية، بحيث يمكن حساب الطول أو معرفة أين تنتهي السلسلة بالمرور على العناصر حتى الوصول لهذا المحرف.

---

### السؤال 15
ما ناتج تنفيذ الكود التالي؟

```cpp
string c1 = "hello world";
cout << c1.substr(6, 5);
```

A- `world`
B- `hello`
C- ` worl`
D- خطأ تنفيذ

**الإجابة الصحيحة: A**
**الشرح:** التابع `substr(start, length)` يعيد سلسلة جزئية طولها `length` بدءًا من الفهرس `start`. الفهرس 6 يقابل الحرف `w` (بعد `hello ` أي 6 محارف من الفهرس 0 إلى 5)، وبطول 5 محارف تكون النتيجة `world`.

---

### السؤال 16
بفرض الكود التالي:

```cpp
string str = "code";
cout << str.front() << str.back() << str.at(1);
```

ما هو ناتج الطباعة؟

A- `coo`
B- `ceo`
C- `ced`
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** `front()` يعيد أول حرف وهو `c`، و`back()` يعيد آخر حرف وهو `e`، و`at(1)` يعيد الحرف عند الفهرس 1 وهو `o`. الناتج بالترتيب: `c` ثم `e` ثم `o` أي `ceo`.

---

### السؤال 17
هل يمكن اعتبار الصف `string` مصفوفة عادية عند استخدام مؤشر يؤشر على متحول من نوع `string`؟

A- نعم، يتصرف المؤشر تمامًا كما مع مصفوفة المحارف
B- لا، لأن `string` غرض (Object) وليس مصفوفة، والتعامل معه عبر المؤشر يختلف عن التعامل مع المصفوفات العادية

**الإجابة الصحيحة: B**
**الشرح:** الصف `string` هو نمط بيانات مجرّد (Class) يغلّف البيانات والتوابع ضمن كتلة واحدة، فعند استخدام مؤشر معه يُعامَل المؤشر كمؤشر على غرض (Object) وليس كمؤشر على مصفوفة محارف تقليدية.

---

### السؤال 18
ما ناتج تنفيذ الكود التالي؟

```cpp
string str = "abc";
str += "de";
cout << str.length();
```

A- 3
B- 5
C- 2 
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** المُعامل `+=` مع الصف `string` يقوم بإلحاق (concatenation) السلسلة الثانية إلى نهاية الأولى، فتصبح `str` تساوي `"abcde"`، وطولها 5 محارف، وهو ما يعيده التابع `length()`.

---

### السؤال 19
ما ناتج تنفيذ الكود التالي؟

```cpp
string s1 = "Cat";
string s2 = "cat";
cout << (s1 == s2);
```

A- `1`
B- `0`
C- `true`
D- خطأ ترجمة

**الإجابة الصحيحة: B**
**الشرح:** عملية المقارنة `==` بين متحولات من نوع `string` تراعي حالة الأحرف (Case-Sensitive)، أي أن `A` تختلف عن `a`. بما أن `s1` و `s2` مختلفتان في حالة الحرف الأول، تكون نتيجة المقارنة `false` والتي تُطبع كـ `0`.

---

### السؤال 20
لتضمين إمكانية استخدام الصف `string` ضمن برنامج ++C، يجب إضافة أي سطر إلى بداية الكود؟

A- `#include <cstring>`
B- `#include <char>`
C- `#include <string>`
D- لا حاجة لأي إضافة، الصف `string` مدمج تلقائيًا

**الإجابة الصحيحة: C**
**الشرح:** الصف `string` معرّف ضمن المكتبة القياسية `<string>`، ويجب تضمينها صراحة (`#include <string>`) لاستخدام هذا الصف والتوابع المرتبطة به مثل `length()`، `substr()`، `at()`، وغيرها.

---

## جدول الإجابات السريع لما سبق

| رقم السؤال | الإجابة | رقم السؤال | الإجابة |
|:---:|:---:|:---:|:---:|
| 1 | B | 11 | C |
| 2 | B | 12 | A |
| 3 | A | 13 | B |
| 4 | C | 14 | B |
| 5 | B | 15 | A |
| 6 | B | 16 | B |
| 7 | B | 17 | B |
| 8 | B | 18 | B |
| 9 | B | 19 | B |
| 10 | B | 20 | C |

---

*تم إعداد هذه الأسئلة اعتمادًا على محتوى المحاضرة الثالثة (المؤشرات - الجزء الثالث والأخير، د. أسامه ناصر، 2025-2026)، مع محاكاة أسلوب وصياغة ومستوى صعوبة نموذج امتحان البرمجة 2 (النموذج A).*
