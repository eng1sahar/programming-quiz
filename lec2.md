# أسئلة مؤتمتة – المحاضرة الثانية: المؤشرات (الجزء الثاني)
### إعداد بأسلوب مشابه لأسلوب د. أسامه ناصر / د. محسن حسين في نموذج الامتحان
**المقرر:** برمجة 2 | **السنة الأولى – الفصل الأول**

> **ملاحظة:** جميع الأسئلة اختيار من متعدد (A/B/C/D) ما لم يُذكر خلاف ذلك، وتغطي كامل مفاهيم المحاضرة:
> المصفوفات كمؤشرات، حساب عناوين المؤشرات، مخاطر تجاوز حدود المصفوفة، الحجز الديناميكي للذاكرة (new/delete)،
> تسرب الذاكرة (Memory Leak)، المؤشرات الذكية (unique_ptr وshared_ptr)، ومقارنة C++ مع لغات أخرى.

---

## القسم الأول: المصفوفات والمؤشرات (الأسئلة 1 – 10) — 3 درجات لكل سؤال

### السؤال 1
بفرض لدينا الكود التالي:
```cpp
int main(){
    int a[5];
    int *aPtr=a;
    cout<<(a==aPtr)<<endl;
}
```
ما هو خرج البرنامج؟

A- 0
B- 1
C- خطأ ترجمة (Compile Error)
D- قيمة عشوائية

**الإجابة الصحيحة: B**
**الشرح:** في ++C يتم التعامل مع اسم المصفوفة على أنه مؤشر يحمل عنوان أول عنصر فيها (`&a[0]`)، وبما أن `aPtr=a` فإنّ كلا المؤشرين يشيران لنفس العنوان، وبالتالي عملية المقارنة تُعيد `true` أي `1`.

---

### السؤال 2
ما هو خرج البرنامج التالي؟
```cpp
int main(){
    int a[5]={10,20,30,40,50};
    cout<<*(a+2)<<endl;
}
```

A- 10
B- 20
C- 30
D- خطأ تنفيذ

**الإجابة الصحيحة: C**
**الشرح:** التعبير `*(a+i)` مكافئ تمامًا لـ `a[i]`، وبما أن `i=2` فإن الناتج هو العنصر الثالث في المصفوفة وقيمته `30`.

---

### السؤال 3
بفرض أن لدينا مصفوفة `int a[5]` وأن عنوان `a[0]` هو `0x1000`، فإن عنوان `a[1]` (على نظام تكون فيه `int` بحجم 4 بايت) هو:

A- `0x1001` لأن المؤشر يتنقل بايت بايت دائمًا
B- `0x1004` لأن المؤشر يتنقل خانة خانة (بحجم النمط) بغض النظر عن حجم الخانة
C- يعتمد فقط على نوع المصفوفة إذا كانت ساكنة أو ديناميكية
D- لا يمكن تحديد ذلك إلا أثناء التنفيذ

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، عملية جمع قيمة على المؤشر لا تُسبب الانتقال بايت بايت، بل خانة خانة بغض النظر عن حجم الخانة، أي أن `aPtr+1` تعني الانتقال بمقدار `sizeof(نوع العنصر)` بايت.

---

### السؤال 4
لماذا لا يتم تمرير المصفوفة إلى تابع في ++C بالقيمة (by value)؟

A- لأن المصفوفات لا تُخزَّن في الذاكرة أصلًا
B- لأن ++C تمنع تمرير المصفوفات نهائيًا
C- لأنه يتم تمرير عنوان أول عنصر فيها (تُعامل كمؤشر)
D- لأن المصفوفات تُنسخ دائمًا بشكل تلقائي بغض النظر عن الحجم

**الإجابة الصحيحة: C**
**الشرح:** بما أن ++C تتعامل مع المصفوفة مثل المؤشر (والمؤشر مثل المصفوفة)، فإن تمرير المصفوفة لتابع ما يُمرر فعليًا عنوان أول عنصر فيها وليس نسخة كاملة من كل عناصرها.

---

### السؤال 5
هل الكود التالي صحيح ويُعطي نفس النتيجة على أي مترجم (Compiler)؟
```cpp
int main(){
    int x=10;
    int z=50;
    int *y=&x;
    y++;
    cout<<*y<<endl;
}
```

A- نعم، دائمًا يطبع 50 على كل الأنظمة
B- لا، السلوك غير مضمون (Undefined Behavior) والنتيجة تعتمد على طريقة ترتيب المتحولات في الذاكرة من قبل المترجم
C- خطأ ترجمة لأن `y++` غير مسموحة على المؤشرات
D- يطبع دائمًا 10

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، تحريك المؤشر خارج حدود المصفوفة/المتحول الذي يشير إليه هو سلوك غير معرّف رسميًا؛ فقد يعطي القيمة `50` على G++ لأن المتحولات تُوضع بشكل متسلسل في قسم البيانات، بينما قد يعطي قيمة عشوائية على مترجمات أخرى (مثل CXXDroid) بسبب اختلاف ترتيب الحجز في الذاكرة.

---

### السؤال 6
ما هو المخطر الأمني (Security Risk) الأشهر المرتبط بإمكانية تجاوز مؤشر لحدود مصفوفة، والمذكور في المحاضرة؟

A- ثغرة Heartbleed ضمن مكتبة OpenSSL
B- ثغرة Meltdown في المعالجات
C- هجوم SQL Injection
D- هجوم DDoS

**الإجابة الصحيحة: A**
**الشرح:** ذُكرت في المحاضرة ثغرة Heartbleed ضمن مكتبة OpenSSL، والتي هددت الخوادم ومواقع الإنترنت حول العالم بسبب عدم التحقق من حجم مصفوفة أثناء التنقل عليها بالاعتماد على مؤشر.

---

### السؤال 7
عند محاولة الوصول إلى عنصر خارج حدود مصفوفة باستخدام مؤشر، ماذا يحدث في لغة ++C مقارنة بلغة Java؟

A- كلتا اللغتين تطلقان استثناء (Exception) تلقائيًا
B- في Java يحدث الخطأ IndexOutOfBoundException، بينما في ++C لا يُطلق أي خطأ ويتم الوصول لحجرة ذاكرة تالية قد تكون عشوائية أو متحولًا آخر
C- في ++C يحدث خطأ ترجمة قبل التنفيذ أصلًا
D- كلتا اللغتين تتجاهلان الخطأ دون أي عواقب

**الإجابة الصحيحة: B**
**الشرح:** لغة Java تتحقق من حدود المصفوفة أثناء التنفيذ وتطلق `IndexOutOfBoundException` عند التجاوز، أما ++C فلا تقوم بأي تحقق وتسمح بالوصول للحجرة التالية في الذاكرة، وهذا هو مصدر الخطورة الأمنية.

---

### السؤال 8
بفرض المؤشر `int *b=a;` حيث `a` مصفوفة، هل يمكن التعامل مع `b` كأنه مصفوفة عبر الكتابة `b[i]`؟

A- نعم، لأن ++C تسمح بالتعامل مع المؤشر كأنه مصفوفة، تمامًا كما تسمح بالتعامل مع المصفوفة كمؤشر
B- لا، هذا خطأ ترجمة لأن `b` مؤشر وليس مصفوفة
C- يعمل فقط إذا كانت المصفوفة الأصلية ديناميكية
D- يعمل فقط مع الأنواع الأولية (int, char) وليس مع الأصناف (class)

**الإجابة الصحيحة: A**
**الشرح:** كما ورد في المحاضرة، يمكنك أيضًا التعامل مع المؤشر كأنه مصفوفة باستخدام `b[i]`، وهذا يعادل `*(b+i)`.

---

### السؤال 9
ما هو خرج البرنامج التالي؟
```cpp
int main(){
    int a[5]={1,2,3,4,5};
    int *p=a;
    for(int i=0;i<5;i++)
        cout<<*(p+i)<<" ";
}
```

A- 1 2 3 4 5
B- عناوين ذاكرة مختلفة
C- خطأ تنفيذ
D- 5 4 3 2 1

**الإجابة الصحيحة: A**
**الشرح:** التعبير `*(p+i)` يطبع قيمة كل عنصر من عناصر المصفوفة بالترتيب، تمامًا كما لو كتبنا `p[i]` أو `a[i]`.

---

### السؤال 10
لماذا تُعتبر المؤشرات "أحد أكبر المخاطر" التي ذُكرت في المحاضرة فيما يخص التعامل مع البيانات؟

A- لأنها بطيئة جدًا مقارنة بالمصفوفات الساكنة
B- لأنها تُتيح إمكانية الوصول لأي مكوّن من البيانات أو الكود (في حال كان المؤشر على تابع)، مما قد يؤدي للوصول لمتحولات خارج نطاق الاستخدام
C- لأن ++C تمنع استخدامها في أغلب الحالات
D- لأنها تتطلب دائمًا حجزًا ديناميكيًا للذاكرة

**الإجابة الصحيحة: B**
**الشرح:** كما ذُكر في المحاضرة، الخطورة تأتي من إمكانية الوصول لأي جزء من البيانات (أو حتى كود تابع في حال كان المؤشر عليه)، وبالتالي إمكانية الوصول لمتحولات خارج نطاق الاستخدام أو تجاوز حجم مصفوفة.

---

## القسم الثاني: الحجز الديناميكي للذاكرة (الأسئلة 11 – 22) — درجتان لكل سؤال

### السؤال 11
ما الفرق الأساسي بين إنشاء مصفوفة بالشكل `int a[n]={0};` وبين `int *a=new int[n];`؟

A- لا يوجد أي فرق بينهما إطلاقًا
B- الأولى مسموحة فقط في الإصدارات الجديدة من ++C وتُنشئ المصفوفة على المكدس (Stack)، بينما الثانية (باستخدام new) هي الحجز الديناميكي التقليدي على الكومة (Heap) والمتاح في كل الإصدارات
C- الثانية أسرع دائمًا من الأولى
D- الأولى فقط هي التي تحتاج تحرير الذاكرة بـ delete

**الإجابة الصحيحة: B**
**الشرح:** ورد في المحاضرة أن `int a[n]` بحجم يحدده المستخدم متاحة في الإصدارات الجديدة من ++C، أما `int *a=new int[n]` فهي الطريقة التقليدية (في الإصدارات القديمة) للحجز الديناميكي بالاعتماد على المؤشرات والكلمة المفتاحية `new`، وهي التي ركزت عليها المحاضرة.

---

### السؤال 12
بفرض الكود التالي:
```cpp
int main(){
    int n; cin>>n;
    int *a=new int[n];
    for(int i=0;i<n;i++) cin>>a[i];
    for(int i=0;i<n;i++) cout<<a[i];
    return 0;
}
```
هل هذا الكود يحرر الذاكرة المحجوزة قبل انتهاء البرنامج؟

A- نعم، تلقائيًا عند `return 0`
B- لا، ينقصه استدعاء `delete[] a;` قبل الخروج من `main` لتجنب تسرب الذاكرة
C- لا حاجة لتحرير الذاكرة لأن المصفوفة من نوع `int`
D- نعم لأن الحلقات (for) تُحرر الذاكرة تلقائيًا بعد الاستخدام

**الإجابة الصحيحة: B**
**الشرح:** أي مؤشر تم إنشاؤه باستخدام `new[]` يجب تحريره باستخدام `delete[]` بعد الانتهاء من استخدامه؛ إغفال ذلك يُسبب ما يُعرف بـ Memory Leak (تسرب الذاكرة).

---

### السؤال 13
عند إنشاء مؤشر باستخدام `new int;` (لعنصر واحد)، ما هي الطريقة الصحيحة لتحريره؟

A- `delete[] ptr;`
B- `free(ptr);`
C- `delete ptr;`
D- `ptr=NULL;` فقط

**الإجابة الصحيحة: C**
**الشرح:** كما ورد في المحاضرة: عند إنشاء مؤشر باستخدام `new` يتم التخلص منه باستخدام `delete`، بينما عند إنشاء مؤشر باستخدام `new[]` (لمصفوفة) يتم التخلص منه باستخدام `delete[]`.

---

### السؤال 14
ما هو خرج البرنامج التالي (بغض النظر عن القيمة العشوائية الدقيقة، اختر الوصف الأصح لسلوك المؤشر)؟
```cpp
int main(){
    int* p1 = new int;
    *p1 = 100;
    delete p1;
    cout<<*p1<<endl;
}
```

A- يطبع دائمًا القيمة 100 لأن `delete` لا تغيّر محتوى الذاكرة
B- خطأ ترجمة
C- المؤشر p1 يصبح "متدليًا" (Dangling Pointer) بعد `delete`، والوصول لمحتواه سلوك غير معرّف وقد يطبع قيمة عشوائية
D- يطبع 0 دائمًا لأن `delete` تصفّر المحتوى

**الإجابة الصحيحة: C**
**الشرح:** كما وضّح المثال العملي في المحاضرة، بعد `delete p1` يبقى المؤشر `p1` يحمل نفس العنوان القديم لكن الحجرة لم تعد محجوزة للبرنامج، وبالتالي فإن طباعة `*p1` تُعطي قيمة عشوائية غير مضمونة (Undefined Behavior) وليس بالضرورة القيمة الأصلية أو صفرًا.

---

### السؤال 15
ما هو "تسرب الذاكرة" (Memory Leak)؟

A- خطأ ترجمة ينتج عن استخدام `new` بدون `delete`
B- خطأ برمجي ناتج عن عدم تحرير الذاكرة المحجوزة ديناميكيًا بعد الانتهاء من استخدامها، مما يُبقيها محجوزة للبرنامج دون فائدة
C- ظاهرة طبيعية تحدث تلقائيًا عند إغلاق البرنامج
D- خطأ ينتج فقط عند استخدام المؤشرات الذكية

**الإجابة الصحيحة: B**
**الشرح:** عندما ننتهي من استخدام مؤشر يشير إلى حجرة ذاكرية دون استدعاء `delete`، تبقى الذاكرة محجوزة للبرنامج رغم أنها غير مستخدمة فعليًا، وهذا هو تسرب الذاكرة (Memory Leak).

---

### السؤال 16
بفرض أن لديك مصفوفة مكوّنة من 1024 عنصر، كل عنصر بحجم 5MB، ولم تقم بتحرير الذاكرة بعد الانتهاء من استخدامها. ما هي المساحة التقريبية المهدورة من الـ RAM؟

A- 1024 KB
B- 5 GB
C- 1 GB
D- لا توجد أي مساحة مهدورة

**الإجابة الصحيحة: B**
**الشرح:** هذا المثال بالذات ورد حرفيًا في المحاضرة: 1024 عنصر × 5MB لكل عنصر ≈ 5GB من الذاكرة تبقى مهدورة (Wasted) في حال لم يتم تحرير الذاكرة.

---

### السؤال 17
أي اللغات التالية تعتمد على مفهوم "جامع القمامة" (Garbage Collector) لإدارة الذاكرة تلقائيًا، بحسب ما ورد في المحاضرة؟

A- ++C فقط
B- Rust فقط
C- Python، C#، Java، JavaScript
D- لغة التجميع (Assembly) فقط

**الإجابة الصحيحة: C**
**الشرح:** ذكرت المحاضرة أن هذه اللغات الأربع تعتمد على مفهوم جامع القمامة (Garbage Collector) الذي يتولى عملية إدارة الذاكرة بشكل كامل ويحرر حجرات الذاكرة غير المستخدمة أثناء التنفيذ.

---

### السؤال 18
ما هي الآلية التي تعتمدها لغة Rust لإدارة الذاكرة، والتي تختلف عن آلية جامع القمامة؟

A- الحجز اليدوي فقط دون أي رقابة
B- مفهوم الملكية والاستعارة (Ownership & Borrowing) يتم التحقق منه أثناء الترجمة (Compile Time)
C- نفس آلية ++C تمامًا دون أي تعديل
D- الاعتماد الكلي على جامع قمامة مشابه لجافا

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، طوّرت Rust آلية لإدارة الذاكرة تعتمد على مفهوم الملكية والاستعارة (Ownership & Borrowing)، ويتم التحقق من صحتها أثناء الترجمة (Compile Time) وليس أثناء التنفيذ.

---

### السؤال 19
لماذا لا تُعتبر لغة ++C "فاشلة" رغم عدم امتلاكها جامع قمامة تلقائي مثل Java أو Python (بحسب منطق المحاضرة)؟

A- لأنها أسرع في الكتابة فقط
B- لأنها لا تحتاج لتواجد بيئة تشغيل (Runtime Environment) لتعمل، فيمكن تشغيل أكوادها على العتاد مباشرة، وهذا يجعلها مناسبة لتطوير أنظمة التشغيل والمتحكمات المصغرة والبرامج التقليدية الكبرى
B- لأنها اللغة الوحيدة المستخدمة في الألعاب
C- لأن جميع اللغات الأخرى مبنية أصلًا بلغة بايثون
D- لأنها لا تدعم المؤشرات إطلاقًا

**الإجابة الصحيحة: A**
**الشرح:** خلافًا للغات مثل JavaScript أو #C أو Java أو Python التي تحتاج لبيئة تشغيل (متصفح، NodeJS، .Net، JVM) لتعمل، فإن ++C وC وRust لا تحتاج لتواجد بيئة، أي يمكن تشغيل أكوادها على العتاد مباشرة؛ لذلك تُستخدم في أنظمة التشغيل، المتحكمات المصغرة، وحتى في برامج ضخمة مثل Microsoft Office وChrome ومحرك الألعاب Unreal Engine.

---

### السؤال 20
أي مما يلي مثال صحيح على برنامج مكتوب أساسًا بلغة ++C كما ورد في المحاضرة؟

A- Mozilla Firefox (وأجزاء منه بـ Rust)
B- Microsoft Office
C- Google Chrome
D- كل ما سبق صحيح

**الإجابة الصحيحة: D**
**الشرح:** ذكرت المحاضرة الأمثلة الثلاثة معًا: Microsoft Office (++C)، Mozilla Firefox (++C + Rust)، Chrome (++C)، بالإضافة إلى نواة أندرويد (Linux) المكتوبة بـ ++C وRust.

---

### السؤال 21
ما هو ناتج تنفيذ البرنامج التالي؟
```cpp
int main(){
    int n=3;
    int *a=new int[n];
    for(int i=0;i<n;i++) a[i]=i*10;
    delete[] a;
    a=new int[n];
    for(int i=0;i<n;i++) cout<<a[i]<<" ";
}
```

A- 0 10 20 دائمًا
B- خطأ تنفيذ حتمًا
C- قيم غير محددة (Undefined) لأن `new int[n]` الجديدة لا تضمن تصفير المحتوى تلقائيًا
D- خطأ ترجمة لأنه لا يمكن إعادة استخدام مؤشر بعد `delete[]`

**الإجابة الصحيحة: C**
**الشرح:** بعد `delete[] a` تُحرر الذاكرة، وعند إعادة الحجز بـ `new int[n]` لا تضمن ++C أن المحتوى سيكون مصفرًا أو مطابقًا للقيم القديمة؛ القيم تعتمد على حالة الذاكرة عند الحجز وهي غير محددة ما لم تُهيّأ صراحة.

---

### السؤال 22
أي الجمل التالية صحيحة فيما يخص `delete` و `delete[]`؟

A- يمكن استخدام `delete` لتحرير مصفوفة تم إنشاؤها بـ `new[]` دون أي مشكلة
B- استخدام `delete` بدلًا من `delete[]` لتحرير مصفوفة سلوك غير معرّف وقد يُسبب مشاكل في الذاكرة
C- `delete[]` تُستخدم فقط مع الأصناف (class) وليس الأنواع الأولية
D- لا فرق إطلاقًا بين الاثنين في أي حالة

**الإجابة الصحيحة: B**
**الشرح:** يجب مطابقة أداة التحرير مع أداة الحجز: `new` مع `delete`، و`new[]` مع `delete[]`. استخدام `delete` لتحرير مصفوفة محجوزة بـ `new[]` هو سلوك غير معرّف رسميًا (Undefined Behavior) وقد يؤدي لتحرير جزئي أو تلف في الذاكرة.

---

## القسم الثالث: المؤشرات الذكية unique_ptr و shared_ptr (الأسئلة 23 – 32) — درجتان لكل سؤال

### السؤال 23
ما هو المفهوم الأساسي الذي يعتمده `unique_ptr`؟

A- الملكية المشتركة (Shared Ownership)
B- الملكية الحصرية (Exclusive Ownership) بحيث لا يمكن لنسختين امتلاك نفس الكائن في آن واحد
C- عداد المراجع (Reference Counting)
D- عدم امتلاك أي كائن إطلاقًا

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، `unique_ptr` يعتمد مفهوم الملكية الفردية/الحصرية، بحيث لا يمكن لنسختين من `unique_ptr` امتلاك نفس الكائن في نفس الوقت.

---

### السؤال 24
أي الملف (Header) يجب تضمينه لاستخدام `unique_ptr` أو `shared_ptr`؟

A- `<iostream>`
B- `<pointer>`
C- `<memory>`
D- `<smart_ptr>`

**الإجابة الصحيحة: C**
**الشرح:** ورد صراحة في المحاضرة أنه كي نتمكن من استخدام `unique_ptr` (وكذلك `shared_ptr`) علينا الاعتماد على تضمين الملف `<memory>`.

---

### السؤال 25
ما هو الفرق بين إنشاء `unique_ptr` باستخدام `make_unique<int>()` وبين استخدام `new int`؟

A- لا يوجد فرق إطلاقًا وكلاهما يُستخدمان بنفس الصياغة تمامًا
B- في المحاضرة تم التأكيد أن `unique_ptr` يُنشأ عادة باستخدام `make_unique` وليس بالاعتماد المباشر على `new`
C- `new` هي الوحيدة المسموحة مع `unique_ptr`
D- `make_unique` غير موجودة أصلًا في ++C القياسية

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة: "يتم إنشاء مؤشر جديد بالاعتماد على `make_unique` وليس الأمر `new`"، وهي الطريقة الشائعة والآمنة لإنشاء `unique_ptr`.

---

### السؤال 26
ما هو ناتج تنفيذ البرنامج التالي؟
```cpp
#include<iostream>
#include<memory>
using namespace std;
int main(){
    unique_ptr<int> ptr=make_unique<int>();
    *ptr=5;
    unique_ptr<int> x=move(ptr);
    cout<<*x<<endl;
    cout<<*ptr<<endl;
}
```

A- يطبع 5 ثم 5 مرة أخرى
B- يطبع 5 بشكل سليم، ثم استخدام `*ptr` بعد النقل يُعتبر سلوكًا خاطئًا/غير معرّف لأن ptr لم يعد يؤشر على أي كائن (أصبح فارغًا/nullptr) بعد عملية move
C- خطأ ترجمة عند السطر `unique_ptr<int> x=move(ptr);`
D- يطبع 0 ثم 5

**الإجابة الصحيحة: B**
**الشرح:** كما وضّح مثال المحاضرة تمامًا، عملية `move(ptr)` تنقل الملكية من `ptr` إلى `x`، فيصبح `ptr` فارغًا (nullptr)، وبالتالي فإن الاستمرار في استخدام `*ptr` بعد النقل يُسبب خطأً (Dereference لمؤشر فارغ)، بينما `*x` تطبع القيمة `5` بشكل سليم.

---

### السؤال 27
هل يمكن كتابة الكود التالي وترجمته بنجاح؟
```cpp
unique_ptr<int> ptr1=make_unique<int>();
unique_ptr<int> ptr2=ptr1;
```

A- نعم، لأن `unique_ptr` قابل للنسخ (Copyable) دائمًا
B- لا، لأن `unique_ptr` غير قابل للنسخ (Non-copyable)؛ لا يمكن لنسختين امتلاك نفس المورد، ويجب استخدام `move` بدلًا من النسخ المباشر
C- نعم لكن فقط في حال كان النوع `int`
D- لا، لأن `unique_ptr` لا يمكن تعريفه أصلًا داخل `main`

**الإجابة الصحيحة: B**
**الشرح:** من الخصائص التقنية المذكورة في المحاضرة أن `unique_ptr` "غير قابل للنسخ" (Non-copyable) لأن ذلك سيؤدي لوجود مالكين لنفس المورد، وهذا يخالف مبدأ الملكية الحصرية؛ الطريقة الصحيحة لنقل الملكية هي استخدام `std::move`.

---

### السؤال 28
ما هو المفهوم الأساسي الذي يعتمده `shared_ptr` خلافًا لـ `unique_ptr`؟

A- الملكية الحصرية (Exclusive Ownership)
B- الملكية المشتركة (Shared Ownership)، حيث يمكن لعدة مؤشرات أن تمتلك نفس الكائن في الذاكرة
C- عدم امتلاك أي شيء إطلاقًا (Weak Ownership فقط)
D- يعمل فقط مع الأنواع البدائية مثل int وfloat

**الإجابة الصحيحة: B**
**الشرح:** خلافًا لـ `unique_ptr` الذي يسمح بمالك واحد فقط، تتيح `shared_ptr` لعدة مؤشرات أن تمتلك نفس الكائن في الذاكرة في نفس الوقت (Reference Counting).

---

### السؤال 29
ما هو ناتج البرنامج التالي؟
```cpp
#include<iostream>
#include<memory>
using namespace std;
int main(){
    shared_ptr<int> ptr=make_shared<int>();
    *ptr=5;
    shared_ptr<int> x=ptr;
    cout<<*ptr<<endl;
    cout<<*x<<endl;
}
```

A- خطأ ترجمة لأن `shared_ptr` غير قابل للنسخ
B- 5 ثم 5 — لأن النسخ في `shared_ptr` يزيد عداد المراجع بمقدار 1 ويسمح لكلا المؤشرين بالوصول لنفس الكائن دون مشاكل
C- 5 ثم خطأ تنفيذ لأن `ptr` أصبح فارغًا بعد النسخ (تمامًا كما في unique_ptr)
D- 0 ثم 5

**الإجابة الصحيحة: B**
**الشرح:** خلافًا لـ `unique_ptr`، فإن نسخ `shared_ptr` (`x=ptr`) مسموح تمامًا ويزيد عداد المراجع (Reference Counter) بمقدار 1 دون أن يُفرغ المؤشر الأصلي؛ لذلك يبقى كل من `ptr` و`x` صالحين للاستخدام ويطبعان القيمة نفسها `5`.

---

### السؤال 30
متى يتم تدمير الكائن الذي يديره `shared_ptr` وتحرير ذاكرته فعليًا؟

A- عند تدمير أول نسخة من `shared_ptr` تشير إليه، بغض النظر عن باقي النسخ
B- فقط عندما يتم تدمير آخر `shared_ptr` يمتلك ذلك الكائن (أي عندما يصل عداد المراجع إلى صفر)
C- لا يتم تحرير الكائن أبدًا طالما البرنامج يعمل
D- عند أول استدعاء لـ `cout`

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، يتم تدمير الكائن وتحرير الذاكرة فقط عندما يتم تدمير آخر `shared_ptr` يمتلك ذلك الكائن، أي عندما يصل عداد المراجع (Reference Counter) إلى الصفر.

---

### السؤال 31
ما هي مكونات "كتلة التحكم" (Control Block) التي يستخدمها `shared_ptr` داخليًا؟

A- عداد المراجع المشتركة (Shared Counter) فقط
B- عداد المراجع المشتركة (Shared Counter)، عداد المراجع الضعيفة (Weak Counter)، والمُدمِّر (Deleter)
C- اسم المتحول وقيمته فقط
D- لا توجد كتلة تحكم في shared_ptr، هذا خاص بـ unique_ptr فقط

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، تستخدم `shared_ptr` داخليًا "كتلة تحكم" (Control Block) تتضمن: عداد المراجع المشتركة (يحسب عدد المؤشرات التي تمتلك الكائن حاليًا)، عداد المراجع الضعيفة (لعدد `weak_ptr` المرتبطة به)، والمُدمِّر (طريقة حذف الكائن).

---

### السؤال 32
لماذا تُعتبر `make_shared<T>()` الطريقة المفضلة لإنشاء `shared_ptr` مقارنة بالاعتماد المباشر على `new`؟

A- لأنها الطريقة الوحيدة المسموحة في ++C أصلًا
B- لأنها تخصص الذاكرة للكائن ولكتلة التحكم (Control Block) في عملية تخصيص واحدة (Single Allocation)، مما يزيد الكفاءة
C- لأنها تمنع استخدام عداد المراجع نهائيًا
D- لأنها أبطأ لكنها أكثر أمانًا

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، `make_shared` هي الطريقة المفضلة والآمنة لإنشاء `shared_ptr` لأنها تتميز بكفاءة أعلى؛ إذ تُخصص الذاكرة للكائن وكتلة التحكم معًا في عملية تخصيص واحدة (Single Allocation) بدلًا من عمليتين منفصلتين.

---

## القسم الرابع: أسئلة صح/خطأ سريعة (الأسئلة 33 – 38) — درجة واحدة لكل سؤال

### السؤال 33
في ++C، عملية جمع قيمة على مؤشر تنقله بمقدار بايت واحد دائمًا بغض النظر عن نوع البيانات.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** المؤشر يتنقل خانة خانة، أي بمقدار `sizeof` النوع الذي يشير إليه، وليس بايتًا واحدًا دائمًا.

---

### السؤال 34
كل مؤشر تم إنشاؤه باستخدام `new` أو `new[]` يجب تحريره يدويًا في ++C التقليدية، على عكس Java التي تعتمد على Garbage Collector.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** ++C لا تملك جامع قمامة تلقائيًا للذاكرة المحجوزة بـ `new`، لذا يجب على المبرمج استدعاء `delete` أو `delete[]` يدويًا لتفادي تسرب الذاكرة.

---

### السؤال 35
`unique_ptr` قابل للنقل (Movable) وقابل للنسخ (Copyable) في آنٍ معًا.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** `unique_ptr` قابل للنقل فقط (باستخدام `std::move`)، وغير قابل للنسخ إطلاقًا للحفاظ على مبدأ الملكية الحصرية.

---

### السؤال 36
لغة Rust تتحقق من صحة إدارة الذاكرة (Ownership & Borrowing) أثناء التنفيذ (Runtime) وليس أثناء الترجمة.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** يتم التحقق من قواعد الملكية والاستعارة في Rust أثناء الترجمة (Compile Time)، وهذا ما يميزها عن آلية جامع القمامة التي تعمل أثناء التنفيذ.

---

### السؤال 37
++C وC وRust لا تحتاج لتواجد بيئة تشغيل (Runtime) لتعمل، لذلك تُستخدم في تطوير أنظمة التشغيل والمتحكمات المصغرة.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** هذا ما ورد حرفيًا في المحاضرة كسبب رئيسي لأهمية هذه اللغات رغم عدم امتلاكها لآلية إدارة ذاكرة تلقائية بسيطة مثل جامع القمامة.

---

### السؤال 38
يمكن للمؤشر أن يصل لأي مكوّن من البيانات أو حتى إلى كود (تابع) في حال كان يشير إلى عنوان تابع، وهذا مصدر خطورة رئيسي.

A- صح
B- خطأ

**الإجابة الصحيحة: A (صح)**
**الشرح:** هذا ما ورد في المحاضرة كتوصيف لأحد أكبر المخاطر التي تعاني منها المؤشرات: إمكانية الوصول لأي مكوّن من البيانات أو كود.

---

## جدول تلخيصي سريع للمفاهيم الأساسية

| المفهوم | التوضيح المختصر |
|---|---|
| `a` (اسم مصفوفة) | يُعامل كمؤشر يحمل عنوان `&a[0]` |
| `*(a+i)` | مكافئ تمامًا لـ `a[i]` |
| تنقل المؤشر (`ptr++`) | ينتقل بمقدار `sizeof(النوع)` وليس بايتًا واحدًا |
| C++ عند تجاوز حدود مصفوفة | لا يوجد تحقق تلقائي (خلافًا لـ Java) → خطر أمني (Heartbleed) |
| `int a[n]` بحجم متغير | متاح في إصدارات ++C الجديدة (Stack) |
| `int *a=new int[n]` | الحجز الديناميكي التقليدي (Heap) |
| `delete` | لتحرير مؤشر لعنصر واحد أُنشئ بـ `new` |
| `delete[]` | لتحرير مؤشر لمصفوفة أُنشئت بـ `new[]` |
| Memory Leak | عدم تحرير الذاكرة بعد الانتهاء من استخدامها |
| Garbage Collector | تُستخدم في Python, C#, Java, JavaScript |
| Ownership & Borrowing | آلية Rust؛ يتم التحقق منها أثناء الترجمة |
| `unique_ptr` | ملكية حصرية، غير قابل للنسخ، قابل للنقل عبر `move` |
| `shared_ptr` | ملكية مشتركة، عداد مراجع، يُحذف الكائن عند وصول العداد للصفر |
| `make_unique` / `make_shared` | الطريقة المفضلة لإنشاء المؤشرات الذكية بدلًا من `new` المباشر |
| Header المطلوب | `<memory>` لاستخدام unique_ptr وshared_ptr |

---

*تم إعداد هذه الأسئلة اعتمادًا على محتوى المحاضرة الثانية (المؤشرات - الجزء الثاني) ومحاكاة أسلوب وصياغة ومستوى صعوبة نموذج امتحان الدكتور المرفق.*
