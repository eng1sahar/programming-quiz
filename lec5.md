# أسئلة مؤتمتة – المحاضرة 5: الملفات (برمجة 2)
**بأسلوب ومستوى صعوبة أسئلة د. أسامه ناصر**

> ملاحظة: إن كل المكتبات المطلوبة قد تم إدراجها في البرنامج (`#include<iostream>`, `#include<fstream>`, `using namespace std;`)

---

## القسم الأول: اختيار من متعدد (3 درجات لكل سؤال)

### السؤال 1
ما هي الوحدة الأساسية لتخزين البيانات والمعلومات في الحاسب؟

A- المجلد (Folder)
B- الملف (File)
C- القرص (Disk)
D- الذاكرة العشوائية (RAM)

**الإجابة الصحيحة: B**
**الشرح:** حسب المحاضرة، الملفات هي الوحدة الأساسية لتخزين البيانات والمعلومات في الحاسب، بينما المجلد هو مجرد وحدة تنظيمية لترتيب الملفات.

---

### السؤال 2
أي مما يلي **ليس** طريقة صحيحة لتمييز الملفات عن بعضها؟

A- اللاحقة (Extension)
B- الترويسة (Header)
C- حجم الملف بالبايت
D- تحليل معطيات الملف الداخلية (Header Analysis)

**الإجابة الصحيحة: C**
**الشرح:** الطريقتان المذكورتان في المحاضرة لتمييز الملفات هما: اللاحقة (extension) والترويسة (Header)، أما حجم الملف فلا يستخدم كآلية لتحديد نوع الملف.

---

### السؤال 3
لماذا تعتبر طريقة اللاحقة (Extension) في تمييز نوع الملف طريقة غير مضمونة؟

A- ألن بعض أنظمة التشغيل لا تدعم اللواحق
B- ألنه يمكن تغيير اللاحقة أثناء إعادة تسمية الملف دون تغيير محتواه الفعلي
C- ألن اللاحقة تشغل مساحة إضافية من الذاكرة
D- ألن اللاحقة لا تظهر إلا في نظام Windows

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة، هذه الطريقة سهلة لكنها ليست مضمونة إذ يمكن أثناء إعادة تسمية الملف تغيير اللاحقة دون أن يتغير محتوى الملف الحقيقي.

---

### السؤال 4
بفرض لدينا ملف باللاحقة `.jpg`، فإن ترويسة (Header) هذا الملف تحتوي عادةً على:

A- اسم مالك الملف وتاريخ الإنشاء فقط
B- حجم الصورة، مستوى الضغط، تفاصيل الألوان، ومعطيات فك الضغط
C- كود البرنامج المصدر الذي أنشأ الصورة
D- عدد المستخدمين الذين فتحوا الملف

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة يذكر صراحةً أن صور JPG تحتوي معلومات عن حجم الصورة، مستوى الضغط، تفاصيل الألوان، ومعطيات فك الضغط ضمن الترويسة.

---

### السؤال 5
يتم تخزين الملفات ضمن:

A- بنية خطية (Linear Structure)
B- بنية هرمية (Hierarchical Structure) تعتمد على المجلدات
C- بنية دائرية (Circular Structure)
D- بنية عشوائية بدون أي تنظيم

**الإجابة الصحيحة: B**
**الشرح:** ذكرت المحاضرة أن الملفات تُخزَّن ضمن بنية هرمية تعتمد بشكل أساسي على المجلدات، والمجلد وحدة تنظيمية تستخدم لترتيب الملفات والمجلدات الأخرى.

---

### السؤال 6
في الأنظمة القائمة على Unix (مثل Linux، MacOS، Android)، يتم التخزين ضمن مجلد أساسي يسمى:

A- Home
B- Root ويرمز له بـ `/`
C- System32
D- Drive C

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة صريح: "في الأنظمة القائمة على Unix يتم التخزين ضمن مجلد أساسي يسمى الجذر (Root) `/`".

---

### السؤال 7
في نظام Windows، تعتبر المجلدات الرئيسية أقراصاً (Drives)، وهذه الأقراص تشير إلى:

A- أقراص حقيقية فقط
B- أجزاء (Partitions) فقط
C- أقراص حقيقية أو أجزاء (Partitions) تعبر عن أقراص منطقية
D- لا شيء مما سبق، فهي وهمية بالكامل

**الإجابة الصحيحة: C**
**الشرح:** حسب المحاضرة، أقراص Windows الرئيسية تشير إلى أقراص حقيقية أو أجزاء (Partition) تعبر عن أقراص منطقية.

---

### السؤال 8
بغض النظر عن نوع الملف، يمكن النظر لكل ملف في نظام التشغيل على أنه:

A- شجرة من العقد (Tree of Nodes)
B- مصفوفة من البايتات المتتالية (Array of Bytes)
C- قائمة مترابطة من المحارف
D- كائن من نوع struct فقط

**الإجابة الصحيحة: B**
**الشرح:** المحاضرة تنص على أن "أي الملفات عبارة عن مصفوفة من البايتات Array of Bytes" بغض النظر عن نوع الملف أو محتواه.

---

### السؤال 9
يسمى الملف الذي تتم قراءته كمجموعة من المحارف بشكل تسلسلي (مشابه للقراءة من `cin`):

A- ملف ثنائي (Binary File)
B- ملف عشوائي (Random Access File)
C- ملف تسلسلي (Sequential File)
D- ملف مضغوط (Compressed File)

**الإجابة الصحيحة: C**
**الشرح:** نص المحاضرة: "يمكننا قراءة الملفات كمجموعة من المحارف مثل القراءة من cin (يسمى ذلك ملفات تسلسلية)".

---

### السؤال 10
لقراءة السطر رقم 100 في ملف تسلسلي، يجب أولاً:

A- الانتقال مباشرة إلى موقع السطر 100 في الملف
B- قراءة السطر 99 قبل الوصول إلى السطر 100 (أي قراءة كل الأسطر السابقة له بالترتيب)
C- إغلاق الملف وإعادة فتحه من نهايته
D- استخدام `seekg` للوصول المباشر بدون قراءة الأسطر السابقة

**الإجابة الصحيحة: B**
**الشرح:** خاصية الملفات التسلسلية أن القراءة تتم بالترتيب، وقد ذكرت المحاضرة صراحةً: "قراءة السطر 100 تتطلب قراءة 99 سطر قبل الوصول إلى السطر 100".

---

### السؤال 11
للتعامل مع الملفات بشكل عام في لغة ++C نحتاج لاستخدام المكتبة:

A- `<iostream>`
B- `<fstream>`
C- `<sstream>`
D- `<file>`

**الإجابة الصحيحة: B**
**الشرح:** المحاضرة تذكر بوضوح أن المكتبة اللازمة للتعامل مع الملفات في ++C هي `fstream`.

---

### السؤال 12
لفتح ملف بغرض القراءة فقط، نستخدم الصنف:

A- `ofstream` فقط
B- `ifstream` أو `fstream`
C- `sstream` فقط
D- `iostream` فقط

**الإجابة الصحيحة: B**
**الشرح:** حسب المحاضرة: "إذا كنا نريد القراءة من ملف نستخدم ifstream أو fstream".

---

### السؤال 13
بفرض الكود التالي:
```cpp
ofstream fileWriter("my-file.txt", ios::out);
ifstream fileReader("my-file.txt", ios::in);
```
هل صياغة الوسيط الثاني `ios::out` و `ios::in` هنا ضرورية؟

A- نعم، ضرورية دائماً ولا يمكن حذفها
B- لا، لأنها تمثل السلوك الافتراضي لكل من `ofstream` و `ifstream` على التوالي، وبالتالي لسنا بحاجة لإضافتها
C- نعم، لأن حذفها يسبب خطأ ترجمة (Compile Error)
D- لا يوجد فرق البتة بين استخدامها أو حذفها في أي حالة

**الإجابة الصحيحة: B**
**الشرح:** ورد في المحاضرة: "`ios::in` نفتح للقراءة (السلوك الافتراضي في ifstream وبالتالي لسنا بحاجة لإضافته)" وبالمثل بالنسبة لـ `ios::out` مع `ofstream`.

---

### السؤال 14
عند فتح ملف باستخدام `ios::app`، فإن الكتابة تتم:

A- من بداية الملف، فيتم تغيير المحتوى كاملاً
B- في نهاية الملف، فيتم زيادة المحتوى (إكمال الكتابة من حيث توقف آخر مرة)
C- في منتصف الملف دائماً
D- بشكل عشوائي حسب حجم البيانات المدخلة

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة: "app::ios إلحاق الخرج بنهاية الملف... في هذه الحالة يتم الكتابة في نهاية الملف وبالتالي يتم زيادة المحتوى (إكمال كتابة من نهاية التوقف الماضي)"، بعكس `ios::out` التي تكتب من بداية الملف وتغيّر المحتوى كاملاً.

---

### السؤال 15
ما هو الفرق بين استخدام `ios::out` وحده و `ios::binary` أثناء فتح ملف؟

A- لا يوجد أي فرق بينهما
B- `ios::binary` تفتح الملف كملف ثنائي، بينما `ios::out` تحدد وضعية الفتح للكتابة
C- `ios::binary` تستخدم فقط مع الملفات التسلسلية
D- `ios::out` تفتح الملف للقراءة فقط

**الإجابة الصحيحة: B**
**الشرح:** الوسيط الثاني في فتح الملف يحدد "حالة عملية الفتح"؛ `ios::binary` تخبر النظام أن الملف ثنائي، بينما `ios::out`/`ios::in` تحددان اتجاه العملية (كتابة/قراءة)، ويمكن دمجهما معاً.

---

### السؤال 16
بفرض الكود التالي:
```cpp
ifstream fin("my-file.txt");
if(fin.is_open())
    while(!fin.eof()){
        string x;
        fin >> x;
        cout << x;
    }
fin.close();
```
ما وظيفة التابع `eof()` في هذا الكود؟

A- يعيد `true` إذا كان الملف مفتوحاً من قبل البرنامج
B- يعيد `true` في حال وصلنا إلى نهاية الملف
C- يغلق الملف بعد الوصول إلى نهايته
D- يعيد عدد الأسطر المتبقية في الملف

**الإجابة الصحيحة: B**
**الشرح:** حسب المحاضرة: "eof هو تابع يعيد القيمة true في حال وصلنا إلى نهاية الملف"، وهو ما يُستخدم كشرط توقف حلقة القراءة.

---

### السؤال 17
في الكود الوارد في السؤال السابق، ما وظيفة `is_open()`؟

A- يعيد `true` إذا وصلنا إلى نهاية الملف
B- يعيد `true` إذا كان الملف مفتوحاً من قبل البرنامج
C- يفتح الملف تلقائياً إذا لم يكن مفتوحاً
D- يتحقق من صلاحيات الكتابة على الملف

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة: "is_open تابع يعيد القيمة true إذا كان الملف مفتوحاً من قبل البرنامج".

---

### السؤال 18
لماذا من الضروري استدعاء `fin.close()` بعد الانتهاء من التعامل مع الملف؟

A- لأن البرنامج لن يتم ترجمته (Compile) بدون استدعائها
B- لأن عدم إغلاق الملفات غير المحتاجة قد يسبب مشاكل، خصوصاً إذا كان البرنامج يفتح عدة ملفات خلال فترة التشغيل
C- لأن `close()` تطبع محتوى الملف على الشاشة تلقائياً
D- لا داعي لاستدعائها إطلاقاً لأن النظام يغلق الملف تلقائياً فور انتهاء آخر عملية قراءة

**الإجابة الصحيحة: B**
**الشرح:** ورد في المحاضرة: "في حال كان البرنامج يفتح عدداً من الملفات، لا يحتاجها خلال فترة التشغيل (أي أنه ليس بحاجة لها دائماً) لا بد من إغلاقها لحظة الانتهاء منها تجنباً لأي مشاكل".

---

### السؤال 19 (صح/خطأ)
عند التعامل مع الملفات التسلسلية، نستخدم عادةً `fstream` و `ofstream` معاً.

A- صح
B- خطأ

**الإجابة الصحيحة: B (خطأ)**
**الشرح:** نص المحاضرة يذكر أن الزوج المستخدم عادةً في الملفات التسلسلية هو `ifstream` و `ofstream`، وليس `fstream` و `ofstream`.

---

### السؤال 20
بفرض لدينا السجل التالي:
```cpp
struct student{
    long id;
    string firstName;
    string lastName;
    float points;
};
```
ما هي الطريقة الصحيحة لتمرير كائن من هذا النوع إلى تابع طباعة بهدف تجنب استنساخ القيم وهدر الذاكرة؟

A- `void output(student c)`
B- `void output(student &c)`
C- `void output(student* c)` فقط، ولا يصح استخدام المرجع
D- `void output(const student c)`

**الإجابة الصحيحة: B**
**الشرح:** نص المحاضرة صراحةً: "تم اعتماد تمرير السجل بالمرجع وليس بالقيمة تجنباً لاستنساخ القيم وهدر الذاكرة"، والتمرير بالمرجع في ++C يتم عبر `&`.

---

### السؤال 21
بفرض التابع التالي المسؤول عن إدخال بيانات طالب من ملف:
```cpp
student input(ifstream &fin){
    student c;
    fin >> c.id >> c.firstName >> c.lastName >> c.points;
    return c;
}
```
لماذا تم تمرير `fin` بالمرجع (`ifstream &fin`) وليس بالقيمة؟

A- لأن `ifstream` لا يمكن تمريره أصلاً إلا بالمرجع (لا يمكن نسخ كائنات التدفقات Streams)
B- لتسريع تنفيذ البرنامج فقط
C- لأن التمرير بالقيمة يسبب خطأ ترجمة في التوابع من نوع string فقط
D- لا فرق حقيقي، والسبب تجميلي فقط

**الإجابة الصحيحة: A**
**الشرح:** كائنات التدفقات (streams) مثل `ifstream` و `ofstream` غير قابلة للنسخ (non-copyable) في ++C، لذا يجب تمريرها دائماً بالمرجع، وهذا امتداد منطقي لمبدأ "التمرير بالمرجع لتجنب استنساخ القيم" الذي أكدته المحاضرة.

---

### السؤال 22
أي مما يلي يُعتبر من أنواع الملفات **التنفيذية** حسب التصنيف الوارد في المحاضرة؟

A- `.jpg`, `.png`
B- `.exe`, `.dll`, `.sys`
C- `.mp3`, `.wav`
D- `.zip`, `.rar`

**الإجابة الصحيحة: B**
**الشرح:** صنّفت المحاضرة الملفات التنفيذية على أنها EXE, DLL, SYS، بينما JPG/PNG صور، وMP3/WAV صوت، وZIP/RAR ملفات مضغوطة.

---

### السؤال 23
عند إنشاء `ofstream` لملف موجود مسبقاً دون تحديد `ios::app`، ماذا يحدث لمحتوى الملف الأصلي؟

A- يبقى كما هو وتتم الإضافة إلى نهايته
B- يتم حذفه بالكامل والكتابة تبدأ من جديد (Overwrite)
C- يظهر خطأ تنفيذ لأن الملف موجود مسبقاً
D- يتم دمج المحتوى الجديد مع القديم بشكل عشوائي

**الإجابة الصحيحة: B**
**الشرح:** كما ورد في المحاضرة: في حالة `ios::out` تتم الكتابة من بداية الملف وبالتالي "يتم تغيير المحتوى كاملاً"، بعكس `ios::app` التي تحافظ على المحتوى القديم وتضيف إليه.

---

## القسم الثاني: أسئلة قصيرة (درجتان لكل سؤال)

### السؤال 24
اذكر الفرق بين تمييز الملفات عبر **اللاحقة (Extension)** وتمييزها عبر **الترويسة (Header)** من حيث الموثوقية.

**الإجابة النموذجية:**
تمييز الملف عبر اللاحقة طريقة سهلة وسريعة، لكنها غير مضمونة لأن اللاحقة يمكن تغييرها يدوياً عند إعادة تسمية الملف دون أن يتغير محتواه الحقيقي. أما تمييز الملف عبر الترويسة (Header) فهو أكثر موثوقية لأنه يعتمد على معلومات فعلية مضمّنة داخل الملف تعبّر عن محتواه وتفاصيله (كحجم الصورة أو نوع الملف التنفيذي)، لكنه يتطلب آلية تحليل (Parsing) لهذه الترويسة لتحديد نوع الملف.

---

### السؤال 25
اكتب الكود اللازم لفتح ملف باسم `"data.bin"` بحيث نستطيع القراءة منه والكتابة إليه معاً وباعتباره ملفاً ثنائياً.

**الإجابة النموذجية:**
```cpp
fstream file("data.bin", ios::in | ios::out | ios::binary);
```
**الشرح:** بما أننا نحتاج القراءة والكتابة معاً، يجب استخدام `fstream` (وليس `ifstream` أو `ofstream` منفردَين)، مع دمج الأعلام (flags) الثلاثة بواسطة عامل `|` كما هو معتاد في تمارين الدكتور المتعلقة بالملفات الثنائية.

---

## جدول مرجعي سريع لأهم مفاهيم المحاضرة

| المفهوم | الوصف المختصر |
|---|---|
| الملف File | الوحدة الأساسية لتخزين البيانات في الحاسب |
| Extension | لاحقة تلي اسم الملف، طريقة سهلة لكن غير مضمونة لتمييز نوع الملف |
| Header | معلومات مضمّنة داخل الملف تصف محتواه، أكثر موثوقية لكن تحتاج تحليل |
| Root `/` | المجلد الأساسي في أنظمة Unix |
| Partition | الأقراص المنطقية في نظام Windows |
| Array of Bytes | النظرة العامة لأي ملف بغض النظر عن نوعه |
| ملف تسلسلي Sequential | يُقرأ بالترتيب كمجموعة محارف، مشابه لـ `cin`/`cout` |
| ملف ثنائي/عشوائي Binary/Random | يمكن قراءة/كتابة أي بايت في أي مكان مباشرة |
| `fstream` | المكتبة العامة للتعامل مع الملفات في ++C |
| `ifstream` | فتح ملف للقراءة (`ios::in` افتراضي) |
| `ofstream` | فتح ملف للكتابة (`ios::out` افتراضي) |
| `ios::app` | الكتابة بالإلحاق في نهاية الملف بدل استبدال المحتوى |
| `ios::binary` | فتح الملف كملف ثنائي |
| `eof()` | `true` عند الوصول إلى نهاية الملف |
| `is_open()` | `true` إذا كان الملف مفتوحاً فعلياً |
| `close()` | إغلاق الملف، ضروري لتجنب المشاكل عند فتح عدة ملفات |
| تمرير السجلات والتدفقات | يُفضّل دائماً بالمرجع (`&`) لتجنب استنساخ القيم وهدر الذاكرة |

---

*تم إعداد هذه الأسئلة آلياً بالاستناد إلى محتوى المحاضرة الخامسة (الملفات) ونموذج امتحان الدكتور أسامه ناصر لضمان التطابق في الأسلوب ومستوى الصعوبة.*
